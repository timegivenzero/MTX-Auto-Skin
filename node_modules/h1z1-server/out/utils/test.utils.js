"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createFakeCharacter = createFakeCharacter;
exports.createFakeZoneClient = createFakeZoneClient;
const zoneclient_1 = require("../servers/ZoneServer2016/classes/zoneclient");
const character_1 = require("../servers/ZoneServer2016/entities/character");
function createFakeCharacter(zone) {
    const characterId = zone.generateGuid();
    const transientId = zone.getTransientId(characterId);
    const character = new character_1.Character2016(characterId, transientId, zone);
    zone._characters[characterId] = character;
    return character;
}
function createFakeZoneClient(zone, character) {
    const randomInt = Math.floor(Math.random() * 1000);
    const client = new zoneclient_1.ZoneClient2016(randomInt, "fake", "fake", character.characterId, character.transientId, zone);
    zone._clients[randomInt] = client;
    return client;
}
//# sourceMappingURL=test.utils.js.map