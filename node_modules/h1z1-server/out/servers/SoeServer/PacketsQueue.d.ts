import { LogicalPacket } from "./logicalPacket";
export declare class PacketsQueue {
    packets: LogicalPacket[];
    CurrentByteLength: number;
    clear(): void;
    addPacket(packet: LogicalPacket): void;
}
