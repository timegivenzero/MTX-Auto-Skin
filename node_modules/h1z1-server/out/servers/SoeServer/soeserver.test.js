"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const node_test_1 = __importStar(require("node:test"));
const soeserver_1 = require("./soeserver");
const logicalPacket_1 = require("./logicalPacket");
const PacketsQueue_1 = require("./PacketsQueue");
const node_assert_1 = __importDefault(require("node:assert"));
(0, node_test_1.default)("SoeServer", { timeout: 5000 }, async (t) => {
    const soeServer = new soeserver_1.SOEServer(0, Buffer.from("1"));
    await t.test("start", async () => {
        soeServer.start();
    });
    await t.test("canBeBufferedIntoQueue", async () => {
        const queue = new PacketsQueue_1.PacketsQueue();
        const max = soeServer["_maxMultiBufferSize"];
        const maxSizelogical = new logicalPacket_1.LogicalPacket(Buffer.allocUnsafe(max), 1);
        let result = soeServer["_canBeBufferedIntoQueue"](maxSizelogical, queue);
        node_assert_1.default.strictEqual(result, true);
        const bigLogical = new logicalPacket_1.LogicalPacket(Buffer.allocUnsafe(max + 1), 1);
        result = soeServer["_canBeBufferedIntoQueue"](bigLogical, queue);
        node_assert_1.default.strictEqual(result, false);
        // if there is already bytes in the queue
        queue.CurrentByteLength += 1;
        result = soeServer["_canBeBufferedIntoQueue"](maxSizelogical, queue);
        node_assert_1.default.strictEqual(result, false);
    });
    await t.test("soeclient deletions", async () => {
        const c = soeServer["_createClient"]({
            address: "127.0.0.1",
            family: "IPv4",
            port: 0,
            size: 0
        });
        node_assert_1.default.strictEqual(soeServer["_clients"].size, 1, "Client wasn't created");
        soeServer.deleteClient(c);
        node_assert_1.default.strictEqual(soeServer["_clients"].size, 0, "Client wasn't deleted");
    });
    await t.test("stop", async () => {
        await soeServer.stop();
    });
});
(0, node_test_1.after)(() => {
    setImmediate(() => {
        process.exit(0);
    });
});
//# sourceMappingURL=soeserver.test.js.map