"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
const node_test_1 = __importStar(require("node:test"));
const loginserver_1 = require("./loginserver");
const promises_1 = require("node:timers/promises");
const isMongoTests = process.env.MONGO_TESTS === "true";
(0, node_test_1.default)("LoginServer", { timeout: 5000 }, async (t) => {
    const loginServer = new loginserver_1.LoginServer(0);
    await t.test("start", async () => {
        await loginServer.start();
    });
    await t.test("stop", async () => {
        await loginServer.stop();
    });
});
(0, node_test_1.default)("LoginServer-mongo", { timeout: 5000, skip: !isMongoTests }, async (t) => {
    const loginServer = new loginserver_1.LoginServer(0, "mongodb://localhost:27017");
    await t.test("start", async () => {
        await loginServer.start();
    });
    await promises_1.scheduler.yield();
    await t.test("stop", async () => {
        await loginServer.stop();
    });
});
(0, node_test_1.after)(() => {
    setImmediate(() => {
        process.exit(0);
    });
});
//# sourceMappingURL=loginserver.test.js.map