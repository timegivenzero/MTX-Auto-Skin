import { ZoneClient2016 as Client } from "../../classes/zoneclient";
import { ZoneServer2016 } from "../../zoneserver";
import { Command } from "./types";
import { CommandExecuteCommand } from "types/zone2016packets";
import { ReceivedPacket } from "types/shared";
export declare class CommandHandler {
    readonly commands: {
        [hash: number]: Command;
    };
    readonly internalCommands: {
        [name: string]: Command;
    };
    constructor();
    private clientHasCommandPermission;
    private indexCommands;
    executeCommand(server: ZoneServer2016, client: Client, packet: ReceivedPacket<CommandExecuteCommand>): void;
    executeInternalCommand(server: ZoneServer2016, client: Client, commandName: string, packet: any): void;
    reloadCommands(): void;
}
