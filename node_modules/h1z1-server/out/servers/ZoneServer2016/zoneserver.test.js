"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const node_test_1 = __importStar(require("node:test"));
const zoneserver_1 = require("./zoneserver");
const promises_1 = require("node:timers/promises");
const test_utils_1 = require("../../utils/test.utils");
const node_assert_1 = __importDefault(require("node:assert"));
process.env.FORCE_DISABLE_WS = "true";
const isMongoTests = process.env.MONGO_TESTS === "true";
(0, node_test_1.default)("ZoneServer2016", { timeout: 10000 }, async (t) => {
    const zone = new zoneserver_1.ZoneServer2016(0);
    await t.test("start", async () => {
        await zone.start();
    });
    await t.test("save", async () => {
        await zone.saveWorld();
    });
    await t.test("character deletion", async () => {
        const character = (0, test_utils_1.createFakeCharacter)(zone);
        (0, test_utils_1.createFakeZoneClient)(zone, character);
        node_assert_1.default.equal(Object.keys(zone._characters).length, 1, "Character not created");
        node_assert_1.default.equal(Object.keys(zone._clients).length, 1, "Client not created");
        const client = zone.getClientByCharId(character.characterId);
        if (client) {
            await zone.deleteClient(client);
            node_assert_1.default.equal(Object.keys(zone._characters).length, 0, "Character not deleted");
            node_assert_1.default.equal(Object.keys(zone._clients).length, 0, "Client not deleted");
        }
        else {
            throw "client undefined";
        }
    });
    await t.test("stop", async () => {
        await zone.stop();
    });
});
(0, node_test_1.default)("ZoneServer2016-mongo", { timeout: 10000, skip: !isMongoTests }, async (t) => {
    const zone = new zoneserver_1.ZoneServer2016(0, Buffer.from("fake"), "mongodb://localhost:27017");
    await t.test("start", async () => {
        await zone.start();
    });
    await t.test("save", async () => {
        await zone.saveWorld();
    });
    await t.test("character deletion", async () => {
        const character = (0, test_utils_1.createFakeCharacter)(zone);
        (0, test_utils_1.createFakeZoneClient)(zone, character);
        node_assert_1.default.equal(Object.keys(zone._characters).length, 1, "Character not created");
        node_assert_1.default.equal(Object.keys(zone._clients).length, 1, "Client not created");
        const client = zone.getClientByCharId(character.characterId);
        if (client) {
            await zone.deleteClient(client);
            node_assert_1.default.equal(Object.keys(zone._characters).length, 0, "Character not deleted");
            node_assert_1.default.equal(Object.keys(zone._clients).length, 0, "Client not deleted");
        }
        else {
            throw "client undefined";
        }
    });
    await promises_1.scheduler.wait(500);
    await t.test("stop", async () => {
        await zone.stop();
    });
});
(0, node_test_1.after)(() => {
    setImmediate(() => {
        process.exit(0);
    });
});
//# sourceMappingURL=zoneserver.test.js.map