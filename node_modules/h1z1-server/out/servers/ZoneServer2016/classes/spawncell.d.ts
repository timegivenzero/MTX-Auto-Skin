/** Used when a player respawns and decides a location */
export declare class SpawnCell {
    position: Float32Array;
    spawnPoints: Float32Array[];
    width: number;
    height: number;
    constructor(x: number, y: number, width: number, height: number);
}
