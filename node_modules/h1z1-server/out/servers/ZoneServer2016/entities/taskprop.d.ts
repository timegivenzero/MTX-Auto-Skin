import { ZoneServer2016 } from "../zoneserver";
import { BaseLightweightCharacter } from "./baselightweightcharacter";
import { ZoneClient2016 } from "../classes/zoneclient";
export declare class TaskProp extends BaseLightweightCharacter {
    /** ModelId of the TaskProp */
    actorModel: string;
    spawnerId: number;
    /** Id of the required item for the corresponding task */
    requiredItemId: number;
    /** Array of items to be rewarded upon  */
    rewardItems: number[];
    constructor(characterId: string, transientId: number, actorModelId: number, position: Float32Array, rotation: Float32Array, server: ZoneServer2016, scale: Float32Array, zoneId: number, renderDistance: number, actorModel: string);
    getRewardItemCount(itemId: number): number;
    getRequiredItemCount(itemId: number): number;
    getTaskPropData(): void;
    destroy(server: ZoneServer2016): boolean;
    OnInteractionString(server: ZoneServer2016, client: ZoneClient2016): void;
    OnPlayerSelect(server: ZoneServer2016, client: ZoneClient2016, isInstant?: boolean): void;
}
