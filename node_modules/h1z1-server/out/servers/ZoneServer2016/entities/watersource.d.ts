import { DamageInfo } from "types/zoneserver";
import { ZoneClient2016 } from "../classes/zoneclient";
import { ZoneServer2016 } from "../zoneserver";
import { TaskProp } from "./taskprop";
export declare class WaterSource extends TaskProp {
    /** The amount of interactions left on the WaterSource */
    usesLeft?: number;
    /** Amount to be refilled in a water bottle */
    refillAmount: number;
    /** Returns true if the fire hydrant is currently releasing water */
    isHydrantGushing: boolean;
    /** Returns true if the hydrant is currently not able to be melee'd for water */
    isHydrantOnCooldown: boolean;
    constructor(characterId: string, transientId: number, actorModelId: number, position: Float32Array, rotation: Float32Array, server: ZoneServer2016, scale: Float32Array, zoneId: number, renderDistance: number, actorModel: string, refillAmount: number);
    replenish(): void;
    OnInteractionString(server: ZoneServer2016, client: ZoneClient2016): void;
    OnPlayerSelect(server: ZoneServer2016, client: ZoneClient2016): void;
    OnMeleeHit(server: ZoneServer2016, damageInfo: DamageInfo): void;
}
