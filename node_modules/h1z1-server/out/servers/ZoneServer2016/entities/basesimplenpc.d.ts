import { AddSimpleNpc, CharacterUpdateSimpleProxyHealth } from "types/zone2016packets";
import { ZoneServer2016 } from "../zoneserver";
import { BaseEntity } from "./baseentity";
export declare abstract class BaseSimpleNpc extends BaseEntity {
    /** Maximum health alloted BaseSimpleNPC */
    maxHealth: number;
    /** Health initialized by maxHealth to determine the health percentage in pGetSimpleProxyHealth */
    health: number;
    constructor(characterId: string, transientId: number, actorModelId: number, position: Float32Array, rotation: Float32Array, server: ZoneServer2016);
    pGetSimpleNpc(): AddSimpleNpc;
    pGetSimpleProxyHealth(): CharacterUpdateSimpleProxyHealth;
}
