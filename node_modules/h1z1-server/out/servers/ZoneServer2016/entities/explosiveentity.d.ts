import { DamageInfo } from "types/zoneserver";
import { ZoneServer2016 } from "../zoneserver";
import { BaseLightweightCharacter } from "./baselightweightcharacter";
import { ZoneClient2016 } from "../classes/zoneclient";
import { BaseEntity } from "./baseentity";
export declare class ExplosiveEntity extends BaseLightweightCharacter {
    /** Id of the item - See ServerItemDefinitions.json for more information */
    itemDefinitionId: number;
    /** The distance where the explosive will render for the player */
    npcRenderDistance: number;
    /** Returns true upon explosion of the explosive */
    detonated: boolean;
    /** Used for shooting explosion with projectiles, 1 for IEDS and a coinflip between 1 and 2 for biofuel/ethanol */
    triggerExplosionShots: number;
    /** the characterId from who place this to keep track */
    ownerCharacterId: string;
    creationTime: number;
    isAwaitingExplosion: boolean;
    constructor(characterId: string, transientId: number, actorModelId: number, position: Float32Array, rotation: Float32Array, server: ZoneServer2016, itemDefinitionId: number, ownerCharacterId?: string);
    isIED(): boolean;
    isLandmine(): boolean;
    ignite(server: ZoneServer2016, client: ZoneClient2016): void;
    detonate(characterId?: string): void;
    /** Used by landmines to arm their explosivenss */
    arm(server: ZoneServer2016): Promise<void>;
    /** Called when the explosive gets hit by a projectile (bullet, arrow, etc.) */
    OnProjectileHit(server: ZoneServer2016, damageInfo: DamageInfo): void;
    OnExplosiveHit(server: ZoneServer2016, sourceEntity: BaseEntity, client?: ZoneClient2016, waitTime?: number, useRaycast?: boolean): Promise<void>;
    destroy(): boolean;
}
