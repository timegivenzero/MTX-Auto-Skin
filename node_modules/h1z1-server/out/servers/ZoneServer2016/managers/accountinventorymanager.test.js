"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
// ======================================================================
//
//   GNU GENERAL PUBLIC LICENSE
//   Version 3, 29 June 2007
//   copyright (C) 2020 - 2021 Quentin Gruber
//   copyright (C) 2021 - 2026 H1emu community
//
//   https://github.com/QuentinGruber/h1z1-server
//   https://www.npmjs.com/package/h1z1-server
//
//   Based on https://github.com/psemu/soe-network
// ======================================================================
const node_test_1 = __importStar(require("node:test"));
const zoneserver_1 = require("../zoneserver");
const node_assert_1 = __importDefault(require("node:assert"));
const h1emu_core_1 = require("h1emu-core");
const enums_1 = require("../models/enums");
process.env.FORCE_DISABLE_WS = "true";
const isMongoTests = process.env.MONGO_TESTS === "true";
(0, node_test_1.default)("AccountInventoriesManager", { timeout: 10000 }, async (t) => {
    const zone = new zoneserver_1.ZoneServer2016(0);
    await zone.start();
    const accountInventoriesManager = zone.accountInventoriesManager;
    const rndId = (0, h1emu_core_1.generate_random_guid)();
    const originalStackCount = 11;
    const item = zone.generateAccountItem(enums_1.Items.REWARD_CRATE_WASTELAND, originalStackCount);
    node_assert_1.default.strictEqual(item?.stackCount, originalStackCount, "wtf");
    (0, node_assert_1.default)(item, "Item creation failed");
    await t.test("AddAccountItem", async () => {
        await accountInventoriesManager.addAccountItem(rndId, item);
        const savedItem = await accountInventoriesManager.getAccountItem(rndId, item.itemDefinitionId);
        node_assert_1.default.strictEqual(item.itemDefinitionId, savedItem?.itemDefinitionId, "Item def doesn't match");
        node_assert_1.default.strictEqual(savedItem?.stackCount, originalStackCount, "Stack count doesn't match");
        // Since we pick the first itemDefinitionId that match this test will mostly fail
        // assert.strictEqual(item.itemGuid, savedItem?.itemGuid);
    });
    await t.test("UpdateAccountItem", async () => {
        let savedItem;
        savedItem = await accountInventoriesManager.getAccountItem(rndId, item.itemDefinitionId);
        node_assert_1.default.strictEqual(savedItem?.stackCount, originalStackCount, "Stack count doesn't match");
        const newStackCount = 2;
        item.stackCount = newStackCount;
        await accountInventoriesManager.updateAccountItem(rndId, item);
        savedItem = await accountInventoriesManager.getAccountItem(rndId, item.itemDefinitionId);
        node_assert_1.default.strictEqual(savedItem?.stackCount, newStackCount, "Stack count doesn't match after update");
    });
    await t.test("GetAccountItems", async () => {
        const savedItems = await accountInventoriesManager.getAccountItems(rndId);
        node_assert_1.default.strictEqual(savedItems?.length, 1, "Account inventory size doesn't match");
    });
    await t.test("RemoveAccountItem", async () => {
        const savedItemsLen = (await accountInventoriesManager.getAccountItems(rndId)).length;
        await accountInventoriesManager.removeAccountItem(rndId, item);
        const savedItemsLenAfterDelete = (await accountInventoriesManager.getAccountItems(rndId)).length;
        node_assert_1.default.strictEqual(savedItemsLen - 1, savedItemsLenAfterDelete, "Item isn't removed");
        const savedItem = await accountInventoriesManager.getAccountItem(rndId, item.itemDefinitionId);
        node_assert_1.default.strictEqual(savedItem, null, "Item isn't removed");
    });
});
(0, node_test_1.default)("AccountInventoriesManager-Mongo", { timeout: 10000, skip: !isMongoTests }, async (t) => {
    const zone = new zoneserver_1.ZoneServer2016(0, Buffer.from("fake"), "mongodb://localhost:27017");
    await zone.start();
    const accountInventoriesManager = zone.accountInventoriesManager;
    const rndId = (0, h1emu_core_1.generate_random_guid)();
    const originalStackCount = 11;
    const item = zone.generateAccountItem(enums_1.Items.REWARD_CRATE_WASTELAND, originalStackCount);
    node_assert_1.default.strictEqual(item?.stackCount, originalStackCount, "wtf");
    (0, node_assert_1.default)(item, "Item creation failed");
    await t.test("AddAccountItem", async () => {
        await accountInventoriesManager.addAccountItem(rndId, item);
        const savedItem = await accountInventoriesManager.getAccountItem(rndId, item.itemDefinitionId);
        node_assert_1.default.strictEqual(item.itemDefinitionId, savedItem?.itemDefinitionId, "Item def doesn't match");
        node_assert_1.default.strictEqual(savedItem?.stackCount, originalStackCount, "Stack count doesn't match");
        // Since we pick the first itemDefinitionId that match this test will mostly fail
        // assert.strictEqual(item.itemGuid, savedItem?.itemGuid);
    });
    await t.test("UpdateAccountItem", async () => {
        let savedItem;
        savedItem = await accountInventoriesManager.getAccountItem(rndId, item.itemDefinitionId);
        node_assert_1.default.strictEqual(savedItem?.stackCount, originalStackCount, "Stack count doesn't match");
        const newStackCount = 2;
        item.stackCount = newStackCount;
        await accountInventoriesManager.updateAccountItem(rndId, item);
        savedItem = await accountInventoriesManager.getAccountItem(rndId, item.itemDefinitionId);
        node_assert_1.default.strictEqual(savedItem?.stackCount, newStackCount, "Stack count doesn't match after update");
    });
    await t.test("GetAccountItems", async () => {
        const savedItems = await accountInventoriesManager.getAccountItems(rndId);
        node_assert_1.default.strictEqual(savedItems?.length, 1, "Account inventory size doesn't match");
    });
    await t.test("RemoveAccountItem", async () => {
        const savedItemsLen = (await accountInventoriesManager.getAccountItems(rndId)).length;
        await accountInventoriesManager.removeAccountItem(rndId, item);
        const savedItemsLenAfterDelete = (await accountInventoriesManager.getAccountItems(rndId)).length;
        node_assert_1.default.strictEqual(savedItemsLen - 1, savedItemsLenAfterDelete, "Item isn't removed");
        const savedItem = await accountInventoriesManager.getAccountItem(rndId, item.itemDefinitionId);
        node_assert_1.default.strictEqual(savedItem, null, "Item isn't removed");
    });
});
(0, node_test_1.after)(() => {
    setImmediate(() => {
        process.exit(0);
    });
});
//# sourceMappingURL=accountinventorymanager.test.js.map