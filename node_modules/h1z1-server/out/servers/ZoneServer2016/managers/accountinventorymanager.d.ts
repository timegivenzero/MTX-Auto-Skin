import { AccountItem } from "types/zoneserver";
import { BaseItem } from "../classes/baseItem";
import { ZoneServer2016 } from "../zoneserver";
import { Collection } from "mongodb";
export declare class AccountInventoryManager {
    isInSoloMode: boolean;
    mongodbCollection: Collection;
    private _soloDataPath;
    constructor(zoneServer: ZoneServer2016);
    init(collection: Collection): void;
    private _getSoloAccountItems;
    private _saveSoloAccountItems;
    getAccountItems(loginSessionId: string): Promise<AccountItem[]>;
    getAccountItem(loginSessionId: string, itemDefinitionId: number): Promise<AccountItem | null>;
    addAccountItem(loginSessionId: string, item: BaseItem): Promise<import("mongodb").InsertOneResult<import("bson").Document> | import("mongodb").UpdateResult<import("bson").Document> | undefined>;
    updateAccountItem(loginSessionId: string, item: BaseItem): Promise<import("mongodb").UpdateResult<import("bson").Document> | undefined>;
    removeAccountItem(loginSessionId: string, item: BaseItem): Promise<import("mongodb").DeleteResult | undefined>;
    rollbackItems(loginSessionId: string, removedItems: {
        inventoryItem: AccountItem;
        count: number;
    }[]): Promise<void>;
}
