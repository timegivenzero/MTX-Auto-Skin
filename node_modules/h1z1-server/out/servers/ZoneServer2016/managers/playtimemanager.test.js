"use strict";
// ======================================================================
//
//   GNU GENERAL PUBLIC LICENSE
//   Version 3, 29 June 2007
//   copyright (C) 2020 - 2021 Quentin Gruber
//   copyright (C) 2021 - 2026 H1emu community
//
//   https://github.com/QuentinGruber/h1z1-server
//   https://www.npmjs.com/package/h1z1-server
//
//   Based on https://github.com/psemu/soe-network
// ======================================================================
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const node_test_1 = __importStar(require("node:test"));
const zoneserver_1 = require("../zoneserver");
const test_utils_1 = require("../../../utils/test.utils");
const node_assert_1 = __importDefault(require("node:assert"));
process.env.FORCE_DISABLE_WS = "true";
const isMongoTests = process.env.MONGO_TESTS === "true";
(0, node_test_1.default)("PlayTimeManager", { timeout: 10000 }, async (t) => {
    t.mock.timers.enable({ apis: ["setInterval"] });
    const zone = new zoneserver_1.ZoneServer2016(0);
    await zone.start();
    const char = (0, test_utils_1.createFakeCharacter)(zone);
    await t.test("PlayTime incrementation", () => {
        for (let i = 1; i <= 60; i++) {
            t.mock.timers.tick(60_000);
            node_assert_1.default.strictEqual(char.playTime, i, "Playtime doesn't update well!");
        }
    });
});
(0, node_test_1.default)("PlayTimeManager-mongo", { timeout: 60000, skip: !isMongoTests }, async (t) => {
    t.mock.timers.enable({ apis: ["setInterval"] });
    const zone = new zoneserver_1.ZoneServer2016(0, Buffer.from("fake"), "mongodb://localhost:27017");
    await zone.start();
    const char = (0, test_utils_1.createFakeCharacter)(zone);
    const client = (0, test_utils_1.createFakeZoneClient)(zone, char);
    await t.test("PlayTime incrementation", () => {
        for (let i = 1; i <= 60; i++) {
            t.mock.timers.tick(60_000);
            node_assert_1.default.strictEqual(char.playTime, i, "Playtime doesn't update well!");
        }
    });
    await t.test("PlayTimeSave", async () => {
        await zone.saveWorld();
        await zone.fetchCharacterData(client);
        node_assert_1.default.strictEqual(client.character.playTime, char.playTime, "Playtime doesn't save well!");
    });
});
(0, node_test_1.after)(() => {
    setImmediate(() => {
        process.exit(0);
    });
});
//# sourceMappingURL=playtimemanager.test.js.map