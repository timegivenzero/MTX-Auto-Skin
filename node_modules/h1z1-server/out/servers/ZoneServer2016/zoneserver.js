"use strict";
// ======================================================================
//
//   GNU GENERAL PUBLIC LICENSE
//   Version 3, 29 June 2007
//   copyright (C) 2020 - 2021 Quentin Gruber
//   copyright (C) 2021 - 2026 H1emu community
//
//   https://github.com/QuentinGruber/h1z1-server
//   https://www.npmjs.com/package/h1z1-server
//
//   Based on https://github.com/psemu/soe-network
// ======================================================================
Object.defineProperty(exports, "__esModule", { value: true });
exports.ZoneServer2016 = void 0;
const debugName = "ZoneServer", debug = require("debug")(debugName);
const node_events_1 = require("node:events");
const h1z1protocol_1 = require("../../protocols/h1z1protocol");
const loginconnectionmanager_1 = require("../LoginZoneConnection/loginconnectionmanager");
const node_dns_1 = require("node:dns");
const node_util_1 = require("node:util");
const zonepackethandlers_1 = require("./zonepackethandlers");
const zoneclient_1 = require("./classes/zoneclient");
const vehicle_1 = require("./entities/vehicle");
const gridcell_1 = require("./classes/gridcell");
const spawncell_1 = require("./classes/spawncell");
const worldobjectmanager_1 = require("./managers/worldobjectmanager");
const voicechatmanager_1 = require("./managers/voicechatmanager");
const smeltingmanager_1 = require("./managers/smeltingmanager");
const decaymanager_1 = require("./managers/decaymanager");
const abilitiesmanager_1 = require("./managers/abilitiesmanager");
const enums_1 = require("./models/enums");
const weathermanager_1 = require("./managers/weathermanager");
const character_1 = require("./entities/character");
const utils_1 = require("../../utils/utils");
const itemobject_1 = require("./entities/itemobject");
const constants_1 = require("../../utils/constants");
const trapentity_1 = require("./entities/trapentity");
const doorentity_1 = require("./entities/doorentity");
const npc_1 = require("./entities/npc");
const explosiveentity_1 = require("./entities/explosiveentity");
const baselightweightcharacter_1 = require("./entities/baselightweightcharacter");
const basesimplenpc_1 = require("./entities/basesimplenpc");
const constructionparententity_1 = require("./entities/constructionparententity");
const constructionchildentity_1 = require("./entities/constructionchildentity");
const worlddatamanager_1 = require("./managers/worlddatamanager");
const useoptions_1 = require("./data/useoptions");
const enums_2 = require("../../utils/enums");
const functions_1 = require("../shared/functions");
const hookmanager_1 = require("./managers/hookmanager");
const baseItem_1 = require("./classes/baseItem");
const weapon_1 = require("./classes/weapon");
const lootbag_1 = require("./entities/lootbag");
const baselootableentity_1 = require("./entities/baselootableentity");
const lootableconstructionentity_1 = require("./entities/lootableconstructionentity");
const lootableprop_1 = require("./entities/lootableprop");
const plantingdiameter_1 = require("./entities/plantingdiameter");
const plant_1 = require("./entities/plant");
const smeltingentity_1 = require("./classes/smeltingentity");
const threads_1 = require("threads");
const processErrorHandling_1 = require("../../utils/processErrorHandling");
const chatmanager_1 = require("./managers/chatmanager");
const crate_1 = require("./entities/crate");
const configmanager_1 = require("./managers/configmanager");
const rconmanager_1 = require("./managers/rconmanager");
const groupmanager_1 = require("./managers/groupmanager");
const speedtreemanager_1 = require("./managers/speedtreemanager");
const constructionmanager_1 = require("./managers/constructionmanager");
const fairplaymanager_1 = require("./managers/fairplaymanager");
const pluginmanager_1 = require("./managers/pluginmanager");
const destroyable_1 = require("./entities/destroyable");
const soeoutputstream_1 = require("../../servers/SoeServer/soeoutputstream");
const promises_1 = require("node:timers/promises");
const h1emu_core_1 = require("h1emu-core");
const gametimemanager_1 = require("./managers/gametimemanager");
const gatewayserver_1 = require("../GatewayServer/gatewayserver");
const watersource_1 = require("./entities/watersource");
const commandhandler_1 = require("./handlers/commands/commandhandler");
const accountinventorymanager_1 = require("./managers/accountinventorymanager");
const playtimemanager_1 = require("./managers/playtimemanager");
const rewardmanager_1 = require("./managers/rewardmanager");
const node_timers_1 = require("node:timers");
const recast_1 = require("../../utils/recast");
const projectileentity_1 = require("./entities/projectileentity");
const challengemanager_1 = require("./managers/challengemanager");
const randomeventsmanager_1 = require("./managers/randomeventsmanager");
const aimanager_1 = require("./managers/aimanager");
const airdropmanager_1 = require("./managers/airdropmanager");
//import { TaskManager } from "./managers/tasksmanager";
const spawnLocations2 = require("../../../data/2016/zoneData/Z1_gridSpawns.json"), deprecatedDoors = require("../../../data/2016/sampleData/deprecatedDoors.json"), itemDefinitions = require("./../../../data/2016/dataSources/ServerItemDefinitions.json"), containerDefinitions = require("./../../../data/2016/dataSources/ContainerDefinitions.json"), profileDefinitions = require("./../../../data/2016/dataSources/ServerProfileDefinitions.json"), projectileDefinitons = require("./../../../data/2016/dataSources/ServerProjectileDefinitions.json"), itemClassDefinitions = require("./../../../data/2016/dataSources/ServerItemClassDefinitions.json"), loadoutSlotItemClasses = require("./../../../data/2016/dataSources/LoadoutSlotItemClasses.json"), equipSlotItemClasses = require("./../../../data/2016/dataSources/EquipSlotItemClasses.json"), weaponDefinitions = require("../../../data/2016/dataSources/ServerWeaponDefinitions"), resourceDefinitions = require("../../../data/2016/dataSources/Resources"), Z1_POIs = require("../../../data/2016/zoneData/Z1_POIs"), hudIndicators = require("../../../data/2016/dataSources/HudIndicators"), screenEffects = require("../../../data/2016/sampleData/screenEffects.json"), clientEffectsDataSource = require("../../../data/2016/dataSources/ClientEffects.json"), itemUseOptionsDataSource = require("../../../data/2016/dataSources/ItemUseOptions"), gameRulesSource = require("../../../data/2016/dataSources/ServerGameRules"), models = require("../../../data/2016/dataSources/Models"), accountItemConversions = require("./../../../data/2016/dataSources/AcctItemConversions.json"), rewardCrates = require("./../../../data/2016/dataSources/AccountCrates.json"), equipmentModelTexturesMapping = require("../../../data/2016/sampleData/equipmentModelTexturesMapping.json");
class ZoneServer2016 extends node_events_1.EventEmitter {
    /** Networking layer - allows sending game data to the game client,
     * lays on top of the H1Z1 protocol (on top of the actual H1Z1 packets)
     */
    _gatewayServer;
    _protocol;
    _db;
    _soloMode;
    _serverName = process.env.SERVER_NAME || "";
    _mongoAddress;
    _clientProtocol;
    _loginConnectionManager;
    _serverGuid = (0, utils_1.generateRandomGuid)();
    _worldId = 0;
    _grid = [];
    _spawnGrid = [];
    saveTimeInterval = 600000;
    nextSaveTime = Date.now() + this.saveTimeInterval;
    /** Total amount of clients on the server */
    _clients = {};
    /** Global dictionaries for all entities */
    _characters = {};
    _npcs = {};
    _spawnedItems = {};
    _plants = {};
    _doors = {};
    _explosives = {};
    _traps = {};
    _temporaryObjects = {};
    _vehicles = {};
    _lootbags = {};
    _lootableConstruction = {};
    _constructionFoundations = {};
    _constructionDoors = {};
    _constructionSimple = {};
    _lootableProps = {};
    _taskProps = {};
    _crates = {};
    _destroyables = {};
    _worldLootableConstruction = {};
    _worldSimpleConstruction = {};
    _throwableProjectiles = {};
    _destroyableDTOlist = [];
    _hudIndicators = {};
    _screenEffects = {};
    _clientEffectsData = {};
    _modelsData = {};
    _syncTeleport = {};
    blockSyncTeleportTicks = 0;
    /** Interactible options for items - See (ZonePacketHandlers.ts or datasources/ItemUseOptions) */
    _itemUseOptions = {};
    _decoys = {};
    _serverStartTime = new utils_1.TimeWrapper(0);
    _transientIds = {};
    _characterIds = {};
    /** Determines which login server is used, Localhost by default. */
    _loginServerInfo = {
        address: process.env.LOGINSERVER_IP,
        port: 1110
    };
    worldRoutineTimer;
    _allowedCommands = process.env.ALLOWED_COMMANDS
        ? JSON.parse(process.env.ALLOWED_COMMANDS)
        : [];
    /** Handles all packets for H1Z1 */
    _packetHandlers;
    /** Managers used for handling core functionalities */
    accountInventoriesManager;
    rewardManager;
    worldObjectManager;
    voiceChatManager;
    smeltingManager;
    decayManager;
    abilitiesManager;
    weatherManager;
    worldDataManager;
    hookManager;
    chatManager;
    rconManager;
    groupManager;
    speedtreeManager;
    constructionManager;
    fairPlayManager;
    pluginManager;
    configManager;
    playTimeManager;
    aiManager;
    airdropManager;
    _ready = false;
    /** Information from ServerItemDefinitions.json */
    _itemDefinitions = itemDefinitions;
    _weaponDefinitions = weaponDefinitions.WEAPON_DEFINITIONS;
    _firegroupDefinitions = weaponDefinitions.FIRE_GROUP_DEFINITIONS;
    _firemodeDefinitions = weaponDefinitions.FIRE_MODE_DEFINITIONS;
    _accountItemDefinitions = accountItemConversions;
    _rewardCrateDefinitions = rewardCrates.map((crate) => ({
        ...crate,
        rewards: crate.rewards.map((reward) => ({
            ...reward,
            rewardChance: Math.ceil(reward.rewardChance) // Don't fuck with floats, this makes the server crash since it tries to convert the reward chance into an array and floats are not valid indexes.
        }))
    }));
    itemDefinitionsCache;
    dynamicAppearanceCache;
    weaponDefinitionsCache;
    initialDataStaticDtoCache;
    projectileDefinitionsCache;
    profileDefinitionsCache;
    itemClassDefinitionsCache;
    _containerDefinitions = containerDefinitions;
    lastItemGuid = 0x3000000000000000n;
    _transientIdGenerator = (0, utils_1.generateTransientId)();
    enableWorldSaves;
    gameVersion = enums_2.GAME_VERSIONS.H1Z1_6dec_2016;
    isSaving = false;
    _isSaving = false;
    worldSaveVersion = 2;
    enablePacketInputLogging = false;
    shutdownStartedTime = 0;
    isRebooting = false;
    abortShutdown = false;
    shutdownStarted = false;
    isLocked = false;
    staticDTOs = [];
    serverGameRules;
    routinesLoopTimer;
    _mongoClient;
    rebootTimeTimer;
    inGameTimeManager = new gametimemanager_1.IngameTimeManager();
    commandHandler;
    dynamicappearance;
    /** MANAGED BY CONFIGMANAGER - See defaultConfig.yaml for more information */
    proximityItemsDistance;
    interactionDistance;
    charactersRenderDistance;
    tickRate;
    worldRoutineRate;
    welcomeMessage;
    adminMessage;
    enableLoginServerKickRequests;
    rebootTime; // in hours
    rebootWarnTime; // in seconds
    isPvE;
    isHeadshotOnly;
    isFirstPersonOnly;
    isNoBuildInPois;
    baseConstructionDamage;
    crowbarHitRewardChance;
    crowbarHitDamage;
    damageWeapons;
    disablePOIManager;
    disableMapBoundsCheck;
    /*                          */
    navManager;
    staticBuildings = require("../../../data/2016/sampleData/staticbuildings.json");
    worldSaveFailed = false;
    challengeManager;
    challengePositionCheckInterval;
    randomEventsManager;
    gameMode = enums_1.GameModes.SURVIVAL;
    maxPacketLoss = 5;
    //tasksManager: TaskManager;
    //clientRoutineRate!: number;
    constructor(serverPort, gatewayKey = Buffer.from(constants_1.DEFAULT_CRYPTO_KEY), mongoAddress = "", worldId, internalServerPort, protocol = "ClientProtocol_1080") {
        super();
        this._clientProtocol = protocol;
        this._gatewayServer = new gatewayserver_1.GatewayServer(serverPort, gatewayKey);
        this._packetHandlers = new zonepackethandlers_1.ZonePacketHandlers();
        this._mongoAddress = mongoAddress;
        this._worldId = worldId || 0;
        this._protocol = new h1z1protocol_1.H1Z1Protocol(this._clientProtocol);
        this.worldObjectManager = new worldobjectmanager_1.WorldObjectManager();
        this.voiceChatManager = new voicechatmanager_1.VoiceChatManager();
        this.smeltingManager = new smeltingmanager_1.SmeltingManager();
        this.decayManager = new decaymanager_1.DecayManager();
        this.abilitiesManager = new abilitiesmanager_1.AbilitiesManager();
        this.weatherManager = new weathermanager_1.WeatherManager();
        this.hookManager = new hookmanager_1.HookManager();
        this.chatManager = new chatmanager_1.ChatManager();
        this.rconManager = new rconmanager_1.RConManager();
        this.groupManager = new groupmanager_1.GroupManager();
        this.speedtreeManager = new speedtreemanager_1.SpeedTreeManager();
        this.rewardManager = new rewardmanager_1.RewardManager(this);
        this.constructionManager = new constructionmanager_1.ConstructionManager();
        this.fairPlayManager = new fairplaymanager_1.FairPlayManager();
        this.pluginManager = new pluginmanager_1.PluginManager();
        this.commandHandler = new commandhandler_1.CommandHandler();
        this.playTimeManager = new playtimemanager_1.PlayTimeManager();
        this.aiManager = new aimanager_1.AiManager(this);
        this.airdropManager = new airdropmanager_1.AirdropManager(this);
        this.navManager = new recast_1.NavManager();
        this.challengeManager = new challengemanager_1.ChallengeManager(this);
        this.randomEventsManager = new randomeventsmanager_1.RandomEventsManager(this);
        //this.tasksManager = new TaskManager();
        /* CONFIG MANAGER MUST BE INSTANTIATED LAST ! */
        this.configManager = new configmanager_1.ConfigManager(this, process.env.CONFIG_PATH);
        this.enableWorldSaves =
            process.env.ENABLE_SAVES?.toLowerCase() == "false" ? false : true;
        /* Managers events */
        this.weatherManager.on("raining", () => {
            this.smeltingManager.fillDewCollectors(this);
        });
        /** Determines what rulesets are used. */
        const serverGameRules = [];
        if (this.isSurvival()) {
            serverGameRules.push(this.isPvE ? "PvE" : "PvP");
            if (this.isFirstPersonOnly)
                serverGameRules.push("FirstPersonOnly");
            if (this.isHeadshotOnly)
                serverGameRules.push("Headshots");
            if (this.isNoBuildInPois)
                serverGameRules.push("NoBuildNearPois");
        }
        else if (this.isBattleRoyale()) {
            switch (this.gameMode) {
                case enums_1.GameModes.BATTLE_ROYALE:
                    serverGameRules.push("BattleRoyale");
                    break;
                case enums_1.GameModes.BATTLE_ROYALE_2:
                    serverGameRules.push("BattleRoyaleTwoPlayerTeams");
                    break;
                case enums_1.GameModes.BATTLE_ROYALE_5:
                    serverGameRules.push("BattleRoyaleFivePlayerTeams");
                    break;
            }
        }
        this.serverGameRules = serverGameRules.join(",");
        this._soloMode = false;
        if (!this._mongoAddress) {
            this._soloMode = true;
            this.fairPlayManager.useFairPlay = false;
            console.log("Server in solo mode !");
        }
        this.accountInventoriesManager = new accountinventorymanager_1.AccountInventoryManager(this);
        this.on("login", async (client) => {
            if (!this._soloMode) {
                this.sendZonePopulationUpdate();
            }
            await this.onZoneLoginEvent(client);
        });
        this._gatewayServer.on("login", async (soeClientId, characterId, guid, clientProtocol) => {
            debug(`Client logged in from ${soeClientId} with character id: ${characterId}`);
            if (!this._soloMode) {
                this._loginConnectionManager.sendData({
                    ...this._loginServerInfo,
                    serverId: this._worldId
                }, "ClientMessage", {
                    guid,
                    message: `Connected to server with id: ${this._worldId}`,
                    showConsole: false,
                    clearOutput: true
                });
            }
            const generatedTransient = this.getTransientId(characterId);
            const sessionId = this._gatewayServer.getSoeClientSessionId(soeClientId);
            if (!sessionId) {
                return;
            }
            const zoneClient = this.createClient(sessionId, soeClientId, guid, characterId, generatedTransient);
            if (clientProtocol !== this._clientProtocol) {
                debug(`${soeClientId} is using the wrong client protocol`);
                debug(`Protocol ${clientProtocol}`);
                this.sendData(zoneClient, "LoginFailed", {});
                return;
            }
            if (!this._soloMode) {
                const address = this._gatewayServer.getSoeClientNetworkInfos(soeClientId)?.address;
                if (!address) {
                    return;
                }
                if (await this.isClientBanned(zoneClient.loginSessionId, address, zoneClient)) {
                    this.enforceBan(zoneClient);
                    return;
                }
                const adminData = (await this._db
                    ?.collection(enums_2.DB_COLLECTIONS.ADMINS)
                    .findOne({
                    sessionId: zoneClient.loginSessionId
                }));
                if (adminData) {
                    zoneClient.isAdmin = true;
                    zoneClient.permissionLevel = adminData.permissionLevel ?? 3;
                }
            }
            else {
                zoneClient.isAdmin = true;
                zoneClient.permissionLevel = 3;
            }
            // If the client is already connected, we kick the old one
            const oldClient = this.getClientByGuid(guid);
            if (oldClient) {
                this.sendData(oldClient, "LoginFailed", {});
                await this.deleteClient(oldClient);
            }
            this._clients[sessionId] = zoneClient;
            this._characters[characterId] = zoneClient.character;
            this.emit("login", zoneClient);
        });
        this._gatewayServer.on("disconnect", (sessionId) => {
            // this happen when the connection is close without a regular logout
            const client = this._clients[sessionId];
            if (client) {
                if (client.properlyLogout) {
                    this.deleteClient(client);
                }
                else {
                    setTimeout(() => {
                        // Double-check client still exists before deleting
                        if (this._clients[sessionId]) {
                            this.deleteClient(client);
                        }
                    }, 10_000);
                }
            }
        });
        this._gatewayServer.on("tunneldata", async (soeClientSessionId, data, flags) => {
            if (!this._soloMode && this.enablePacketInputLogging) {
                this._db.collection("packets").insertOne({
                    data,
                    flags,
                    loginSessionId: this._clients[soeClientSessionId].loginSessionId
                });
            }
            // Idk if this is really efficient , we may lose too much time on context switching
            // if (flags < GatewayChannels.UpdatePosition) {
            //   // if the packet isn't a high priority one, we can wait for the next tick to process it
            //   // If there is a lot of packet to process, it's better, if there is none then we only add like some µsec
            //   await scheduler.yield();
            // }
            const packet = this._protocol.parse(data, flags);
            // for reversing new packets
            /*
            if(
              packet?.name == ""
            ) {
              let hexString = '';
              for (let i = 0; i < data.length; i++) {
                  const byte = data[i].toString(16).padStart(2, '0');
                  hexString += byte + ' ';
              }
              console.log(`<Buffer ${hexString.trim()}>`);
            }
            */
            if (packet) {
                this.onZoneDataEvent(this._clients[soeClientSessionId], packet);
            }
            else {
                debug("zonefailed : ", data);
            }
        });
        if (!this._soloMode) {
            this.registerLoginConnectionListeners(internalServerPort);
        }
        const dynamicappearance = (0, utils_1.loadJson)(__dirname + "/../../../data/2016/sampleData/dynamicappearance.json");
        this.dynamicappearance = dynamicappearance;
        if (this._mongoAddress && this.rebootTime) {
            console.log("Reboot time set to " + this.rebootTime + " hours");
            this.rebootTimeTimer = setTimeout(() => {
                console.log("Rebooting server due to reboot time set");
                this.shutdown(this.rebootWarnTime, "Server rebooting");
            }, this.rebootTime * 60 * 60 * 1000);
        }
        for (let i = 0; i < this.staticBuildings.length; i++) {
            const v = this.staticBuildings[i];
            v.characterId = this.generateGuid();
            v.transientId = this.getTransientId(v.characterId);
        }
    }
    /*get gameLoopUpdateRate() {
      return 1000 / this.gameLoopTickRate;
    }*/
    async reloadCommandCache() {
        delete require.cache[require.resolve("./handlers/commands/commandhandler")];
        const CommandHandler = require("./handlers/commands/commandhandler").CommandHandler;
        this.commandHandler = new CommandHandler();
        this.commandHandler.reloadCommands();
    }
    async registerLoginConnectionListeners(internalServerPort) {
        this._loginConnectionManager = new loginconnectionmanager_1.LoginConnectionManager(this._worldId, internalServerPort); // opens local socket to connect to loginserver
        this._loginConnectionManager.on("session", (err, client) => {
            if (err) {
                debug(`An error occured for LoginConnection with ${client.sessionId}`);
                console.error(err);
            }
            else {
                this.sendZonePopulationUpdate();
                debug(`LoginConnection established for ${client.sessionId}`);
            }
        });
        this._loginConnectionManager.on("sessionfailed", (err, client) => {
            console.error(`h1emuServer sessionfailed for ${client.sessionId}`);
            console.error(err);
            process.exitCode = 11;
        });
        this._loginConnectionManager.on("disconnect", (err, client, reason) => {
            debug(`LoginConnection dropped: ${reason ? "Connection Lost" : "Unknown Error"}`);
        });
        this._loginConnectionManager.on("data", async (err, client, packet) => {
            if (err) {
                console.error(err);
            }
            else {
                switch (packet.name) {
                    case "CharacterCreateRequest":
                        this.onCharacterCreateRequest(client, packet);
                        break;
                    case "ClientIsAdminRequest":
                        this.onClientIsAdminRequest(client, packet);
                        break;
                    case "CharacterAllowedRequest":
                        this.onClientAllowedRequest(client, packet);
                        break;
                    case "CharacterDeleteRequest": {
                        const { characterId, reqId } = packet.data;
                        try {
                            const collection = this._db.collection(enums_2.DB_COLLECTIONS.CHARACTERS);
                            const charactersArray = await collection
                                .find({ characterId: characterId })
                                .toArray();
                            if (charactersArray.length === 1) {
                                await collection.updateOne({ characterId: characterId }, {
                                    $set: {
                                        status: 0
                                    }
                                });
                                this._loginConnectionManager.sendData(client, "CharacterDeleteReply", { status: 1, reqId: reqId });
                                const groupId = charactersArray[0]?.groupId;
                                if (groupId) {
                                    this.groupManager.removeGroupMember(this, characterId, groupId);
                                    if (this.constructionManager.hasOwnedBases(this, characterId)) {
                                        this.constructionManager.removeOwnerFromBases(this, characterId);
                                    }
                                }
                            }
                            else {
                                this._loginConnectionManager.sendData(client, "CharacterDeleteReply", { status: 1, reqId: reqId });
                            }
                        }
                        catch (error) {
                            console.error(error);
                            this._loginConnectionManager.sendData(client, "CharacterDeleteReply", {
                                status: 0,
                                reqId: reqId
                            });
                        }
                        break;
                    }
                    case "LoginKickRequest": {
                        const { guid, reason } = packet.data;
                        if (!this.enableLoginServerKickRequests) {
                            console.log(`LoginServer requested to kick client with guid ${guid} for reason: ${reason}! Ignored due to configuration setting.`);
                            return;
                        }
                        let reasonString = "";
                        switch (reason) {
                            case enums_2.LOGIN_KICK_REASON.UNDEFINED:
                                reasonString = "UNDEFINED";
                                break;
                            case enums_2.LOGIN_KICK_REASON.GLOBAL_BAN:
                                reasonString = "Global ban.";
                                break;
                            default:
                                reasonString = "INVALID";
                                break;
                        }
                        const client = this.getClientByGuid(guid);
                        if (!client) {
                            console.log(`LoginServer requested to kick INVALID client with guid ${guid} for reason: ${reason}!`);
                            return;
                        }
                        console.log(`LoginServer kicking ${client.character.name} for reason: ${reasonString}`);
                        this.kickPlayerWithReason(client, reasonString);
                        break;
                    }
                    case "OverrideAllowedFileHashes":
                        debug("Received OverrideAllowedFileHashes from loginserver");
                        const hashTypesList = packet.data.types, assetHashes = hashTypesList.find((entry) => entry.type == "assets");
                        if (assetHashes) {
                            debug("Using defaultAssetHashes from loginserver");
                            this.fairPlayManager.defaultHashes = assetHashes.hashes;
                        }
                        break;
                    default:
                        debug(`Unhandled h1emu packet: ${packet.name}`);
                        break;
                }
            }
        });
    }
    async stop() {
        (0, node_timers_1.clearInterval)(this.challengePositionCheckInterval);
        this.worldDataManager.kill();
        this.inGameTimeManager.stop();
        this.smeltingManager.clearTimers();
        this.decayManager.clearTimers();
        this.randomEventsManager.stop();
        clearTimeout(this.worldRoutineTimer);
        clearTimeout(this.weatherManager.dynamicWorker);
        clearTimeout(this.routinesLoopTimer);
        clearTimeout(this.rebootTimeTimer);
        if (this._loginConnectionManager) {
            await this._loginConnectionManager.stop();
        }
        if (this._mongoClient) {
            await this._mongoClient.close();
        }
        await this._gatewayServer.stop();
        await this.rconManager.stop();
    }
    async shutdown(timeLeft, message) {
        this.shutdownStarted = true;
        if (this.abortShutdown) {
            this.abortShutdown = false;
            this.shutdownStarted = false;
            this.sendAlertToAll(`Server shutdown aborted.`);
            return;
        }
        if (this.shutdownStartedTime === 0) {
            this.shutdownStartedTime = Date.now();
        }
        const timeLeftMs = timeLeft * 1000;
        const currentTimeLeft = timeLeftMs - (Date.now() - this.shutdownStartedTime);
        if (currentTimeLeft < 0) {
            this.sendAlertToAll(`Server will shutdown now`);
            this.enableWorldSaves = false;
            if (!this.worldSaveFailed) {
                try {
                    await this.saveWorld();
                }
                catch (e) {
                    console.error(e);
                    console.error("saving world failed on reboot");
                }
            }
            Object.values(this._clients).forEach((client) => {
                this.sendData(client, "CharacterSelectSessionResponse", {
                    status: 0,
                    sessionId: client.loginSessionId
                });
            });
            setTimeout(async () => {
                await this.stop();
                process.exit(0);
            }, 30000);
        }
        else {
            this.sendAlertToAll(`Server will shutdown in ${Math.ceil(currentTimeLeft / 1000)} seconds. Reason: ${message}`);
            if (currentTimeLeft / 1000 <= 60) {
                // block client connections for last minute
                this.isRebooting = true;
            }
            setTimeout(() => this.shutdown(timeLeft, message), currentTimeLeft <= 60 * 1000 ? timeLeftMs / 6 : timeLeftMs / 10);
        }
    }
    async onZoneLoginEvent(client) {
        debug("zone login");
        try {
            await this.sendInitData(client);
        }
        catch (error) {
            console.log(error);
            this.sendData(client, "LoginFailed", {});
            if (!this._soloMode) {
                await this._db.collection(enums_2.DB_COLLECTIONS.LOGIN_ERRORS).insertOne({
                    error,
                    guid: client.loginSessionId,
                    characterId: client.character.characterId
                });
            }
        }
        /*this.tasksManager.register(async () => {
          debug("zone login");
          try {
            await this.sendInitData(client);
          } catch (error) {
            console.log(error);
            this.sendData<LoginFailed>(client, "LoginFailed", {});
            if (!this._soloMode) {
              await this._db.collection(DB_COLLECTIONS.LOGIN_ERRORS).insertOne({
                error,
                guid: client.loginSessionId,
                characterId: client.character.characterId
              });
            }
          }
        });*/
    }
    onZoneDataEvent(client, packet) {
        if (!client) {
            return;
        }
        if (packet.name != "Command.ExecuteCommand" &&
            packet.name != "H1emu.RequestModules" &&
            packet.name != "KeepAlive" &&
            packet.name != "PlayerUpdatePosition" &&
            packet.name != "PlayerUpdateManagedPosition" &&
            packet.name != "ClientUpdate.MonitorTimeDrift") {
            debug(`Receive Data ${[packet.name]}`);
        }
        if (packet.flag === h1emu_core_1.GatewayChannels.UpdatePosition) {
            if (!packet.data.flags)
                return;
            const movingCharacter = this._characters[client.character.characterId];
            if (movingCharacter) {
                this.sendRawToAllOthersWithSpawnedCharacter(client, movingCharacter.characterId, this._protocol.createPositionBroadcast2016(packet.data.raw, movingCharacter.transientId));
            }
            /*this.tasksManager.register(async () => {
              if (!packet.data.flags) return;
              const movingCharacter = this._characters[client.character.characterId];
              if (movingCharacter) {
                this.sendRawToAllOthersWithSpawnedCharacter(
                  client,
                  movingCharacter.characterId,
                  this._protocol.createPositionBroadcast2016(
                    packet.data.raw,
                    movingCharacter.transientId
                  )
                );
              }
            }, true);*/
        }
        try {
            this._packetHandlers.processPacket(this, client, packet);
        }
        catch (error) {
            console.error(error);
            console.error(`An error occurred while processing a packet : `, packet);
            (0, processErrorHandling_1.logVersion)();
        }
        /*this.tasksManager.register(async () => {
          try {
            this._packetHandlers.processPacket(
              this,
              client,
              packet as ReceivedPacket<any>
            );
          } catch (error) {
            console.error(error);
            console.error(`An error occurred while processing a packet : `, packet);
            logVersion();
          }
        });*/
    }
    async getIsAdmin(loginSessionId) {
        return Boolean(await this._db
            ?.collection(enums_2.DB_COLLECTIONS.ADMINS)
            .findOne({ sessionId: loginSessionId, permissionLevel: { $ne: 0 } }));
    }
    async onCharacterCreateRequest(client, packet) {
        const { characterObjStringify, reqId } = packet.data;
        try {
            const characterData = JSON.parse(characterObjStringify), characterModelData = (0, functions_1.getCharacterModelData)(characterData.payload);
            let character = require("../../../data/2016/sampleData/character.json");
            character = {
                ...character,
                serverId: characterData.serverId,
                creationDate: (0, utils_1.toHex)(Date.now()),
                lastLoginDate: (0, utils_1.toHex)(Date.now()),
                characterId: characterData.characterId,
                ownerId: characterData.ownerId,
                characterName: characterData.payload.characterName,
                actorModelId: characterModelData.modelId,
                headActor: characterModelData.headActor,
                hairModel: characterModelData.hairModel,
                gender: characterData.payload.gender,
                status: 1,
                playTime: 0,
                worldSaveVersion: this.worldSaveVersion
            };
            const collection = this._db.collection(enums_2.DB_COLLECTIONS.CHARACTERS);
            const charactersArray = await collection.findOne({
                characterId: character.characterId
            });
            if (!charactersArray) {
                const result = await collection.insertOne(character);
                if (!result.insertedId || !result.acknowledged) {
                    console.error(result);
                    throw new Error("Failed to insert character into database");
                }
            }
            this._loginConnectionManager.sendData(client, "CharacterCreateReply", {
                reqId: reqId,
                status: 1
            });
        }
        catch (error) {
            console.error(error);
            this._loginConnectionManager.sendData(client, "CharacterCreateReply", {
                reqId: reqId,
                status: 0
            });
        }
    }
    async onClientIsAdminRequest(client, packet) {
        const { guid, reqId } = packet.data;
        const isAdmin = await this.getIsAdmin(guid);
        try {
            this._loginConnectionManager.sendData(client, "ClientIsAdminReply", {
                reqId: reqId,
                status: isAdmin
            });
        }
        catch (error) {
            console.error(error);
            this._loginConnectionManager.sendData(client, "ClientIsAdminReply", {
                reqId: reqId,
                status: 0
            });
        }
    }
    sendCharacterAllowedReply(client, reqId, status, rejectionFlag, message = "") {
        this._loginConnectionManager.sendData(client, "CharacterAllowedReply", {
            reqId,
            status: status ? 1 : 0,
            rejectionFlag,
            message
        });
    }
    async onClientAllowedRequest(client, packet) {
        const { characterId, loginSessionId, reqId } = packet.data;
        if (this.isRebooting) {
            console.log(`Character (${characterId}) connection rejected due to reboot`);
            this.sendCharacterAllowedReply(client, reqId, false, enums_2.CONNECTION_REJECTION_FLAGS.SERVER_REBOOT);
            return;
        }
        if (this.isLocked && !(await this.getIsAdmin(loginSessionId))) {
            console.log(`Character (${characterId}) connection rejected due to server lock`);
            this.sendCharacterAllowedReply(client, reqId, false, enums_2.CONNECTION_REJECTION_FLAGS.SERVER_LOCKED);
            return;
        }
        const ban = await this.isClientBanned(loginSessionId, client.address);
        if (ban) {
            console.log(`Character (${characterId}) connection rejected due to local ban`);
            const unbanTime = ban.expirationDate
                ? (0, utils_1.getDateString)(ban.expirationDate)
                : 0, reason = ban.banReason;
            const reasonString = `You have been ${unbanTime ? "" : "permanently "}banned from the server${unbanTime ? ` until ${unbanTime}` : ""}. Reason: ${reason}.`;
            this.sendCharacterAllowedReply(client, reqId, false, enums_2.CONNECTION_REJECTION_FLAGS.LOCAL_BAN, reasonString);
            return;
        }
        const rejectionFlags = packet.data.rejectionFlags ?? [];
        try {
            for (let i = 0; i < rejectionFlags.length; i++) {
                const rejectionFlag = rejectionFlags[i].rejectionFlag;
                if (this.fairPlayManager.acceptedRejectionTypes.includes(rejectionFlag)) {
                    if (rejectionFlag === enums_2.CONNECTION_REJECTION_FLAGS.VPN) {
                        const userIsAllowedToUseVPN = await this._db
                            .collection(enums_2.DB_COLLECTIONS.VPN_WHITELIST)
                            .findOne({
                            zoneSessionId: loginSessionId
                        });
                        if (userIsAllowedToUseVPN) {
                            continue;
                        }
                    }
                    console.log(`Character (${characterId}) connection rejected due to rejection type ${rejectionFlag}`);
                    this.sendCharacterAllowedReply(client, reqId, false, rejectionFlag);
                    return;
                }
            }
            const collection = this._db.collection(enums_2.DB_COLLECTIONS.CHARACTERS);
            const character = await collection.findOne({
                characterId: characterId,
                serverId: this._worldId,
                status: 1
            });
            if (!character) {
                this.sendCharacterAllowedReply(client, reqId, false, enums_2.CONNECTION_REJECTION_FLAGS.CHARACTER_NOT_FOUND);
            }
            this.sendCharacterAllowedReply(client, reqId, true);
        }
        catch (error) {
            console.log(error);
            this.sendCharacterAllowedReply(client, reqId, false, enums_2.CONNECTION_REJECTION_FLAGS.ERROR);
        }
    }
    getProximityItems(client) {
        const proximityItems = { items: [] };
        let foundation = this._constructionFoundations[client.character.insideBuilding] ??
            this._constructionSimple[client.character.insideBuilding]?.getParentFoundation(this);
        for (const object of client.spawnedEntities) {
            if (object instanceof plant_1.Plant)
                continue; // Don't include plants (seeds and crops)
            if (object instanceof itemobject_1.ItemObject) {
                let yDistance = 1;
                if (client.character.state.position[1] <
                    object.state.position[1] - 0.5) {
                    yDistance = 1.8;
                }
                if ((0, utils_1.isPosInRadiusWithY)(this.proximityItemsDistance, client.character.state.position, object.state.position, yDistance)) {
                    if (object.checkBuildingObstruct(this, client.character.state.position, foundation))
                        continue;
                    const proximityItem = {
                        itemDefinitionId: object.item.itemDefinitionId,
                        associatedCharacterGuid: object.characterId,
                        itemData: {
                            itemDefinitionId: object.item.itemDefinitionId,
                            tintId: 0,
                            guid: object.item.itemGuid,
                            count: object.item.stackCount,
                            itemSubData: {
                                hasSubData: false
                            },
                            unknownBoolean1: true,
                            ownerCharacterId: object.characterId,
                            unknownDword9: 1
                        }
                    };
                    proximityItems.items.push(proximityItem);
                }
            }
            if (object instanceof lootableconstructionentity_1.LootableConstructionEntity) {
                // If player is underneath, allow further Y distance
                let yDistance = 1;
                if (client.character.state.position[1] <
                    object.state.position[1] - 0.5) {
                    yDistance = 3;
                }
                if ((0, utils_1.isPosInRadiusWithY)(2, // radius stays 2
                client.character.state.position, object.state.position, yDistance // only Y-difference changes
                ) ||
                    object.mountedCharacter == client.character.characterId) {
                    if (object.parentObjectCharacterId) {
                        const parent = object.getParent(this);
                        if (parent &&
                            parent.isSecured &&
                            client.character.isHidden != parent.characterId) {
                            continue;
                        }
                    }
                    const container = object.getContainer();
                    if (container) {
                        Object.values(container.items).forEach((item) => {
                            const proximityItem = {
                                itemDefinitionId: item.itemDefinitionId,
                                associatedCharacterGuid: client.character.characterId,
                                itemData: object.pGetItemData(this, item, container.containerDefinitionId)
                            };
                            proximityItems.items.push(proximityItem);
                        });
                    }
                }
            }
            if (object instanceof lootableprop_1.LootableProp) {
                if ((0, utils_1.isPosInRadiusWithY)(2, client.character.state.position, object.state.position, 1) &&
                    client.searchedProps.includes(object)) {
                    const container = object.getContainer();
                    if (container) {
                        Object.values(container.items).forEach((item) => {
                            const proximityItem = {
                                itemDefinitionId: item.itemDefinitionId,
                                associatedCharacterGuid: client.character.characterId,
                                itemData: object.pGetItemData(this, item, container.containerDefinitionId)
                            };
                            proximityItems.items.push(proximityItem);
                        });
                    }
                }
            }
            if (object instanceof lootbag_1.Lootbag) {
                if ((0, utils_1.isPosInRadiusWithY)(2, client.character.state.position, object.state.position, 1)) {
                    const container = object.getContainer();
                    if (container) {
                        Object.values(container.items).forEach((item) => {
                            const proximityItem = {
                                itemDefinitionId: item.itemDefinitionId,
                                associatedCharacterGuid: client.character.characterId,
                                itemData: object.pGetItemData(this, item, container.containerDefinitionId)
                            };
                            proximityItems.items.push(proximityItem);
                        });
                    }
                }
            }
        }
        return proximityItems;
    }
    getCraftingProximityItems(client) {
        const proximityItems = { items: [] };
        for (const gridCell of this._grid) {
            if (!(0, utils_1.isPosInRadius)(client.chunkRenderDistance, gridCell.position, client.character.state.position))
                continue;
            for (const object of gridCell.objects) {
                if (object instanceof itemobject_1.ItemObject) {
                    if ((0, utils_1.isPosInRadiusWithY)(this.proximityItemsDistance, client.character.state.position, object.state.position, 1)) {
                        const proximityItem = {
                            itemDefinitionId: object.item.itemDefinitionId,
                            associatedCharacterGuid: object.characterId,
                            itemData: {
                                itemDefinitionId: object.item.itemDefinitionId,
                                tintId: 0,
                                guid: object.item.itemGuid,
                                count: object.item.stackCount,
                                itemSubData: {
                                    hasSubData: false
                                },
                                unknownBoolean1: true,
                                ownerCharacterId: object.characterId,
                                unknownDword9: 1
                            }
                        };
                        proximityItems.items.push(proximityItem);
                    }
                }
            }
        }
        return proximityItems;
    }
    async fetchCharacterData(client) {
        if (!this.hookManager.checkHook("OnSendCharacterData", client))
            return;
        if (!(await this.hookManager.checkAsyncHook("OnSendCharacterData", client)))
            return;
        let savedCharacter;
        try {
            savedCharacter = await this.worldDataManager.fetchCharacterData(client.character.characterId);
        }
        catch (e) {
            console.log(e);
            this.sendData(client, "LoginFailed", {});
            return;
        }
        await this.loadCharacterData(client, savedCharacter);
        client.startingPos = client.character.state.position;
    }
    sendCharacterData(client, accountItems) {
        this.sendData(client, "SendSelfToClient", client.character.pGetSendSelf(this, client, accountItems));
        client.character.initialized = true;
        this.initializeContainerList(client);
        this._characters[client.character.characterId] = client.character; // character will spawn on other player's screen(s) at this point
        this.hookManager.checkHook("OnSentCharacterData", client);
    }
    /**
     * Caches item definitons so they aren't packed every time a client logs in.
     */
    packItemDefinitions() {
        /*
        this.itemDefinitionsCache = this._protocol.pack("Command.ItemDefinitions", {
          data: {
            itemDefinitions: Object.values(this._itemDefinitions).map(
              (itemDef: any) => {
                return {
                  ID: itemDef.ID,
                  definitionData: {
                    ...itemDef,
                    HUD_IMAGE_SET_ID: itemDef.IMAGE_SET_ID,
                    ITEM_TYPE_1: itemDef.ITEM_TYPE,
                    flags1: {
                      ...itemDef,
                    },
                    flags2: {
                      ...itemDef,
                    },
                    stats: [],
                  },
                };
              }
            ),
          },
        });
        */
        // temp custom logic for items with custom itemDefintion data
        const defs = [];
        Object.values(this._itemDefinitions).forEach((itemDef) => {
            switch (itemDef.ID) {
                case enums_1.Items.FANNY_PACK_DEV:
                case enums_1.Items.HEADLIGHTS_ATV:
                case enums_1.Items.HEADLIGHTS_OFFROADER:
                case enums_1.Items.HEADLIGHTS_PICKUP:
                case enums_1.Items.HEADLIGHTS_POLICE:
                case enums_1.Items.AIRDROP_CODE:
                    defs.push({
                        ID: itemDef.ID,
                        definitionData: {
                            ...itemDef,
                            HUD_IMAGE_SET_ID: itemDef.IMAGE_SET_ID,
                            ITEM_TYPE_1: itemDef.ITEM_TYPE,
                            flags1: {
                                ...itemDef
                            },
                            flags2: {
                                ...itemDef
                            },
                            stats: []
                        }
                    });
            }
            if (itemDef.ID >= 3941 && itemDef.ID < 3959) {
                // new js base stuff for fun
                defs.push({
                    ID: itemDef.ID,
                    definitionData: {
                        ...itemDef,
                        HUD_IMAGE_SET_ID: itemDef.IMAGE_SET_ID,
                        ITEM_TYPE_1: itemDef.ITEM_TYPE,
                        flags1: {
                            ...itemDef
                        },
                        flags2: {
                            ...itemDef
                        },
                        stats: []
                    }
                });
            }
        });
        const itemDefinitionsCache = this._protocol.pack("Command.ItemDefinitions", {
            data: {
                itemDefinitions: defs
            }
        });
        if (!itemDefinitionsCache)
            return;
        this.itemDefinitionsCache = itemDefinitionsCache;
    }
    packDynamicAppearance() {
        const dynamicAppearanceCache = this._protocol.pack("ReferenceData.DynamicAppearance", {
            ITEM_APPEARANCE_DEFINITIONS: this.dynamicappearance.ITEM_APPEARANCE_DEFINITIONS,
            SHADER_SEMANTIC_DEFINITIONS: this.dynamicappearance.SHADER_SEMANTIC_DEFINITIONS,
            SHADER_PARAMETER_DEFINITIONS: this.dynamicappearance.SHADER_PARAMETER_DEFINITIONS
        });
        if (!dynamicAppearanceCache)
            return;
        this.dynamicAppearanceCache = dynamicAppearanceCache;
    }
    /**
     * Caches weapon definitons so they aren't packed every time a client logs in.
     */
    packWeaponDefinitions() {
        const weaponDefinitionsCache = this._protocol.pack("ReferenceData.WeaponDefinitions", {
            data: {
                definitionsData: {
                    WEAPON_DEFINITIONS: Object.values(weaponDefinitions.WEAPON_DEFINITIONS),
                    FIRE_GROUP_DEFINITIONS: Object.values(weaponDefinitions.FIRE_GROUP_DEFINITIONS),
                    FIRE_MODE_DEFINITIONS: Object.values(weaponDefinitions.FIRE_MODE_DEFINITIONS),
                    PLAYER_STATE_GROUP_DEFINITIONS: Object.values(weaponDefinitions.PLAYER_STATE_GROUP_DEFINITIONS),
                    FIRE_MODE_PROJECTILE_MAPPING_DATA: Object.values(weaponDefinitions.FIRE_MODE_PROJECTILE_MAPPING_DATA),
                    AIM_ASSIST_DEFINITIONS: Object.values(weaponDefinitions.AIM_ASSIST_DEFINITIONS)
                }
            }
        });
        if (!weaponDefinitionsCache)
            return;
        this.weaponDefinitionsCache = weaponDefinitionsCache;
    }
    /**
     * Caches projectile definitons so they aren't packed every time a client logs in.
     */
    packProjectileDefinitions() {
        const projectileDefinitionsCache = this._protocol.pack("ReferenceData.ProjectileDefinitions", {
            definitionsData: projectileDefinitons
        });
        if (!projectileDefinitionsCache)
            return;
        this.projectileDefinitionsCache = projectileDefinitionsCache;
    }
    /**
     * Caches profile definitons so they aren't packed every time a client logs in.
     */
    packProfileDefinitions() {
        const profileDefinitionsCache = this._protocol.pack("ReferenceData.ProfileDefinitions", {
            data: {
                profiles: profileDefinitions
            }
        });
        if (!profileDefinitionsCache)
            return;
        this.profileDefinitionsCache = profileDefinitionsCache;
    }
    /**
     * Caches itemClass definitons so they aren't packed every time a client logs in.
     */
    packitemClassDefinitions() {
        const itemClassDefinitionsCache = this._protocol.pack("ReferenceData.ItemClassDefinitions", {
            ITEMCLASS_DEFINITIONS: Object.values(itemClassDefinitions).map((itemClassDefinition) => {
                return {
                    ID: itemClassDefinition.ID,
                    ITEMCLASS_DATA: {
                        ...itemClassDefinition
                    }
                };
            })
        });
        if (!itemClassDefinitionsCache)
            return;
        this.itemClassDefinitionsCache = itemClassDefinitionsCache;
    }
    async initializeLoginServerConnection() {
        debug("Starting H1emuZoneServer");
        if (!this._loginServerInfo.address) {
            await this.fetchLoginInfo();
        }
        this._loginConnectionManager.setLoginInfo(this._loginServerInfo, {
            serverId: this._worldId,
            h1emuVersion: process.env.H1Z1_SERVER_VERSION || "unknown",
            gameMode: Number(this.gameMode),
            serverRuleSets: this.serverGameRules
        });
        this._loginConnectionManager.start();
        await this._db
            ?.collection(enums_2.DB_COLLECTIONS.SERVERS)
            .findOneAndUpdate({ serverId: this._worldId }, { $set: { populationNumber: 0, populationLevel: 0 } });
    }
    async loadCharacterData(client, savedCharacter) {
        if (!this.hookManager.checkHook("OnLoadCharacterData", client))
            return;
        if (!(await this.hookManager.checkAsyncHook("OnLoadCharacterData", client)))
            return;
        if (savedCharacter.characterName == undefined) {
            console.log(`ERROR: Undefined character name found for client ${client.loginSessionId}`);
            savedCharacter.characterName =
                "CharacterNameError" + savedCharacter.characterId.slice(-5);
        }
        client.guid = savedCharacter.ownerId;
        client.character.name = savedCharacter.characterName;
        client.character.actorModelId = savedCharacter.actorModelId;
        client.character.headActor = savedCharacter.headActor;
        client.character.isRespawning = savedCharacter.isRespawning;
        client.character.gender = savedCharacter.gender;
        client.character.creationDate = savedCharacter.creationDate;
        client.character.lastLoginDate = savedCharacter.lastLoginDate;
        client.character.hairModel = savedCharacter.hairModel || "";
        client.character.spawnGridData = savedCharacter.spawnGridData;
        client.character.mutedCharacters = savedCharacter.mutedCharacters || [];
        client.character.metrics = savedCharacter.metrics || {};
        client.character.groupId =
            (await this.groupManager.getGroupId(this, client)) ?? 0;
        client.character.playTime = savedCharacter.playTime || 0;
        client.character.lastDropPlaytime = savedCharacter.lastDropPlayTime || 0;
        let newCharacter = false;
        if (utils_1._.isEqual(savedCharacter.position, [0, 0, 0, 1])) {
            // if position hasn't changed
            newCharacter = true;
        }
        // https://github.com/QuentinGruber/h1z1-server/issues/2117
        if (savedCharacter.position.length < 4) {
            newCharacter = true;
            setTimeout(() => {
                this.sendAlert(client, "You've been respawned due to lost position data");
            }, 30000);
        }
        if (newCharacter ||
            client.character.isRespawning ||
            !this.enableWorldSaves) {
            client.character.isRespawning = true;
            const pos = this.calculatePosFromSpawnCell(this._spawnGrid[(0, utils_1.randomIntFromInterval)(0, 99)]);
            this.respawnPlayer(client, pos, false);
        }
        else {
            client.character.state.position = new Float32Array(savedCharacter.position);
            client.character.state.rotation = new Float32Array(savedCharacter.rotation);
            (0, worlddatamanager_1.constructLoadout)(savedCharacter._loadout, client.character._loadout);
            (0, worlddatamanager_1.constructContainers)(savedCharacter._containers, client.character._containers);
            client.character._resources =
                savedCharacter._resources || client.character._resources;
            client.character.generateEquipmentFromLoadout(this);
        }
        this.hookManager.checkHook("OnLoadedCharacterData", client);
    }
    async setupServer() {
        // TODO: Disabled for now
        // await this.navManager.loadNav();
        this.weatherManager.init();
        this.playTimeManager.init(this);
        this.initModelsDataSource();
        this.worldDataManager = (await (0, threads_1.spawn)(new threads_1.Worker("./managers/worlddatamanagerthread")));
        await this.worldDataManager.initialize(this._worldId, this._mongoAddress);
        if (!this._soloMode) {
            [this._db, this._mongoClient] = await worlddatamanager_1.WorldDataManager.getDatabase(this._mongoAddress);
        }
        let isANewWorld = true;
        if (this.enableWorldSaves) {
            const loadedWorld = await this.worldDataManager.getServerData(this._worldId);
            if (loadedWorld != null) {
                isANewWorld = false;
                if (loadedWorld.lastItemGuid) {
                    this.lastItemGuid = BigInt(loadedWorld.lastItemGuid);
                }
                if (loadedWorld.worldSaveVersion !== this.worldSaveVersion) {
                    console.log(`World save version mismatch, deleting world data. Current: ${this.worldSaveVersion} Old: ${loadedWorld.worldSaveVersion}`);
                    await this.worldDataManager.deleteWorld();
                    await this.worldDataManager.insertWorld(BigInt(loadedWorld.lastItemGuid));
                }
            }
            else {
                await this.worldDataManager.insertWorld(this.lastItemGuid);
            }
            console.time("fetch world data");
            const fetchedWorldData = (await this.worldDataManager.fetchWorldData());
            worlddatamanager_1.WorldDataManager.loadConstructionParentEntities(fetchedWorldData.constructionParents, this);
            fetchedWorldData.freeplace.forEach((entityData) => {
                worlddatamanager_1.WorldDataManager.loadLootableConstructionEntity(this, entityData, true);
            });
            fetchedWorldData.crops.forEach((entityData) => {
                worlddatamanager_1.WorldDataManager.loadPlantingDiameter(this, entityData);
            });
            fetchedWorldData.traps.forEach((entityData) => {
                worlddatamanager_1.WorldDataManager.loadTraps(this, entityData);
            });
            fetchedWorldData.vehicles.forEach((entityData) => {
                worlddatamanager_1.WorldDataManager.loadVehicles(this, entityData);
            });
            console.timeEnd("fetch world data");
        }
        if (isANewWorld) {
            // Looks like there's no player spawned objects on the map, so we can assume an object can be spawned
            const stash = this.constructionManager.placeStashEntity(this, 1697, 9248, new Float32Array([481.47, 109.86, 2848.61, 1]), new Float32Array([0, 2.49, 0, 0]), new Float32Array([1, 1, 1, 1]), "");
            stash.lootItem(this, this.generateItem(enums_1.Items.WEAPON_AR15, 1, true)); // A gift from Legends
            stash.lootItem(this, this.generateItem(enums_1.Items.CANDLE, 1, true)); // A gift from Jason - We've discussed the Crossbow so many times, but with storage limits its not possible to put here so I'll burn a candle for you, for sure.
            stash.lootItem(this, this.generateItem(enums_1.Items.WATER_PURE, 1, true)); // A gift from TaxMax
            stash.lootItem(this, this.generateItem(enums_1.Items.WEAPON_308, 1, true)); // A gift from Ghost
            stash.lootItem(this, this.generateItem(enums_1.Items.WEAPON_AK47, 1, true)); // A gift from Doggo
            stash.lootItem(this, this.generateItem(enums_1.Items.GROUND_TILLER, 1, true)); // A gift from Meme - He found out that people were using ground tillers to float decks in the air, we discussed this issue in DMs. RIP, friend.
        }
        if (!this._soloMode) {
            await this.initializeLoginServerConnection();
        }
        // !!ANYTHING THAT USES / GENERATES ITEMS MUST BE CALLED AFTER WORLD DATA IS LOADED!!
        //this.packItemDefinitions(); // No longer necessary, we'll see if only sending the required items will impact server performance
        this.packWeaponDefinitions();
        this.packProjectileDefinitions();
        this.packProfileDefinitions();
        this.packitemClassDefinitions();
        this.packDynamicAppearance();
        this.worldObjectManager.createDoors(this);
        this.worldObjectManager.createProps(this);
        await this.pluginManager.initializePlugins(this);
        this.customizeStaticDTOs();
        /*this.tasksManager.register_schedule(
          this.worldRoutine.bind(this),
          this.worldRoutineRate
        );
    
        this.tasksManager.register_schedule(
          this.clientRoutineLoop.bind(this),
          this.clientRoutineRate
        );*/
        this._ready = true;
        console.log(`Server saving ${this.enableWorldSaves ? "enabled" : "disabled"}.`);
        debug("Server ready");
    }
    async saveWorld() {
        if (this._isSaving) {
            this.sendChatTextToAdmins("A save is already in progress.");
            return;
        }
        this.sendChatTextToAdmins("World save started.");
        this._isSaving = true;
        console.time("ZONE: processing");
        try {
            const characters = worlddatamanager_1.WorldDataManager.convertCharactersToSaveData(Object.values(this._characters), this._worldId);
            const vehicles = worlddatamanager_1.WorldDataManager.convertVehiclesToSaveData(Object.values(this._vehicles), this._worldId);
            const worldConstructions = [];
            Object.values(this._worldLootableConstruction).forEach((entity) => {
                if (entity.parentObjectCharacterId == this._serverGuid ||
                    entity instanceof watersource_1.WaterSource ||
                    (entity instanceof trapentity_1.TrapEntity && entity?.worldOwned))
                    return; // Don't save world spawned campfires / barbeques
                const lootableConstructionSaveData = worlddatamanager_1.WorldDataManager.getLootableConstructionSaveData(entity, this._worldId);
                (0, utils_1.removeUntransferableFields)(lootableConstructionSaveData);
                worldConstructions.push(lootableConstructionSaveData);
            });
            const constructions = [];
            Object.values(this._constructionFoundations).forEach((entity) => {
                if (entity.itemDefinitionId != enums_1.Items.FOUNDATION_EXPANSION) {
                    const construction = worlddatamanager_1.WorldDataManager.getConstructionParentSaveData(entity, this._worldId);
                    // isTransferable(construction) too complex will run on max recursive call error
                    constructions.push(construction);
                }
            });
            const crops = [];
            Object.values(this._temporaryObjects).forEach((entity) => {
                if (entity instanceof plantingdiameter_1.PlantingDiameter) {
                    crops.push(worlddatamanager_1.WorldDataManager.getPlantingDiameterSaveData(entity, this._worldId));
                }
            });
            const traps = [];
            Object.values(this._traps).forEach((entity) => {
                if (entity instanceof trapentity_1.TrapEntity && !entity.worldOwned) {
                    traps.push(worlddatamanager_1.WorldDataManager.getTrapSaveData(entity, this._worldId));
                }
            });
            Object.values(this._explosives).forEach((entity) => {
                if (entity instanceof explosiveentity_1.ExplosiveEntity && entity.isLandmine()) {
                    traps.push(worlddatamanager_1.WorldDataManager.getTrapSaveData(entity, this._worldId));
                }
            });
            console.timeEnd("ZONE: processing");
            await this.worldDataManager.saveWorld({
                lastGuidItem: this.lastItemGuid,
                characters,
                worldConstructions,
                crops,
                traps,
                constructions,
                vehicles
            });
            setTimeout(() => {
                this._isSaving = false;
            }, 10000);
            this.sendChatTextToAdmins("World saved!");
            this.nextSaveTime = Date.now() + this.saveTimeInterval;
            debug("World saved!");
        }
        catch (e) {
            console.error(e);
            this.worldSaveFailed = true;
            this.sendAlertToAll("World save failed!");
            this.shutdown(20, "World saving failed, rollback");
        }
    }
    executeRconCommand(ws, payload) {
        payload = payload.replaceAll("/", "");
        const splitMessage = payload.split(" ");
        const commandName = splitMessage[0];
        console.log(`[RCON]Execute command "${commandName}"`);
        const commandHash = (0, utils_1.flhash)(commandName.toUpperCase());
        const args = payload.replace(commandName, "").trim();
        // TODO: add loginSessionId from the user
        const fakeClient = this.createClient(0, "", "", "", 0);
        // Admin rights for the Rcon client but we should add that as an option in the config
        // TODO: permissionLevel should be the same as the user who use the rcon
        fakeClient.permissionLevel = 2;
        this.commandHandler.executeCommand(this, fakeClient, {
            name: "Command.ExecuteCommand",
            data: { commandHash, arguments: args }
        });
    }
    handleRconMessage(ws, message) {
        try {
            this.rconManager.broadcastLog(`Rcon message type ${message.type} payload: ${message.payload} `);
            switch (message.type) {
                case rconmanager_1.RconMessageType.ExecCommand:
                    if (typeof message.payload === "string") {
                        this.executeRconCommand(ws, message.payload);
                        ws.send(this.rconManager.createMessage(rconmanager_1.RconMessageType.DisplayUserMessage, "Command executed successfully!"));
                    }
            }
        }
        catch (e) {
            console.error(`Rcon message failed for ${ws.url}`);
            console.error(e);
        }
    }
    startH1emuAi() {
        (0, node_timers_1.setInterval)(() => {
            try {
                if (process.env.ENABLE_AI_TIME_LOGS) {
                    const start = performance.now();
                    this.aiManager.run();
                    const end = performance.now();
                    const duration = end - start;
                    if (duration >= 1) {
                        console.log(`H1emu-ai took ${duration}ms with ${this.aiManager.getEntitiesTotalNumber()} entities`);
                    }
                    console.log(this.aiManager.getEntitiesStats());
                }
                else {
                    this.aiManager.run();
                }
            }
            catch (e) {
                console.error(e);
            }
        }, 100);
    }
    async start() {
        debug("Starting server");
        debug(`Protocol used : ${this._protocol.protocolName}`);
        if (!this.hookManager.checkHook("OnServerInit"))
            return;
        if (!(await this.hookManager.checkAsyncHook("OnServerInit")))
            return;
        await this.setupServer();
        if (this.isPvE) {
            console.log("Server in PvE mode");
        }
        this.fairPlayManager.decryptFairPlayValues();
        this._spawnGrid = this.divideMapIntoSpawnGrid(7448, 7448, 744);
        if (this.isSurvival()) {
            this.speedtreeManager.initiateList();
        }
        this.startRoutinesLoop();
        if (this.isSurvival()) {
            this.smeltingManager.checkSmeltables(this);
            this.smeltingManager.checkCollectors(this);
            this.decayManager.run(this);
        }
        this._serverStartTime = (0, utils_1.getCurrentServerTimeWrapper)();
        this.weatherManager.startWeatherWorker(this);
        this.inGameTimeManager.start();
        if (this.isSurvival()) {
            this.randomEventsManager.start();
        }
        if (!this._soloMode) {
            this.accountInventoriesManager.init(this._db.collection(enums_2.DB_COLLECTIONS.ACCOUNT_ITEMS));
            this.challengeManager.init(this._db.collection(enums_2.DB_COLLECTIONS.CHALLENGES));
        }
        this._gatewayServer.start();
        this.worldRoutineTimer = setTimeout(() => this.worldRoutine.bind(this)(), this.worldRoutineRate);
        this.initHudIndicatorDataSource();
        this.initScreenEffectDataSource();
        this.initClientEffectsDataSource();
        this.initUseOptionsDataSource();
        this.rconManager.start();
        this.rconManager.on("message", this.handleRconMessage.bind(this));
        if (this.isSurvival()) {
            this.rewardManager.start();
        }
        this.hookManager.checkHook("OnServerReady");
        if (!process.env.DISABLE_AI) {
            this.startH1emuAi();
        }
        this.challengePositionCheckInterval = (0, node_timers_1.setInterval)(() => this.checkPlayersPositionsChallenges(), 30_000);
    }
    async sendInitData(client) {
        // Load character before sending sendSelf, as we need to send item definitions for the inventory to the client first. This is the same behaviour as Z1BR
        await this.fetchCharacterData(client);
        // This packet is only necessary for transitioning between different zones.
        // KOTK / Z1BR handle it this way because the main menu is also considered a zone, which is not the case in JS.
        /*this.sendData<ClientBeginZoning>(client, "ClientBeginZoning", {
          position: client.character.state.position,
          rotation: client.character.state.rotation,
          skyData: this.weatherManager.weather
        });*/
        const { environment, sku } = this.isBattleRoyale()
            ? { environment: "LIVE_KOTK", sku: "SKU_Is_KotK" }
            : { environment: "LIVE", sku: "SKU_Is_JustSurvive" };
        this.sendData(client, "InitializationParameters", {
            ENVIRONMENT: environment, // LOCAL, MAIN, QA, TEST, STAGE, LIVE, //THE_NINE, INNOVA
            unknownString1: sku, //JS.Environment
            rulesetDefinitions: Object.values(gameRulesSource).map((gameRule) => {
                return {
                    RULESET_ID: gameRule.ID,
                    RULESET_ID_: gameRule.ID,
                    ruleset: gameRule.RULESET,
                    rulesets: Object.values(gameRule?.RULESETS || []).map((ruleSet) => {
                        return {
                            ID: ruleSet.ID,
                            DATA: {
                                ...ruleSet
                            }
                        };
                    })
                };
            })
        });
        this.sendData(client, "SendZoneDetails", {
            zoneName: "Z1",
            zoneType: 4,
            unknownBoolean1: false,
            skyData: this.weatherManager.weather,
            zoneId1: 5,
            geometryId: 5,
            nameId: 7699,
            unknownBoolean2: true,
            lighting: "Lighting_JustSurvive.txt",
            isInvitational: false
        });
        this.sendData(client, "ClientGameSettings", {
            interactionCheckRadius: 16, // need it high for tampers
            unknownBoolean1: true,
            timescale: 1.0,
            enableWeapons: 1, // no longer seems to do anything, used to disable weapons from working
            Unknown5: 1,
            unknownFloat1: 0.0,
            fallDamageVelocityThreshold: 15,
            fallDamageVelocityMultiplier: 11
        });
        // Item definitions are only required if custom icons for items or entirely custom items are needed.
        // This could be an interesting feature to implement in a plugin.
        /*const inventoryItemIds = client.character
          .pGetInventoryItems(this)
          .map((it) => it.itemDefinitionId);
        if (inventoryItemIds) {
          this.sendData(client, "Command.ItemDefinitions", {
            data: {
              itemDefinitions: Object.values(this._itemDefinitions)
                .filter(
                  (item) =>
                    inventoryItemIds.includes(item.ID) &&
                    !["Foundation", "ConstructionSocketBound"].includes(
                      item.CODE_FACTORY_NAME
                    )
                ) // Filter out construction items that aren't freeplace, because they will be handled as freeplace objects if you do. My guess is that this packet structure isn't 100% correct yet. - Jason
                .map((itemDef) => {
                  return {
                    ID: itemDef.ID,
                    definitionData: {
                      ...itemDef,
                      HUD_IMAGE_SET_ID: itemDef.IMAGE_SET_ID,
                      ITEM_TYPE_1: itemDef.ITEM_TYPE,
                      flags1: {
                        ...itemDef
                      },
                      flags2: {
                        ...itemDef
                      },
                      stats: []
                    }
                  };
                })
            }
          });
        }*/
        if (!this.weaponDefinitionsCache) {
            this.packWeaponDefinitions();
        }
        if (this.weaponDefinitionsCache) {
            this.sendRawDataReliable(client, this.weaponDefinitionsCache);
        }
        // Initially, send only the skin tones to ensure the character displays the correct skin tone.
        this.sendData(client, "ReferenceData.DynamicAppearance", {
            ITEM_APPEARANCE_DEFINITIONS: [],
            SHADER_SEMANTIC_DEFINITIONS: [],
            SHADER_PARAMETER_DEFINITIONS: this.dynamicappearance.SHADER_PARAMETER_DEFINITIONS.filter((def) => [125, 129, 122].includes(def?.ID))
        });
        const accountItems = await this.accountInventoriesManager.getAccountItems(client.loginSessionId);
        this.sendCharacterData(client, accountItems);
        // Now we can send all the rest of the data while the player is at the loading screen.
        // This ensures the player doesn't have to wait on the loading screen after clicking 'join', as this packet is large.
        // Z1BR resolved this issue by using LZ4 to compress this block. We can easily add this in the patch, but we'll implement it if users start experiencing network issues.
        // FYI: This was tested with 40 players on my server without any issues. - Jason
        if (!this.dynamicAppearanceCache) {
            this.packDynamicAppearance();
        }
        if (this.dynamicAppearanceCache) {
            this.sendRawDataReliable(client, this.dynamicAppearanceCache);
        }
    }
    divideMapIntoSpawnGrid(mapWidth, mapHeight, gridCellSize) {
        const grid = [];
        for (let i = -mapWidth / 2 + gridCellSize / 2; i < mapWidth / 2; i += gridCellSize) {
            for (let j = mapHeight / 2 - gridCellSize / 2; j > -mapHeight / 2; j -= gridCellSize) {
                const cell = new spawncell_1.SpawnCell(i, j, gridCellSize, gridCellSize);
                spawnLocations2.forEach((location) => {
                    if (!location)
                        return;
                    if (location[0] >= cell.position[0] - cell.width / 2 &&
                        location[0] <= cell.position[0] + cell.width / 2 &&
                        location[2] >= cell.position[2] - cell.height / 2 &&
                        location[2] <= cell.position[2] + cell.height / 2) {
                        cell.spawnPoints.push(new Float32Array(location));
                    }
                });
                grid.push(cell);
            }
        }
        return grid.reverse();
    }
    divideMapIntoGrid(mapWidth, mapHeight, gridCellSize) {
        const grid = [];
        for (let i = -mapWidth / 2; i < mapWidth / 2; i += gridCellSize) {
            for (let j = -mapHeight / 2; j < mapHeight / 2; j += gridCellSize) {
                const cell = new gridcell_1.GridCell(i, j, gridCellSize, gridCellSize);
                grid.push(cell);
            }
        }
        return grid;
    }
    divideLargeCells(threshold) {
        const grid = this._grid;
        for (let i = 0; i < grid.length; i++) {
            const gridCell = grid[i];
            if (gridCell.height < 250)
                continue;
            if (gridCell.objects.length > threshold) {
                const newGridCellWidth = gridCell.width / 2;
                const newGridCellHeight = gridCell.height / 2;
                // 4 cells made of 1
                const newGridCell1 = new gridcell_1.GridCell(gridCell.position[0], gridCell.position[2], newGridCellWidth, newGridCellHeight);
                const newGridCell2 = new gridcell_1.GridCell(gridCell.position[0] + newGridCellWidth, gridCell.position[2], newGridCellWidth, newGridCellHeight);
                const newGridCell3 = new gridcell_1.GridCell(gridCell.position[0], gridCell.position[2] + newGridCellHeight, newGridCellWidth, newGridCellHeight);
                const newGridCell4 = new gridcell_1.GridCell(gridCell.position[0] + newGridCellWidth, gridCell.position[2] + newGridCellHeight, newGridCellWidth, newGridCellHeight);
                // remove old grid cell
                const objects = this._grid[i].objects;
                this._grid.splice(i, 1);
                i--;
                this._grid.push(newGridCell1);
                this._grid.push(newGridCell2);
                this._grid.push(newGridCell3);
                this._grid.push(newGridCell4);
                objects.forEach((object) => {
                    this.pushToGridCell(object);
                });
            }
        }
    }
    pushToGridCell(obj) {
        if (this._grid.length == 0)
            this._grid = this.divideMapIntoGrid(8196, 8196, 250);
        if (obj instanceof vehicle_1.Vehicle2016 ||
            obj instanceof character_1.Character2016 ||
            obj instanceof projectileentity_1.ProjectileEntity //||
        //(obj instanceof ConstructionChildEntity && !obj.getParent(this)) ||
        //(obj instanceof LootableConstructionEntity && !obj.getParent(this))
        ) {
            // dont push objects that can change its position
            return;
        }
        for (let i = 0; i < this._grid.length; i++) {
            const gridCell = this._grid[i];
            if (obj.state.position[0] >= gridCell.position[0] &&
                obj.state.position[0] <= gridCell.position[0] + gridCell.width &&
                obj.state.position[2] >= gridCell.position[2] &&
                obj.state.position[2] <= gridCell.position[2] + gridCell.height) {
                if (gridCell.objects.includes(obj)) {
                    return;
                }
                gridCell.objects.push(obj);
            }
        }
    }
    assignChunkRenderDistance(client) {
        let lowerRenderDistance = false;
        const character = client.character;
        for (let i = 0; i < this._grid.length; i++) {
            const gridCell = this._grid[i];
            if (character.state.position[0] >= gridCell.position[0] &&
                character.state.position[0] <= gridCell.position[0] + gridCell.width &&
                character.state.position[2] >= gridCell.position[2] &&
                character.state.position[2] <= gridCell.position[2] + gridCell.height &&
                gridCell.height < 250) {
                lowerRenderDistance = true;
            }
        }
        client.chunkRenderDistance = lowerRenderDistance ? 600 : 700;
    }
    async worldRoutine() {
        if (!this.hookManager.checkHook("OnWorldRoutine"))
            return;
        else {
            if (this._ready) {
                this.constructionManager.plantManager(this);
                await promises_1.scheduler.yield();
                this.worldObjectManager.run(this);
                await promises_1.scheduler.yield();
                this.checkVehiclesInMapBounds();
                await promises_1.scheduler.yield();
                this.updateSyncTeleport();
                await promises_1.scheduler.yield();
                this.setTickRate();
                await promises_1.scheduler.yield();
                this.updateSpectatorMap();
                if (this.enableWorldSaves &&
                    !this.isSaving &&
                    this.nextSaveTime - Date.now() < 0) {
                    this.saveWorld();
                }
            }
        }
        this.worldRoutineTimer.refresh();
    }
    setTickRate() {
        const size = utils_1._.size(this._clients);
        if (size <= 0) {
            this.tickRate = 3000;
            return;
        }
        this.tickRate = 3000 / size;
    }
    async deleteClient(client) {
        this.hookManager.checkHook("OnPlayerDisconnected", client);
        if (!client) {
            this.setTickRate();
            return;
        }
        if (client.afkTimer) {
            (0, node_timers_1.clearInterval)(client.afkTimer);
        }
        if (client.assetIntegrityKickTimer) {
            clearTimeout(client.assetIntegrityKickTimer);
        }
        if (client.character) {
            client.isLoading = true; // stop anything from acting on character
            this.aiManager.removeEntity(client.character);
            // "shift" time played prior to logging out
            client.character.metrics.startedSurvivingTP +=
                Date.now() - Number(client.character.lastLoginDate);
            clearTimeout(client.character?.resourcesUpdater);
            try {
                const characterSave = worlddatamanager_1.WorldDataManager.convertToCharacterSaveData(client.character, this._worldId);
                if (this.enableWorldSaves) {
                    if (this._soloMode) {
                        await this.saveWorld();
                    }
                    else {
                        await this.worldDataManager.saveCharacterData(characterSave, this.lastItemGuid);
                    }
                }
            }
            catch (e) {
                console.error("Failed to save a character");
                console.error(e);
            }
            this.dismountVehicle(client);
            client.managedObjects?.forEach((characterId) => {
                this.dropVehicleManager(client, characterId);
            });
            this.deleteEntity(client.character.characterId, this._characters);
            if (!this._soloMode) {
                this.groupManager.handlePlayerDisconnect(this, client);
            }
        }
        delete this._clients[client.sessionId];
        this._gatewayServer.deleteSoeClient(client.soeClientId);
        if (!this._soloMode) {
            this.sendZonePopulationUpdate();
        }
        this.setTickRate();
        this.airdropManager.sendDeliveryStatus();
    }
    async generateDamageRecord(targetCharacterId, damageInfo, oldHealth) {
        const targetEntity = this.getEntity(targetCharacterId), sourceEntity = this.getEntity(damageInfo.entity), targetClient = this.getClientByCharId(targetCharacterId), sourceClient = this.getClientByCharId(damageInfo.entity);
        let sourceName = damageInfo.entity, sourcePing = 0, targetName = "Generic", targetPing = 0;
        if (sourceClient && !targetClient) {
            sourceName = sourceClient.character.name || "Unknown";
            const sourceSOEClientAvgPing = this._gatewayServer.getSoeClientAvgPing(sourceClient.soeClientId);
            sourcePing = sourceSOEClientAvgPing ?? 0;
        }
        else if (!sourceClient && targetClient) {
            targetName = targetClient.character.name || "Unknown";
            const targetSOEClientAvgPing = this._gatewayServer.getSoeClientAvgPing(targetClient.soeClientId);
            targetPing = targetSOEClientAvgPing ?? 0;
        }
        else if (sourceClient && targetClient) {
            const sourceSOEClientAvgPing = this._gatewayServer.getSoeClientAvgPing(sourceClient.soeClientId);
            const targetSOEClientAvgPing = this._gatewayServer.getSoeClientAvgPing(targetClient.soeClientId);
            sourcePing = sourceSOEClientAvgPing ?? 0;
            sourceName = sourceClient.character.name || "Unknown";
            targetName = targetClient.character.name || "Unknown";
            targetPing = targetSOEClientAvgPing ?? 0;
        }
        return {
            source: {
                name: sourceName,
                ping: sourcePing
            },
            target: {
                name: targetName,
                ping: targetPing
            },
            hitInfo: {
                timestamp: Date.now(),
                weapon: damageInfo.weapon,
                distance: sourceEntity && targetEntity
                    ? (0, utils_1.getDistance)(sourceEntity.state.position, targetEntity.state.position).toFixed(1)
                    : "0",
                hitLocation: damageInfo.hitReport?.hitLocation || "Unknown",
                hitPosition: damageInfo.hitReport?.position || new Float32Array([0, 0, 0, 0]),
                oldHP: oldHealth,
                newHP: oldHealth - damageInfo.damage < 0 ? 0 : oldHealth - damageInfo.damage,
                message: damageInfo.message || ""
            }
        };
    }
    sendDeathMetrics(client) {
        const damageRecord = Object.values(client.character.getCombatLog()).at(-1);
        this.sendData(client, "ClientUpdate.DeathMetrics", {
            recipesDiscovered: client.character.metrics.recipesDiscovered,
            zombiesKilled: client.character.metrics.zombiesKilled,
            minutesSurvived: Math.ceil((Date.now() - client.character.metrics.startedSurvivingTP) / 60000),
            wildlifeKilled: client.character.metrics.wildlifeKilled,
            vehiclesDestroyed: client.character.metrics.vehiclesDestroyed,
            playersKilled: client.character.metrics.playersKilled,
            lastDamageAmount: damageRecord?.hitInfo.oldHP && damageRecord?.hitInfo.newHP
                ? Math.max(0, damageRecord.hitInfo.oldHP - damageRecord.hitInfo.newHP)
                : 0,
            killedByHeadshot: ["HEAD", "GLASSES", "NECK"].includes(damageRecord?.hitInfo.hitLocation ?? "")
        });
    }
    logPlayerDeath(client, damageInfo) {
        debug(client.character.name + " has died");
        let sourceClient = this.getClientByCharId(damageInfo.entity);
        if (!sourceClient) {
            const sourceEntity = this.getEntity(damageInfo.entity);
            if (sourceEntity instanceof projectileentity_1.ProjectileEntity) {
                sourceClient = this.getClientByCharId(sourceEntity.managerCharacterId);
            }
            else {
                return;
            }
        }
        if (!sourceClient)
            return;
        if (!this._soloMode &&
            client.character.name !== sourceClient.character.name) {
            (0, utils_1.logClientActionToMongo)(this._db.collection(enums_2.DB_COLLECTIONS.KILLS), sourceClient, this._worldId, {
                type: enums_2.KILL_TYPE.PLAYER,
                playerKilled: client.character.name,
                playerKilledLoginSessionId: client.loginSessionId
            });
        }
        client.lastDeathReport = {
            position: client.character.state.position,
            attackerPosition: sourceClient.character.state.position,
            distance: Number((0, utils_1.getDistance)(client.character.state.position, sourceClient.character.state.position).toFixed(2)),
            attacker: sourceClient
        };
        //this.sendConsoleTextToAdmins()
    }
    killCharacter(client, damageInfo) {
        if (!client.character.isAlive)
            return;
        if (!this.hookManager.checkHook("OnPlayerDeath", client, damageInfo))
            return;
        const killerClient = this.getClientByCharId(damageInfo.entity);
        if (killerClient) {
            const weapon = damageInfo.weapon;
            if (weapon && weapon === enums_1.Items.WEAPON_GUITAR) {
                this.challengeManager.registerChallengeProgression(killerClient, challengemanager_1.ChallengeType.ROCKSTAR, 1);
            }
            else if (weapon && weapon === enums_1.Items.WEAPON_FISTS) {
                this.challengeManager.registerChallengeProgression(killerClient, challengemanager_1.ChallengeType.ROCKY, 1);
            }
        }
        for (const a in client.character._characterEffects) {
            const characterEffect = client.character._characterEffects[a];
            if (characterEffect.endCallback)
                characterEffect.endCallback(this, client.character);
        }
        const weapon = client.character.getEquippedWeapon();
        if (weapon && weapon.weapon) {
            this.sendRemoteWeaponUpdateDataToAllOthers(client, client.character.transientId, weapon.itemGuid, "Update.FireState", {
                state: {
                    firestate: 0,
                    transientId: client.character.transientId,
                    position: client.character.state.position
                }
            });
        }
        const pos = client.character.state.position;
        if (client.character.spawnGridData.length < 100) {
            // attemt to fix broken spawn grid after unban
            client.character.spawnGridData = Array.from({ length: 100 }).fill(0);
        }
        this._spawnGrid.forEach((spawnCell) => {
            // find current grid and add it to blocked ones
            if (pos[0] >= spawnCell.position[0] - spawnCell.width / 2 &&
                pos[0] <= spawnCell.position[0] + spawnCell.width / 2 &&
                pos[2] >= spawnCell.position[2] - spawnCell.height / 2 &&
                pos[2] <= spawnCell.position[2] + spawnCell.height / 2) {
                client.character.spawnGridData[this._spawnGrid.indexOf(spawnCell)] =
                    new Date().getTime() + 300000;
                // find neighboring grids and add to blocked ones
                this._spawnGrid.forEach((cell) => {
                    if ((0, utils_1.isPosInRadius)(1100, cell.position, spawnCell.position)) {
                        client.character.spawnGridData[this._spawnGrid.indexOf(cell)] =
                            new Date().getTime() + 300000;
                    }
                });
            }
        });
        const character = client.character, gridArr = [];
        character.spawnGridData.forEach((number) => {
            if (number <= new Date().getTime())
                number = 0;
            gridArr.push({
                unk: number ? Math.floor((number - new Date().getTime()) / 1000) : 0
            });
        });
        this.sendData(client, "ClientUpdate.UpdateLockoutTimes", {
            unk: gridArr,
            bool: true
        });
        client.character._characterEffects = {};
        client.character.isRespawning = true;
        this.sendDeathMetrics(client);
        this.logPlayerDeath(client, damageInfo);
        client.character.isRunning = false;
        character.isAlive = false;
        client.character.characterStates.revivable = true;
        client.character.characterStates.beingRevived = true;
        this.updateCharacterState(client, client.character.characterId, client.character.characterStates, false);
        if (!client.isLoading) {
            this.sendData(client, "Character.StartMultiStateDeath", {
                data: {
                    characterId: client.character.characterId,
                    flag: 128,
                    managerCharacterId: client.character.characterId
                }
            });
            for (const a in this._clients) {
                const iteratedClient = this._clients[a];
                if (iteratedClient.spawnedEntities.has(client.character)) {
                    this.sendData(iteratedClient, "Character.StartMultiStateDeath", {
                        data: {
                            characterId: client.character.characterId,
                            flag: 128,
                            managerCharacterId: iteratedClient.character.characterId
                        }
                    });
                    this.sendData(iteratedClient, "Character.ManagedObject", {
                        objectCharacterId: client.character.characterId,
                        characterId: iteratedClient.character.characterId
                    });
                }
            }
        }
        else {
            this.sendDataToAllOthersWithSpawnedEntity(this._characters, client, client.character.characterId, "Character.StartMultiStateDeath", {
                data: { characterId: client.character.characterId }
            });
        }
        this.clearMovementModifiers(client);
        client.character.dismountContainer(this);
        if (client.vehicle.mountedVehicle) {
            const vehicle = this._vehicles[client.vehicle.mountedVehicle];
            if (vehicle) {
                const container = vehicle?.getContainer();
                if (vehicle && container) {
                    container.items = {
                        ...container.items,
                        ...client.character.getDeathItems(this)
                    };
                }
                // Wait untill killCharacter is finished for dismountVehicle (if not, there will be no option for the dead occupant to respawn)
                this.once("killCharacterComplete", (client) => {
                    this.dismountVehicle(client);
                });
            }
        }
        else {
            Object.values(client.character._loadout).forEach((slot) => {
                // need to find a better way later, if out of bulk ammo will be outside of lootbag
                if (slot.weapon) {
                    const ammo = this.generateItem(this.getWeaponAmmoId(slot.itemDefinitionId), slot.weapon.ammoCount);
                    if (ammo &&
                        slot.weapon.ammoCount > 0 &&
                        slot.weapon.itemDefinitionId != enums_1.Items.WEAPON_REMOVER) {
                        client.character.lootContainerItem(this, ammo, ammo.stackCount, true);
                    }
                    slot.weapon.ammoCount = 0;
                }
            });
            this.worldObjectManager.createLootbag(this, character);
        }
        this.clearInventory(client, false);
        this.sendKillFeed(client, damageInfo);
        this.hookManager.checkHook("OnPlayerDied", client, damageInfo);
        this.emit("killCharacterComplete", client); // Throw emit for dismounting character from vehicle
    }
    sendKillFeed(client, damageInfo) {
        if (client.character.characterId === damageInfo.entity)
            return;
        const killerEntity = this.getEntity(damageInfo.entity);
        let killerClient;
        if (killerEntity instanceof projectileentity_1.ProjectileEntity) {
            killerClient = this.getClientByCharId(killerEntity.managerCharacterId);
        }
        else {
            killerClient = this.getClientByCharId(damageInfo.entity);
        }
        let isInGodMode = false;
        if (killerClient) {
            isInGodMode = killerClient.character.isGodMode();
            this.sendDataToAllWithSpawnedEntity(this._characters, client.character.characterId, "Character.KilledBy", {
                killer: killerClient?.character.characterId,
                killed: client.character.characterId,
                isCheater: isInGodMode
            });
        }
    }
    applyCharacterEffect(character, effect, damage, time) {
        character._characterEffects[effect] = {
            id: effect,
            duration: Date.now() + time,
            callback: function (server, character) {
                if (damage) {
                    character.damage(server, {
                        entity: "Character.CharacterEffect",
                        damage: damage
                    });
                }
                server.sendDataToAllWithSpawnedEntity(server._characters, character.characterId, "Command.PlayDialogEffect", {
                    characterId: character.characterId,
                    effectId: effect
                });
            }
        };
        this.sendDataToAllWithSpawnedEntity(this._characters, character.characterId, "Command.PlayDialogEffect", {
            characterId: character.characterId,
            effectId: effect
        });
    }
    sendAnimationToAllWithSpawnedEntity(character, animationId) {
        this.sendDataToAllWithSpawnedEntity(this._characters, character.characterId, "Character.PlayAnimation", {
            characterId: character.characterId,
            animationName: "Action",
            animationType: "ActionType",
            unm4: 0,
            unknownDword1: 0,
            unknownByte1: 0,
            unknownDword2: 0,
            unknownByte1xda: 0,
            unknownDword3: animationId
        });
    }
    async explosionDamage(sourceEntity, client) {
        const position = sourceEntity.state.position;
        if (!sourceEntity)
            return;
        const sourceIsProjectile = sourceEntity instanceof projectileentity_1.ProjectileEntity;
        if (sourceIsProjectile) {
            if (sourceEntity.itemDefinitionId == enums_1.Items.WEAPON_MOLOTOV) {
                for (const characterId in this._characters) {
                    if (this.isPvE) {
                        break;
                    }
                    const character = this._characters[characterId];
                    if ((0, utils_1.getDistance)(character.state.position, sourceEntity.state.position) <= 5 &&
                        !character.characterStates.inWater) {
                        this.applyCharacterEffect(character, enums_1.Effects.PFX_Fire_Person_loop, 700, 10000);
                    }
                }
                return;
            }
            if (sourceEntity.itemDefinitionId == enums_1.Items.GRENADE_FLASH) {
                for (const a in this._clients) {
                    if (this.isPvE) {
                        break;
                    }
                    const c = this._clients[a];
                    const character = c.character;
                    if ((0, utils_1.getDistance)(character.state.position, sourceEntity.state.position) <= 12) {
                        this.addScreenEffect(c, this._screenEffects["FLASH"]);
                        this.sendAnimationToAllWithSpawnedEntity(c.character, 9);
                    }
                }
                return;
            }
        }
        // render distance is max client.chunkRenderDistance, could probably be lowered a lot
        // - meme
        for (const gridCell of this._grid) {
            if (!(0, utils_1.isPosInRadius)(400, gridCell.position, position)) {
                continue;
            }
            for (const object of gridCell.objects) {
                object.OnExplosiveHit(this, sourceEntity, client);
            }
        }
        if (this.isPvE)
            return;
        // these entities do not use the grid system
        for (const characterId in this._characters) {
            const character = this._characters[characterId];
            if (!(0, utils_1.isPosInRadiusWithY)(5, character.state.position, sourceEntity.state.position, 3))
                continue;
            character.OnExplosiveHit(this, sourceEntity);
        }
        for (const vehicleKey in this._vehicles) {
            const vehicle = this._vehicles[vehicleKey];
            if (!(0, utils_1.isPosInRadius)(5, vehicle.state.position, sourceEntity.state.position))
                continue;
            await vehicle.OnExplosiveHit(this, sourceEntity);
        }
    }
    createProjectileNpc(client, data) {
        const fireHint = client.fireHints[data.projectileId];
        if (!fireHint)
            return;
        const weaponItem = fireHint.weaponItem;
        if (!weaponItem)
            return;
        const itemDefId = weaponItem.itemDefinitionId, itemDef = this.getItemDefinition(itemDefId);
        if (!itemDef)
            return;
        const weaponDefId = itemDef.PARAM1;
        switch (weaponDefId) {
            case enums_1.WeaponDefinitionIds.WEAPON_BOW_MAKESHIFT:
            case enums_1.WeaponDefinitionIds.WEAPON_BOW_RECURVE:
            case enums_1.WeaponDefinitionIds.WEAPON_CROSSBOW:
            case enums_1.WeaponDefinitionIds.WEAPON_BOW_WOOD:
                delete client.fireHints[data.projectileId];
                this.worldObjectManager.createLootEntity(this, this.generateItem(enums_1.Items.AMMO_ARROW), data.position, data.rotation);
                break;
        }
    }
    calculatePosFromSpawnCell(cell) {
        const randomSpawnIndex = Math.floor(Math.random() * cell.spawnPoints.length);
        return new Float32Array([
            cell.spawnPoints[randomSpawnIndex][0],
            cell.spawnPoints[randomSpawnIndex][1] + 1,
            cell.spawnPoints[randomSpawnIndex][2],
            1
        ]);
    }
    respawnPlayer(client, position, clearEquipment = true) {
        if (!this.hookManager.checkHook("OnPlayerRespawn", client, position))
            return;
        if (!client.character.isRespawning)
            return;
        if (client.vehicle.mountedVehicle) {
            this.dismountVehicle(client);
        }
        this.dropAllManagedObjects(client);
        client.isLoading = true;
        client.characterReleased = false;
        client.blockedPositionUpdates = 0;
        client.character.lastLoginDate = (0, utils_1.toHex)(Date.now());
        client.character.resetMetrics();
        client.character.isAlive = true;
        client.character.isRunning = false;
        client.character.isRespawning = false;
        client.character.characterStates.knockedOut = false;
        client.character.characterStates.revivable = false;
        client.character.characterStates.beingRevived = false;
        client.isInAir = false;
        this.sendDataToAllWithSpawnedEntity(this._characters, client.character.characterId, "Command.PlayDialogEffect", {
            characterId: client.character.characterId,
            effectId: 0
        });
        if (client.character.initialized) {
            client.managedObjects?.forEach((characterId) => {
                this.dropVehicleManager(client, characterId);
            });
            this.sendData(client, "Character.RespawnReply", {
                characterId: client.character.characterId,
                status: true
            });
            this.updateCharacterState(client, client.character.characterId, client.character.characterStates, true);
            this.sendData(client, "ClientUpdate.UpdateLocation", {
                position: position,
                unknownBoolean1: false
            });
            client.character.awaitingTeleportLocation = position;
            client.oldPos.position = position;
            /*const damageInfo: DamageInfo = {
              entity: "Server.Respawn",
              damage: 99999
            };
            if (this.fairPlayManager.fairPlayValues && !client.isAdmin) {
              for (
                let x = 1;
                x < this.fairPlayManager.fairPlayValues.respawnCheckIterations;
                x++
              ) {
                setTimeout(() => {
                  if (
                    isPosInRadius(
                      this.fairPlayManager.fairPlayValues?.respawnCheckRange || 100,
                      tempPos,
                      client.character.state.position
                    ) ||
                    !isPosInRadius(
                      this.fairPlayManager.fairPlayValues?.respawnCheckRange || 300,
                      tempPos2,
                      client.character.state.position
                    )
                  )
                    this.killCharacter(client, damageInfo);
                }, x * this.fairPlayManager.fairPlayValues.respawnCheckTime);
              }
            }*/
        }
        if (clearEquipment) {
            Object.values(client.character._equipment).forEach((equipmentSlot) => {
                this.clearEquipmentSlot(client.character, equipmentSlot.slotId, false);
            });
            client.character.updateEquipment(this);
        }
        client.character.equipLoadout(this);
        client.character.state.position = position;
        client.character.resetResources(this);
        client.character.updateEquipment(this);
        client.character.updateFootwearAudio(this);
        this.hookManager.checkHook("OnPlayerRespawned", client);
    }
    requestModules(client) {
        if (!client.isLoading && client.characterReleased && client.isSynced) {
            this.sendData(client, "H1emu.RequestModules", {});
        }
    }
    updateResource(client, entityId, value, resourceId, resourceType // most resources have the same id and type
    ) {
        this.sendData(client, "ResourceEvent", {
            eventData: {
                type: 3,
                value: {
                    characterId: entityId,
                    resourceId: resourceId,
                    resourceType: resourceType ? resourceType : resourceId,
                    initialValue: value >= 0 ? value : 0
                }
            }
        });
    }
    updateResourceToAllWithSpawnedEntity(entityId, value, resourceId, resourceType, dictionary) {
        this.sendDataToAllWithSpawnedEntity(dictionary, entityId, "ResourceEvent", {
            eventData: {
                type: 3,
                value: {
                    characterId: entityId,
                    resourceId: resourceId,
                    resourceType: resourceType,
                    initialValue: value >= 0 ? value : 0
                }
            }
        });
    }
    getLootableEntity(entityKey) {
        return (this._lootbags[entityKey] ||
            this._vehicles[entityKey] ||
            this._lootableConstruction[entityKey] ||
            this._worldLootableConstruction[entityKey] ||
            this._lootableProps[entityKey] ||
            undefined);
    }
    getConstructionEntity(entityKey) {
        return (this._constructionFoundations[entityKey] ||
            this._constructionSimple[entityKey] ||
            this._lootableConstruction[entityKey] ||
            this._constructionDoors[entityKey] ||
            this._worldLootableConstruction[entityKey] ||
            undefined);
    }
    getConstructionDictionary(entityKey) {
        switch (true) {
            case !!this._constructionFoundations[entityKey]:
                return this._constructionFoundations;
            case !!this._constructionSimple[entityKey]:
                return this._constructionSimple;
            case !!this._lootableConstruction[entityKey]:
                return this._lootableConstruction;
            case !!this._constructionDoors[entityKey]:
                return this._constructionDoors;
            case !!this._worldLootableConstruction[entityKey]:
                return this._worldLootableConstruction;
            case !!this._worldSimpleConstruction[entityKey]:
                return this._worldSimpleConstruction;
            default:
                return;
        }
    }
    getEntity(entityKey = "") {
        return (this._npcs[entityKey] ||
            this._vehicles[entityKey] ||
            this._characters[entityKey] ||
            this._spawnedItems[entityKey] ||
            this._doors[entityKey] ||
            this._explosives[entityKey] ||
            this._constructionFoundations[entityKey] ||
            this._constructionDoors[entityKey] ||
            this._constructionSimple[entityKey] ||
            this._lootbags[entityKey] ||
            this._lootableConstruction[entityKey] ||
            this._lootableProps[entityKey] ||
            this._worldLootableConstruction[entityKey] ||
            this._worldSimpleConstruction[entityKey] ||
            this._plants[entityKey] ||
            this._taskProps[entityKey] ||
            this._crates[entityKey] ||
            this._destroyables[entityKey] ||
            this._temporaryObjects[entityKey] ||
            this._traps[entityKey] ||
            this._throwableProjectiles[entityKey] ||
            undefined);
    }
    getEntityDictionary(entityKey) {
        switch (true) {
            case !!this._npcs[entityKey]:
                return this._npcs;
            case !!this._vehicles[entityKey]:
                return this._vehicles;
            case !!this._characters[entityKey]:
                return this._characters;
            case !!this._spawnedItems[entityKey]:
                return this._spawnedItems;
            case !!this._doors[entityKey]:
                return this._doors;
            case !!this._explosives[entityKey]:
                return this._explosives;
            case !!this._constructionFoundations[entityKey]:
                return this._constructionFoundations;
            case !!this._constructionDoors[entityKey]:
                return this._constructionDoors;
            case !!this._constructionSimple[entityKey]:
                return this._constructionSimple;
            case !!this._lootbags[entityKey]:
                return this._lootbags;
            case !!this._lootableConstruction[entityKey]:
                return this._lootableConstruction;
            case !!this._lootableProps[entityKey]:
                return this._lootableProps;
            case !!this._worldLootableConstruction[entityKey]:
                return this._worldLootableConstruction;
            case !!this._worldSimpleConstruction[entityKey]:
                return this._worldSimpleConstruction;
            case !!this._plants[entityKey]:
                return this._plants;
            case !!this._taskProps[entityKey]:
                return this._taskProps;
            case !!this._crates[entityKey]:
                return this._crates;
            case !!this._destroyables[entityKey]:
                return this._destroyables;
            case !!this._temporaryObjects[entityKey]:
            default:
                return;
        }
    }
    damageItem(character, item, damage) {
        if (item.itemDefinitionId == enums_1.Items.WEAPON_FISTS)
            return;
        // break armor if it goes below 100 health, this helps in shotgun fights
        // so 1 pump fully breaks an armor if most pellets are hit
        if (this.isArmor(item.itemDefinitionId) &&
            item.currentDurability - damage <= 100) {
            damage = item.currentDurability;
        }
        item.currentDurability -= damage;
        if (item.currentDurability <= 0) {
            this.removeInventoryItem(character, item);
            if (this.isWeapon(item.itemDefinitionId) &&
                item.itemDefinitionId != enums_1.Items.WEAPON_BRANCH) {
                character.lootContainerItem(this, this.generateItem(enums_1.Items.BROKEN_METAL_ITEM));
            }
            return;
        }
        const client = this.getClientByCharId(character.characterId);
        if (client)
            this.updateItem(client, item);
    }
    getDurabilityDamage(itemDefinitionId) {
        if (!itemDefinitionId)
            return 0;
        switch (itemDefinitionId) {
            case enums_1.Items.WEAPON_CROWBAR:
            case enums_1.Items.WEAPON_HATCHET_MAKESHIFT:
            case enums_1.Items.WEAPON_HATCHET:
                return 8;
            default:
                return 6;
        }
    }
    getClientByCharId(characterId) {
        for (const a in this._clients) {
            const c = this._clients[a];
            if (c.character.characterId === characterId) {
                return c;
            }
        }
    }
    getClientByGuid(guid) {
        for (const a in this._clients) {
            const c = this._clients[a];
            if (c.guid === guid) {
                return c;
            }
        }
    }
    getClientByName(name) {
        let similar = "";
        const targetClient = Object.values(this._clients).find((c) => {
            const clientName = c.character.name?.toLowerCase().replaceAll(" ", "_");
            if (!clientName)
                return;
            if (clientName == name.toLowerCase()) {
                return c;
            }
            else if ((0, utils_1.getDifference)(name.toLowerCase(), clientName) <= 3 &&
                (0, utils_1.getDifference)(name.toLowerCase(), clientName) != 0)
                similar = clientName;
        });
        return targetClient ? targetClient : similar ? similar : undefined;
    }
    getClientByLoginSessionId(loginSessionId) {
        const targetClient = Object.values(this._clients).find((c) => {
            if (c.loginSessionId == loginSessionId)
                return c;
        });
        return targetClient;
    }
    getClientByNameOrLoginSession(name) {
        let similar = "";
        const targetClient = Object.values(this._clients).find((c) => {
            const clientName = c.character.name?.toLowerCase().replaceAll(" ", "_");
            if (!clientName)
                return;
            if (clientName == name.toLowerCase() || c.loginSessionId == name) {
                return c;
            }
            else if ((0, utils_1.getDifference)(name.toLowerCase(), clientName) <= 3 &&
                (0, utils_1.getDifference)(name.toLowerCase(), clientName) != 0)
                similar = clientName;
        });
        return targetClient ? targetClient : similar ? similar : undefined;
    }
    async getOfflineClientByName(name) {
        const c = await this._db.collection(enums_2.DB_COLLECTIONS.CHARACTERS).findOne({
            characterName: { $regex: `.*${name}.*`, $options: "i" },
            status: 1,
            serverId: this._worldId
        });
        if (!c) {
            return;
        }
        const clientName = c.characterName?.toLowerCase().replaceAll(" ", "_");
        if (!clientName)
            return;
        if (clientName == name.toLowerCase()) {
            const client = this.createClient(-1, "", clientName, c.characterId, this.getTransientId(c.characterId));
            client.character.name = c.characterName;
            client.character.mutedCharacters = c.mutedCharacters;
            return client;
        }
        else if ((0, utils_1.getDifference)(name.toLowerCase(), clientName) <= 3 &&
            (0, utils_1.getDifference)(name.toLowerCase(), clientName) != 0)
            return c.characterName;
        return;
    }
    async getOfflineClientByCharId(characterId) {
        const c = await this._db.collection(enums_2.DB_COLLECTIONS.CHARACTERS).findOne({
            // status: 1,
            serverId: this._worldId,
            characterId: characterId
        });
        if (!c) {
            return;
        }
        if (!c)
            return;
        const client = this.createClient(-1, "", characterId, c.characterId, this.getTransientId(c.characterId));
        // TODO: make a method from the mapping in worlddatamanger
        client.character.state.position = c.position;
        client.character.state.rotation = c.rotation;
        client.character.isAlive = false;
        client.character.name = c.characterName;
        return client;
    }
    applyHelmetDamageReduction(character, damage, weaponDmgModifier = 1) {
        // prevent helmet damage in godmode / temp godmode
        if (character instanceof character_1.Character2016 && character.isGodMode()) {
            return damage;
        }
        if (!character.hasHelmet(this)) {
            return damage;
        }
        const helmetDmgModifier = 0.25;
        damage *= helmetDmgModifier;
        this.damageItem(character, character._loadout[enums_1.LoadoutSlots.HEAD], damage / weaponDmgModifier);
        return damage;
    }
    applyArmorDamageReduction(character, damage, weaponDmgModifier = 4) {
        // prevent armor damage in godmode / temp godmode
        if (character instanceof character_1.Character2016 && character.isGodMode()) {
            return damage;
        }
        // hasArmor is not used since itemDef is needed later in this function
        const slot = character._loadout[enums_1.LoadoutSlots.ARMOR], itemDef = this.getItemDefinition(slot?.itemDefinitionId);
        if (!slot || !slot.itemDefinitionId || !itemDef) {
            return damage;
        }
        // these should be configurable
        const makeshiftDamageModifier = 0.7, // was 0.9
        kevlarDamageModifier = 0.5; // was 0.8
        // checking for plated or wooden armor, these don't have custom skins
        if (itemDef.DESCRIPTION_ID == 11151 || itemDef.DESCRIPTION_ID == 11153) {
            damage *= makeshiftDamageModifier;
            this.damageItem(character, slot, damage / (weaponDmgModifier / 2));
            return damage;
        }
        // all other kevlar armors
        damage *= kevlarDamageModifier;
        this.damageItem(character, slot, damage / (weaponDmgModifier / 2));
        return damage / weaponDmgModifier;
    }
    sendHitmarker(client, hitLocation = "", hasHelmet, hasArmor, hasHelmetBefore, hasArmorBefore) {
        let isHeadshot = 0;
        switch (hitLocation) {
            case "HEAD":
            case "GLASSES":
            case "NECK":
                isHeadshot = 1;
                break;
        }
        this.sendData(client, "Ui.ConfirmHit", {
            hitType: {
                isAlly: 0,
                isHeadshot: isHeadshot,
                damagedArmor: (isHeadshot && hasHelmetBefore && hasHelmet) ||
                    (!isHeadshot && hasArmorBefore && hasArmor)
                    ? 1
                    : 0,
                crackedArmor: isHeadshot && hasHelmetBefore && !hasHelmet
                    ? 1
                    : !isHeadshot && hasArmorBefore && !hasArmor
                        ? 1
                        : 0
            }
        });
    }
    getWeaponHitEffect(itemDefinitionId) {
        const itemDefinition = this.getItemDefinition(itemDefinitionId);
        if (!itemDefinition)
            return;
        const weaponDefinitionId = itemDefinition.PARAM1;
        switch (weaponDefinitionId) {
            case enums_1.WeaponDefinitionIds.WEAPON_SHOTGUN:
                return 1302;
            case enums_1.WeaponDefinitionIds.WEAPON_308:
                return 5414;
            default:
                return 1165;
        }
    }
    getProjectileDamage(itemDefinitionId, sourcePos, targetPos) {
        const itemDefinition = this.getItemDefinition(itemDefinitionId);
        if (!itemDefinition)
            return 1000;
        const weaponDefinitionId = itemDefinition.PARAM1;
        switch (weaponDefinitionId) {
            case enums_1.WeaponDefinitionIds.WEAPON_AR15:
            case enums_1.WeaponDefinitionIds.WEAPON_1911:
            case enums_1.WeaponDefinitionIds.WEAPON_BLAZE:
                return 2500;
            case enums_1.WeaponDefinitionIds.WEAPON_M9:
                return 1800;
            case enums_1.WeaponDefinitionIds.WEAPON_R380:
                return 1500;
            case enums_1.WeaponDefinitionIds.WEAPON_SHOTGUN:
                return (0, utils_1.calculate_falloff)((0, utils_1.getDistance)(sourcePos, targetPos), 200, 1400, //1667,
                1, 12);
            case enums_1.WeaponDefinitionIds.WEAPON_NAGAFENS_RAGE:
                return (0, utils_1.calculate_falloff)((0, utils_1.getDistance)(sourcePos, targetPos), 200, 2000, //1667,
                3, 20);
            case enums_1.WeaponDefinitionIds.WEAPON_AK47:
            case enums_1.WeaponDefinitionIds.WEAPON_FROSTBITE:
                return 2900;
            case enums_1.WeaponDefinitionIds.WEAPON_308:
                return 6700;
            case enums_1.WeaponDefinitionIds.WEAPON_REAPER:
                return 14000;
            case enums_1.WeaponDefinitionIds.WEAPON_MAGNUM:
                return 3000;
            case enums_1.WeaponDefinitionIds.WEAPON_BOW_MAKESHIFT:
                return 2500;
            case enums_1.WeaponDefinitionIds.WEAPON_BOW_RECURVE:
                return 2500;
            case enums_1.WeaponDefinitionIds.WEAPON_BOW_WOOD:
                return 2500;
            case enums_1.WeaponDefinitionIds.WEAPON_CROSSBOW:
                return 2500;
            default:
                return 1000;
        }
    }
    validateHit(client, entity) {
        const ret = {
            isValid: true,
            message: ""
        };
        if (!(0, utils_1.isPosInRadius)(entity.npcRenderDistance || this.charactersRenderDistance, client.character.state.position, entity.state.position)) {
            return {
                isValid: false,
                message: "ProjectileDistance"
            };
        }
        if (!client.spawnedEntities.has(entity)) {
            return {
                isValid: false,
                message: "InvalidTarget"
            };
        }
        const target = this.getClientByCharId(entity.characterId);
        if (target) {
            if (!target.spawnedEntities.has(client.character)) {
                return {
                    isValid: false,
                    message: "InvalidTarget"
                };
            }
        }
        /*if (client.isFairPlayFlagged) {
          this.sendChatTextToAdmins(
            `FairPlay: blocked projectile of flagged client: ${client.character.name}`,
            false
          );
          return {
            isValid: false,
            message: "InvalidFlag"
          };
        }*/
        return ret;
    }
    registerHit(client, packet, gameTime) {
        //if (!client.character.isAlive) return;
        // Should be able to trade while in combat
        if (client.isWeaponLock) {
            this.sendChatText(client, "FairPlay: Your projectile was blocked due to low network quality");
            this.sendChatTextToAdmins(`FairPlay: ${client.character.name} shot has been blocked due to low network quality`, false);
            return;
        }
        const { hitReport } = packet;
        if (!hitReport)
            return; // should never trigger
        for (const a in this._decoys) {
            const decoy = this._decoys[a];
            if (decoy.characterId === hitReport.characterId) {
                this.sendChatTextToAdmins(`FairPlay: ${client.character.name} hit a decoy entity at: [${decoy.position[0].toFixed(2)} ${decoy.position[1].toFixed(2)} ${decoy.position[2].toFixed(2)}]`, false);
            }
        }
        const message = `FairPlay: blocked incoming projectile from ${client.character.name}`;
        const fireHint = client.fireHints[hitReport.sessionProjectileCount];
        if (!fireHint)
            return;
        const weaponItem = fireHint.weaponItem;
        if (!weaponItem)
            return;
        const entity = this.getEntity(hitReport.characterId);
        if (weaponItem.itemDefinitionId == enums_1.Items.WEAPON_BOW_RECURVE) {
            for (const a in this._throwableProjectiles) {
                const projectile = this._throwableProjectiles[a];
                if (projectile.projectileUniqueId == fireHint.projectileUniqueId) {
                    projectile.applyPostion(packet.hitReport.position);
                    projectile.onTrigger(this);
                }
            }
            return;
        }
        if (!entity)
            return;
        // Don't allow hits registering over 350 as this is the render distance for NPC's
        if ((0, utils_1.getDistance2d)(entity.state.position, client.character.state.position) >
            350)
            return;
        const targetClient = this.getClientByCharId(entity.characterId);
        if (!fireHint) {
            if (targetClient) {
                this.sendChatText(targetClient, message, false);
                this.sendChatTextToAdmins(`FairPlay: ${client.character.name} has hit ${targetClient.character.name} with non existing projectile`, false);
            }
            return;
        }
        if (fireHint.hitNumber > 0) {
            if (targetClient) {
                this.sendChatTextToAdmins(`FairPlay: ${client.character.name} shot has been blocked due to desync / double usage`, false);
                this.sendChatText(targetClient, message, false);
            }
            return;
        }
        if (this.fairPlayManager.fairPlayValues &&
            !this.fairPlayManager.validateProjectileHit(this, client, entity, fireHint, weaponItem, hitReport, gameTime)) {
            const itemDef = this.getItemDefinition(weaponItem.itemDefinitionId);
            if (!itemDef)
                return;
            const weaponDefinitionId = itemDef.PARAM1;
            switch (weaponDefinitionId) {
                case enums_1.WeaponDefinitionIds.WEAPON_SHOTGUN:
                case enums_1.WeaponDefinitionIds.WEAPON_NAGAFENS_RAGE:
                    break;
                default:
                    client.flaggedShots++;
                    if (client.flaggedShots >=
                        this.fairPlayManager.fairPlayValues.maxFlaggedShots) {
                        client.isFairPlayFlagged = true;
                    }
            }
            return;
        }
        else {
            client.flaggedShots = 0;
        }
        const hitValidation = this.validateHit(client, entity);
        entity.OnProjectileHit(this, {
            entity: client.character.characterId,
            weapon: weaponItem.itemDefinitionId,
            damage: hitValidation.isValid
                ? this.getProjectileDamage(weaponItem.itemDefinitionId, client.character.state.position, entity.state.position)
                : 0,
            hitReport: packet.hitReport,
            message: hitValidation.message
        });
    }
    setGodMode(client, godMode) {
        client.character.godMode = godMode;
        this.updateCharacterState(client, client.character.characterId, client.character.characterStates, false);
    }
    setTempGodMode(client, godMode) {
        client.character.tempGodMode = godMode;
        this.updateCharacterState(client, client.character.characterId, client.character.characterStates, false);
    }
    /*toggleHiddenMode(client: Client) {
      client.character.isHidden = !client.character.isHidden;
      client.character.characterStates.gmHidden = client.character.isHidden;
      this.updateCharacterState(
        client,
        client.character.characterId,
        client.character.characterStates,
        false
      );
    }*/
    tempGodMode(client, durationMs) {
        this.setTempGodMode(client, true);
        setTimeout(() => {
            this.setTempGodMode(client, false);
        }, durationMs);
    }
    updateCharacterState(client, characterId, object, sendToAll) {
        const updateCharacterStateBody = {
            characterId: characterId,
            states1: object,
            states2: object,
            states3: object,
            states4: object,
            states5: object,
            states6: object,
            states7: object
        };
        if (!sendToAll) {
            this.sendData(client, "Character.UpdateCharacterState", updateCharacterStateBody);
        }
        else {
            this.sendDataToAllOthersWithSpawnedEntity(this._characters, client, client.character.characterId, "Character.UpdateCharacterState", updateCharacterStateBody);
        }
    }
    customizeStaticDTOs() {
        // caches DTOs that should always be removed
        for (const object in this._lootableProps) {
            const prop = this._lootableProps[object];
            const propInstance = {
                objectId: prop.spawnerId,
                replacementModel: "Weapon_Empty.adr"
            };
            this.staticDTOs.push(propInstance);
        }
        for (const object in this._taskProps) {
            const prop = this._taskProps[object];
            const propInstance = {
                objectId: prop.spawnerId,
                replacementModel: "Weapon_Empty.adr"
            };
            this.staticDTOs.push(propInstance);
        }
        for (const object in this._crates) {
            const prop = this._crates[object];
            const propInstance = {
                objectId: prop.spawnerId,
                replacementModel: "Weapon_Empty.adr"
            };
            this.staticDTOs.push(propInstance);
        }
        for (let x = 0; x < this._destroyableDTOlist.length; x++) {
            const propInstance = {
                objectId: this._destroyableDTOlist[x],
                replacementModel: "Weapon_Empty.adr"
            };
            this.staticDTOs.push(propInstance);
        }
        deprecatedDoors.forEach((door) => {
            const DTOinstance = {
                objectId: door,
                replacementModel: "Weapon_Empty.adr"
            };
            this.staticDTOs.push(DTOinstance);
        });
        const cache = this._protocol.pack("DtoObjectInitialData", {
            unknownDword1: 1,
            unknownArray1: this.staticDTOs,
            unknownArray2: [{}]
        });
        if (cache) {
            this.initialDataStaticDtoCache = cache;
        }
    }
    customizeDTO(client) {
        const speedtreeDTOs = [];
        this.speedtreeManager.customize(speedtreeDTOs);
        this.sendData(client, "DtoObjectInitialData", {
            unknownDword1: 1,
            unknownArray1: speedtreeDTOs,
            unknownArray2: [{}]
        });
        if (this.initialDataStaticDtoCache) {
            this.sendRawDataReliable(client, this.initialDataStaticDtoCache);
        }
    }
    shouldRemoveEntity(client, entity) {
        return (entity && // in case if entity is undefined somehow
            //!(entity instanceof ConstructionParentEntity) &&
            !(entity instanceof vehicle_1.Vehicle2016) &&
            (this.filterOutOfDistance(entity, client.character.state.position) ||
                this.constructionManager.shouldHideEntity(this, client, entity)));
    }
    removeOutOfDistanceEntities(client) {
        // does not include vehicles
        for (const entity of client.spawnedEntities) {
            if (this.shouldRemoveEntity(client, entity)) {
                this.sendData(client, "Character.RemovePlayer", {
                    characterId: entity.characterId
                });
                client.spawnedEntities.delete(entity);
            }
        }
    }
    despawnEntity(characterId) {
        this.sendDataToAll("Character.RemovePlayer", {
            characterId: characterId
        });
    }
    deleteEntity(characterId, dictionary, effectId, timeToDisappear) {
        if (!dictionary[characterId])
            return false;
        this.sendDataToAllWithSpawnedEntity(dictionary, characterId, "Character.RemovePlayer", {
            characterId: characterId,
            unknownWord1: effectId ? 1 : 0,
            effectId: effectId ? effectId : 0,
            timeToDisappear: timeToDisappear ? timeToDisappear : 0,
            effectDelay: timeToDisappear ? timeToDisappear : 0
        });
        this._grid.forEach((cell) => {
            if (cell.objects.includes(dictionary[characterId])) {
                cell.objects.splice(cell.objects.indexOf(dictionary[characterId]), 1);
            }
        });
        // remove deleted entity from spawned entities (correct me if we do it somewhere else)
        for (const a in this._clients) {
            const client = this._clients[a];
            if (client.spawnedEntities.delete(dictionary[characterId])) {
                this.sendData(client, "ClientUpdate.ProximateItems", this.getProximityItems(client));
            }
            this.aiManager.removeEntity(dictionary[characterId]);
        }
        delete dictionary[characterId];
        delete this._transientIds[this._characterIds[characterId]];
        delete this._characterIds[characterId];
        return true;
    }
    sendManagedObjectResponseControlPacket(client, obj) {
        this.sendData(client, "ClientUpdate.ManagedObjectResponseControl", obj);
    }
    addLightweightNpc(client, entity, nameId = 0) {
        this.sendData(client, "AddLightweightNpc", {
            ...entity.pGetLightweight(),
            nameId
        });
        this.sendReplicationData(client, entity);
    }
    addSimpleNpc(client, entity) {
        this.sendData(client, "AddSimpleNpc", entity.pGetSimpleNpc());
        this.sendReplicationData(client, entity);
    }
    spawnSimpleNpcForAllInRange(entity) {
        this.executeFuncForAllReadyClientsInRange((client) => {
            if (!client.spawnedEntities.has(entity)) {
                this.addSimpleNpc(client, entity);
                client.spawnedEntities.add(entity);
            }
        }, entity);
    }
    spawnContainerAccessNpc(client) {
        /*
          Since all containers are now handled as SimpleNpcs instead of FullNpcs for
          performance reasons, this npc is needed to act as a conduit between the
          character and the simpleNpc's inventory so that the client thinks the target
          container is a FullNpc.
        */
        this.sendData(client, "AddLightweightNpc", {
            characterId: constants_1.EXTERNAL_CONTAINER_GUID,
            transientId: 0,
            actorModelId: 2,
            position: new Float32Array([0, 0, 0, 0]),
            rotation: new Float32Array([0, 0, 0, 0]),
            scale: new Float32Array([0.001, 0.001, 0.001, 0.001]),
            positionUpdateType: 0,
            profileId: 0,
            isLightweight: false,
            flags: {
                flags1: {},
                flags2: {},
                flags3: {}
            },
            headActor: "",
            attachedObject: {}
        });
    }
    spawnStaticBuildings(client) {
        this.staticBuildings.forEach((v) => {
            this.sendData(client, "AddSimpleNpc", v);
        });
    }
    spawnCharacters(client) {
        for (const c in this._clients) {
            const characterObj = this._clients[c].character;
            if (client.character.characterId != characterObj.characterId &&
                characterObj.isReady &&
                (0, utils_1.isPosInRadius)(characterObj.npcRenderDistance || this.charactersRenderDistance, client.character.state.position, characterObj.state.position) &&
                !client.spawnedEntities.has(characterObj) &&
                characterObj.isAlive &&
                !characterObj.isSpectator &&
                !characterObj.isVanished &&
                (characterObj.isHidden == client.character.isHidden ||
                    client.character.isSpectator) /* &&
          client.banType != "hiddenplayers"*/) {
                this.sendData(client, "AddLightweightPc", characterObj.pGetLightweightPC(this, this._clients[c]));
                client.spawnedEntities.add(this._characters[characterObj.characterId]);
            }
        }
    }
    spawnCharacterToOtherClients(character) {
        const client = this.getClientByCharId(character.characterId);
        if (!client)
            return;
        for (const a in this._clients) {
            const c = this._clients[a];
            if ((0, utils_1.isPosInRadius)(character.npcRenderDistance || this.charactersRenderDistance, character.state.position, c.character.state.position) &&
                !c.spawnedEntities.has(character) &&
                character != c.character) {
                this.sendData(c, "AddLightweightPc", character.pGetLightweightPC(this, client));
                c.spawnedEntities.add(character);
            }
        }
    }
    checkVehiclesInMapBounds() {
        if (this.isBattleRoyale())
            return;
        for (const a in this._vehicles) {
            const vehicle = this._vehicles[a];
            let inMapBounds = false;
            this._spawnGrid.forEach((cell) => {
                const position = vehicle.state.position;
                if (position[0] >= cell.position[0] - cell.width / 2 &&
                    position[0] <= cell.position[0] + cell.width / 2 &&
                    position[2] >= cell.position[2] - cell.height / 2 &&
                    position[2] <= cell.position[2] + cell.height / 2) {
                    inMapBounds = true;
                }
            });
            if (!inMapBounds || vehicle.state.position[1] < -50) {
                vehicle.destroy(this, true);
            }
        }
    }
    spawnGridObjects(client) {
        const position = client.character.state.position;
        for (const gridCell of this._grid) {
            if (!(0, utils_1.isPosInRadius)(client.chunkRenderDistance, gridCell.position, position))
                continue;
            for (const object of gridCell.objects) {
                if (client.spawnedEntities.has(object) ||
                    !(0, utils_1.isPosInRadius)(object.npcRenderDistance ||
                        this.charactersRenderDistance, position, object.state.position)) {
                    continue;
                }
                if (object instanceof constructionparententity_1.ConstructionParentEntity) {
                    this.constructionManager.spawnConstructionParent(this, client, object);
                    continue;
                }
                if (object instanceof constructionchildentity_1.ConstructionChildEntity) {
                    if (this.constructionManager.shouldHideEntity(this, client, object))
                        continue;
                    this.constructionManager.spawnSimpleConstruction(this, client, object);
                    continue;
                }
                if (object instanceof lootableconstructionentity_1.LootableConstructionEntity) {
                    if (this.constructionManager.shouldHideEntity(this, client, object))
                        continue;
                    this.constructionManager.spawnLootableConstruction(this, client, object);
                    continue;
                }
                if (object instanceof basesimplenpc_1.BaseSimpleNpc) {
                    if (object instanceof crate_1.Crate) {
                        if (object.spawnTimestamp > Date.now()) {
                            continue;
                        }
                        if (object.destroyed) {
                            object.destroyed = false;
                        }
                    }
                    client.spawnedEntities.add(object);
                    this.addSimpleNpc(client, object);
                    continue;
                }
                client.spawnedEntities.add(object);
                if (object instanceof baselightweightcharacter_1.BaseLightweightCharacter) {
                    if (object.useSimpleStruct) {
                        this.addSimpleNpc(client, object);
                    }
                    else {
                        this.addLightweightNpc(client, object);
                        if (object instanceof doorentity_1.DoorEntity) {
                            if (object.isOpen) {
                                this.sendData(client, "PlayerUpdatePosition", {
                                    transientId: object.transientId,
                                    positionUpdate: {
                                        sequenceTime: 0,
                                        unknown3_int8: 0,
                                        position: object.state.position,
                                        orientation: object.openAngle
                                    }
                                });
                            }
                            continue;
                        }
                        if (object instanceof npc_1.Npc) {
                            object.updateEquipment(this);
                            continue;
                        }
                    }
                }
            }
        }
    }
    spawnLoadingGridObjects(client) {
        const position = client.character.state.position;
        for (const gridCell of this._grid) {
            if (!(0, utils_1.isPosInRadius)(client.chunkRenderDistance, gridCell.position, position)) {
                continue;
            }
            for (const object of gridCell.objects) {
                if (!(0, utils_1.isPosInRadius)(object.npcRenderDistance ||
                    this.charactersRenderDistance, position, object.state.position)) {
                    continue;
                }
                if (client.spawnedEntities.has(object))
                    continue;
                if (object instanceof basesimplenpc_1.BaseSimpleNpc) {
                    if (object instanceof crate_1.Crate) {
                        if (object.spawnTimestamp > Date.now()) {
                            continue;
                        }
                        if (object.destroyed) {
                            object.destroyed = false;
                        }
                    }
                    client.spawnedEntities.add(object);
                    this.addSimpleNpc(client, object);
                    continue;
                }
                if (object instanceof baselightweightcharacter_1.BaseLightweightCharacter &&
                    object.useSimpleStruct) {
                    client.spawnedEntities.add(object);
                    this.addSimpleNpc(client, object);
                }
            }
        }
    }
    checkPlayersPositionsChallenges() {
        for (const key in this._characters) {
            const character = this._characters[key];
            if (character.currentChallenge === challengemanager_1.ChallengeType.PV_PD_SURVIVAL) {
                const client = this.getClientByCharId(character.characterId);
                if (client) {
                    const pv_pd_pos = new Float32Array([-233.5, 25.44, -1153.43]);
                    if ((0, utils_1.isPosInRadius)(25, character.state.position, pv_pd_pos)) {
                        this.challengeManager.registerChallengeProgression(client, challengemanager_1.ChallengeType.PV_PD_SURVIVAL, 1);
                    }
                }
            }
        }
    }
    POIManager(client) {
        // sends POIChangeMessage or clears it based on player location
        let inPOI = false;
        Z1_POIs.forEach((point) => {
            if ((0, utils_1.isPosInRadius)(point.range, client.character.state.position, point.position)) {
                inPOI = true;
                if (client.currentPOI != point.stringId) {
                    // checks if player already was sent POIChangeMessage
                    this.sendData(client, "POIChangeMessage", {
                        messageStringId: point.stringId,
                        id: point.POIid
                    });
                    client.currentPOI = point.stringId;
                    if (point.POIid === 13) {
                        this.challengeManager.registerChallengeProgression(client, challengemanager_1.ChallengeType.RANCHITO, 1);
                    }
                }
            }
        });
        if (!inPOI && client.currentPOI != 0) {
            // checks if POIChangeMessage was already cleared
            this.sendData(client, "POIChangeMessage", {
                messageStringId: 0,
                id: 115
            });
            client.currentPOI = 0;
        }
    }
    _sendData(client, packetName, obj, channel) {
        switch (packetName) {
            case "H1emu.RequestModules":
            case "Command.ExecuteCommand":
            case "KeepAlive":
            case "PlayerUpdatePosition":
            case "GameTimeSync":
            case "Synchronization":
            case "Vehicle.StateData":
                break;
            default:
                debug("send data", packetName);
        }
        const data = this._protocol.pack(packetName, obj);
        if (data) {
            this._gatewayServer.sendTunnelData(client.soeClientId, data, channel);
        }
        /*this.tasksManager.register(() => {
          switch (packetName) {
            case "H1emu.RequestModules":
            case "Command.ExecuteCommand":
            case "KeepAlive":
            case "PlayerUpdatePosition":
            case "GameTimeSync":
            case "Synchronization":
            case "Vehicle.StateData":
              break;
            default:
              debug("send data", packetName);
          }
          const data = this._protocol.pack(packetName, obj);
          if (data) {
            this._gatewayServer.sendTunnelData(client.soeClientId, data, channel);
          }
        });*/
    }
    sendUnbufferedData(client, packetName, obj) {
        this._sendData(client, packetName, obj, soeoutputstream_1.SOEOutputChannels.Reliable);
    }
    sendOrderedData(client, packetName, obj) {
        this._sendData(client, packetName, obj, soeoutputstream_1.SOEOutputChannels.Ordered);
    }
    sendData(client, packetName, obj) {
        this._sendData(client, packetName, obj, soeoutputstream_1.SOEOutputChannels.Reliable);
    }
    sendReplicationData(client, entity) {
        if (entity instanceof crate_1.Crate ||
            entity instanceof destroyable_1.Destroyable ||
            entity instanceof character_1.Character2016 ||
            (entity instanceof constructionchildentity_1.ConstructionChildEntity &&
                entity.itemDefinitionId == enums_1.Items.WORKBENCH))
            return;
        let nameId = 0;
        if (entity instanceof baselightweightcharacter_1.BaseLightweightCharacter) {
            nameId = entity.nameId;
        }
        this.sendData(client, "Replication.CreateRepData", {
            transientId: entity.transientId,
            sequenceNumber: client.sentInteractionCounter++,
            propertyHash: enums_1.ReplicationPropertyHash.ISWORLDITEM
        });
        this.sendData(client, "Replication.CreateComponent", {
            transientId: entity.transientId,
            sequenceNumber: client.sentInteractionCounter++,
            componentName: "ClientNpcComponent",
            propertyHash: enums_1.ReplicationPropertyHash.ISWORLDITEM,
            payload: {
                bufferData: {
                    nameId: nameId,
                    componentName: "ClientNpcComponent"
                }
            }
        });
        this.sendData(client, "Replication.CreateComponent", {
            transientId: entity.transientId,
            componentName: "ClientInteractComponent",
            sequenceNumber: client.sentInteractionCounter++,
            propertyHash: enums_1.ReplicationPropertyHash.UNKNOWN1,
            payload: {
                bufferData: {
                    componentName: "ClientInteractComponent",
                    distance: entity instanceof constructionparententity_1.ConstructionParentEntity ? 15 : 3
                }
            }
        });
    }
    /*addLightWeightNpcQueue(
      client: Client,
      packetName: h1z1PacketsType2016,
      obj: zone2016packets
    ) {
      client.lightWeightNpcQueue.push({ packetName: packetName, data: obj });
    }*/
    sendWeaponData(client, packetName, obj) {
        this.sendData(client, "Weapon.Weapon", {
            weaponPacket: {
                packetName: packetName,
                gameTime: (0, utils_1.getCurrentServerTimeWrapper)().getTruncatedU32(),
                packet: obj
            }
        });
    }
    sendRemoteWeaponData(client, transientId, packetName, obj) {
        this.sendData(client, "Weapon.Weapon", {
            weaponPacket: {
                packetName: "Weapon.RemoteWeapon",
                gameTime: (0, utils_1.getCurrentServerTimeWrapper)().getTruncatedU32(),
                remoteWeaponPacket: {
                    packetName: packetName,
                    transientId: transientId,
                    packet: obj
                }
            }
        });
    }
    sendRemoteWeaponDataToAllOthers(client, transientId, packetName, obj) {
        this.sendDataToAllOthersWithSpawnedEntity(this._characters, client, client.character.characterId, "Weapon.Weapon", {
            weaponPacket: {
                packetName: "Weapon.RemoteWeapon",
                gameTime: (0, utils_1.getCurrentServerTimeWrapper)().getTruncatedU32(),
                remoteWeaponPacket: {
                    packetName: packetName,
                    transientId: transientId,
                    packet: obj
                }
            }
        });
    }
    sendRemoteWeaponUpdateData(client, transientId, weaponGuid, packetName, obj) {
        this.sendData(client, "Weapon.Weapon", {
            weaponPacket: {
                packetName: "Weapon.RemoteWeapon",
                gameTime: (0, utils_1.getCurrentServerTimeWrapper)().getTruncatedU32(),
                remoteWeaponPacket: {
                    packetName: "RemoteWeapon.Update",
                    transientId: transientId,
                    remoteWeaponUpdatePacket: {
                        packetName: packetName,
                        weaponGuid: weaponGuid,
                        packet: obj
                    }
                }
            }
        });
    }
    sendRemoteWeaponUpdateDataToAllOthers(client, transientId, weaponGuid, packetName, obj) {
        this.sendDataToAllOthersWithSpawnedEntity(this._characters, client, client.character.characterId, "Weapon.Weapon", {
            weaponPacket: {
                packetName: "Weapon.RemoteWeapon",
                gameTime: (0, utils_1.getCurrentServerTimeWrapper)().getTruncatedU32(),
                remoteWeaponPacket: {
                    packetName: "RemoteWeapon.Update",
                    transientId: transientId,
                    remoteWeaponUpdatePacket: {
                        packetName: packetName,
                        weaponGuid: weaponGuid,
                        packet: obj
                    }
                }
            }
        });
    }
    sendAlert(client, message) {
        this._sendData(client, "ClientUpdate.TextAlert", {
            message: message
        }, soeoutputstream_1.SOEOutputChannels.Reliable);
    }
    sendAlertToAll(message, playerName = "") {
        this._sendDataToAll("Broadcast.Zone", {
            identity: {
                characterFirstName: playerName
            },
            message: message
        });
    }
    createClient(sessionId, soeClientId, loginSessionId, characterId, generatedTransient) {
        const client = new zoneclient_1.ZoneClient2016(sessionId, soeClientId, loginSessionId, characterId, generatedTransient, this);
        return client;
    }
    isBanExpired(ban) {
        if (!ban || !ban.expirationDate)
            return false;
        if (ban.expirationDate < Date.now())
            return true;
        return false;
    }
    async isClientBanned(loginSessionId, address, client) {
        const addressBan = (await this._db
            ?.collection(enums_2.DB_COLLECTIONS.BANNED)
            .findOne({
            IP: address,
            active: true
        }));
        const idBan = (await this._db
            ?.collection(enums_2.DB_COLLECTIONS.BANNED)
            .findOne({
            loginSessionId,
            active: true
        }));
        if (addressBan || idBan) {
            if (this.isBanExpired(addressBan)) {
                await this._db?.collection(enums_2.DB_COLLECTIONS.BANNED).updateOne({ IP: address, active: true }, {
                    $set: {
                        active: false // Set active to false to indicate that it has expired
                    }
                });
                return false;
            }
            if (this.isBanExpired(idBan)) {
                await this._db?.collection(enums_2.DB_COLLECTIONS.BANNED).updateOne({ loginSessionId, active: true }, {
                    $set: {
                        active: false // Set active to false to indicate that it has expired
                    }
                });
                return false;
            }
            if (client) {
                client.banType = addressBan ? addressBan.banType : idBan?.banType;
            }
            return addressBan || idBan;
        }
        return false;
    }
    async unbanClient(client, name) {
        const unBannedClient = (await this._db
            ?.collection(enums_2.DB_COLLECTIONS.BANNED)
            .findOneAndUpdate({ name, active: true }, { $set: { active: false, unBanAdminName: client.character.name } }));
        return unBannedClient;
    }
    async unbanClientId(client, loginSessionId) {
        const unBannedClient = (await this._db
            ?.collection(enums_2.DB_COLLECTIONS.BANNED)
            .findOneAndUpdate({ loginSessionId, active: true }, { $set: { active: false, unBanAdminName: client.character.name } }))?.value;
        return unBannedClient;
    }
    async banClient(loginSessionId, characterName, reason, adminId, timestamp, isSilent) {
        let client = this.getClientByLoginSessionId(loginSessionId);
        if (!client)
            client = this.getClientByName(characterName);
        if (typeof client == "string")
            client = undefined;
        const object = {
            name: characterName,
            banType: "normal",
            banReason: reason ? reason : "no reason",
            loginSessionId: loginSessionId,
            IP: this._gatewayServer.getSoeClientNetworkInfos(client?.soeClientId ?? "")
                ?.address ?? "",
            adminId: adminId ? adminId : "",
            expirationDate: timestamp,
            active: true,
            unBanAdminName: ""
        };
        this._db?.collection(enums_2.DB_COLLECTIONS.BANNED).insertOne(object);
        if (timestamp && !isSilent) {
            if (client) {
                this.sendAlert(client, reason
                    ? `YOU HAVE BEEN BANNED FROM THE SERVER UNTIL ${(0, utils_1.getDateString)(timestamp)}. REASON: ${reason}`
                    : `YOU HAVE BEEN BANNED FROM THE SERVER UNTIL: ${(0, utils_1.getDateString)(timestamp)}`);
            }
            this.sendAlertToAll(reason
                ? `${characterName} HAS BEEN BANNED FROM THE SERVER UNTIL ${(0, utils_1.getDateString)(timestamp)}. REASON: ${reason}`
                : `${characterName} HAS BEEN BANNED FROM THE SERVER UNTIL: ${(0, utils_1.getDateString)(timestamp)}`);
        }
        else if (!isSilent) {
            if (client) {
                this.sendAlert(client, reason
                    ? `YOU HAVE BEEN PERMANENTLY BANNED FROM THE SERVER REASON: ${reason}`
                    : "YOU HAVE BEEN BANNED FROM THE SERVER.");
            }
            this.sendAlertToAll(reason
                ? `${characterName} HAS BEEN BANNED FROM THE SERVER! REASON: ${reason}`
                : `${characterName} HAS BEEN BANNED FROM THE SERVER!`);
        }
        setTimeout(() => {
            if (!(client instanceof zoneclient_1.ZoneClient2016))
                return;
            this.kickPlayer(client);
        }, 3000);
    }
    enforceBan(client) {
        switch (client.banType) {
            case "normal":
                this.kickPlayer(client);
                return;
            case "rick":
                this.sendData(client, "ClientExitLaunchUrl", {
                    url: "https://www.youtube.com/watch?v=dQw4w9WgXcQ"
                });
                this.sendData(client, "LoginFailed", {});
                this.deleteClient(client);
                setTimeout(() => {
                    if (!client)
                        return;
                    this.deleteClient(client);
                }, 1000);
                break;
        }
    }
    kickPlayerWithReason(client, reason, sendGlobal = false) {
        if (!client || client.isAdmin)
            return;
        for (let i = 0; i < 5; i++) {
            this.sendAlert(client, `You are being kicked from the server. Reason: ${reason}`);
        }
        if (!client) {
            return;
        }
        if (sendGlobal) {
            this.sendGlobalChatText(`${client.character.name} has been kicked from the server!`);
        }
        this.kickPlayer(client);
    }
    kickPlayer(client) {
        if (!client || client.isAdmin)
            return;
        this.sendData(client, "CharacterSelectSessionResponse", {
            status: 0,
            sessionId: client.loginSessionId
        });
        this.deleteClient(client);
    }
    sendGameTimeSync(client) {
        debug("GameTimeSync");
        this.sendData(client, "GameTimeSync", {
            time: (0, utils_1.Int64String)(this.inGameTimeManager.time),
            cycleSpeed: this.inGameTimeManager.getCycleSpeed(),
            unknownBoolean1: false
        });
    }
    sendRawToAllOthersWithSpawnedCharacter(client, entityCharacterId = "", data) {
        for (const a in this._clients) {
            if (client != this._clients[a] &&
                this._clients[a].spawnedEntities.has(this._characters[entityCharacterId])) {
                this.sendRawDataReliable(this._clients[a], data);
            }
        }
    }
    sendRawToAllOthersWithSpawnedEntity(client, dictionary, entityCharacterId = "", data) {
        for (const a in this._clients) {
            if (client != this._clients[a] &&
                this._clients[a].spawnedEntities.has(dictionary[entityCharacterId])) {
                this.sendRawDataReliable(this._clients[a], data);
            }
        }
    }
    //#region ********************VEHICLE********************
    vehicleManager(client) {
        for (const key in this._vehicles) {
            const vehicle = this._vehicles[key];
            if (
            // vehicle spawning / managed object assignment logic
            (0, utils_1.isPosInRadius)(vehicle.npcRenderDistance || this.charactersRenderDistance, client.character.state.position, vehicle.state.position)) {
                if (!client.spawnedEntities.has(vehicle)) {
                    this.sendData(client, "AddLightweightVehicle", {
                        ...vehicle.pGetLightweightVehicle(),
                        unknownGuid1: this.generateGuid()
                    });
                    vehicle.effectTags.forEach((effectTag) => {
                        this.sendData(client, "Character.AddEffectTagCompositeEffect", {
                            characterId: vehicle.characterId,
                            effectId: effectTag,
                            unknownDword1: effectTag,
                            unknownDword2: effectTag
                        });
                    });
                    /*
                    if (vehicle.engineOn) {
                      this.sendData<>(client, "Vehicle.Engine", {
                        vehicleCharacterId: vehicle.characterId,
                        engineOn: true,
                      });
                    }
                    */
                    /*this.sendData<>(client, "Vehicle.OwnerPassengerList", {
                      characterId: client.character.characterId,
                      passengers: vehicle.pGetPassengers(this),
                    });*/
                    client.spawnedEntities.add(vehicle);
                }
                // disable managing vehicles with routine, leaving only managed when entering it
                /*if (!vehicle.isManaged) {
                  // assigns management to first client within radius
                  this.assignManagedObject(client, vehicle);
                }*/
            }
            else if (!(0, utils_1.isPosInRadius)(this.charactersRenderDistance, client.character.state.position, vehicle.state.position)) {
                // vehicle despawning / managed object drop logic
                const vehicleExist = client.spawnedEntities.has(vehicle);
                if (vehicleExist) {
                    if (vehicle.isManaged) {
                        this.dropManagedObject(client, vehicle);
                    }
                    this.sendData(client, "Character.RemovePlayer", {
                        characterId: vehicle.characterId
                    });
                    client.spawnedEntities.delete(vehicle);
                }
            }
        }
    }
    assignManagedObject(client, vehicle) {
        // todo: vehicle seat swap managed object assignment logic
        debug("\n\n\n\n\n\n\n\n\n\n assign managed object");
        this.sendData(client, "Character.ManagedObject", {
            objectCharacterId: vehicle.characterId,
            characterId: client.character.characterId
        });
        this.sendData(client, "ClientUpdate.ManagedObjectResponseControl", {
            control: true,
            objectCharacterId: vehicle.characterId
        });
        client.managedObjects.push(vehicle.characterId);
        vehicle.isManaged = true;
    }
    dropManagedObject(client, vehicle, keepManaged = false) {
        const index = client.managedObjects.indexOf(vehicle.characterId);
        if (index > -1) {
            // todo: vehicle seat swap managed object drop logic
            debug("\n\n\n\n\n\n\n\n\n\n drop managed object");
            this.sendData(client, "ClientUpdate.ManagedObjectResponseControl", {
                control: false,
                objectCharacterId: vehicle.characterId
            });
            client.managedObjects.splice(index, 1);
            // blocks vehicleManager from taking over management during a takeover
            if (!keepManaged)
                vehicle.isManaged = false;
        }
    }
    takeoverManagedObject(newClient, vehicle) {
        const index = newClient.managedObjects.indexOf(vehicle.characterId);
        if (index === -1) {
            // if object is already managed by client, do nothing
            debug("\n\n\n\n\n\n\n\n\n\n takeover managed object");
            for (const characterId in this._clients) {
                const oldClient = this._clients[characterId];
                const idx = oldClient.managedObjects.indexOf(vehicle.characterId);
                if (idx > -1) {
                    this.dropManagedObject(oldClient, vehicle, true);
                    break;
                }
            }
            this.assignManagedObject(newClient, vehicle);
        }
    }
    dropAllManagedObjects(client) {
        client.managedObjects.forEach((characterId) => {
            this.sendManagedObjectResponseControlPacket(client, {
                control: false,
                objectCharacterId: characterId
            });
            client.managedObjects.splice(client.managedObjects.findIndex((e) => e === characterId), 1);
        });
    }
    sendCompositeEffectToAllWithSpawnedEntity(dictionary, object, effectId) {
        this.sendDataToAllWithSpawnedEntity(dictionary, object.characterId, "Character.PlayWorldCompositeEffect", {
            characterId: object.characterId,
            effectId: effectId,
            position: object.state.position
        });
    }
    sendCompositeEffectToAllInRange(range, characterId, position, effectId) {
        this.sendDataToAllInRange(range, position, "Character.PlayWorldCompositeEffect", {
            characterId: characterId,
            effectId: effectId,
            position: position
        });
    }
    sendDialogEffectToAllInRange(range, characterId, position, effectId) {
        this.sendDataToAllInRange(range, position, "Command.PlayDialogEffect", {
            characterId: characterId,
            effectId: effectId
        });
    }
    /**
     * Cancels the currently playing emote for a character
     * @param client - The client whose emote should be cancelled
     */
    cancelEmote(client) {
        if (client.character.currentEmote === 0)
            return; // no emote playing
        // Send effect ID 0 to cancel the emote
        this.sendDataToAllWithSpawnedEntity(this._characters, client.character.characterId, "Command.PlayDialogEffect", {
            characterId: client.character.characterId,
            effectId: 0
        });
        client.character.currentEmote = 0;
    }
    /*sendEffectToAllWithSpawnedEntity( idk whats wrong with it, sometimes works and sometimes doesnt
      characterId: string,
      effectId: number,
      dictionary: any,
    ) {
        this.sendDataToAllWithSpawnedEntity(
            dictionary,
            characterId,
            "Command.PlayDialogEffect",
            {
                characterId: characterId,
                effectId: effectId,
            }
        );
      
    }*/
    debugSendData(packetName) {
        switch (packetName) {
            case "KeepAlive":
            case "PlayerUpdatePosition":
            case "GameTimeSync":
            case "Synchronization":
            case "Vehicle.StateData":
                break;
            default:
                debug("send data", packetName);
        }
    }
    sendDataToAllWithSpawnedEntity(dictionary, entityCharacterId = "", packetName, obj) {
        if (!entityCharacterId)
            return;
        const data = this._protocol.pack(packetName, obj);
        if (!data)
            return;
        this.debugSendData(packetName);
        for (const a in this._clients) {
            if (this._clients[a].spawnedEntities.has(dictionary[entityCharacterId]) ||
                this._clients[a].character.characterId == entityCharacterId) {
                this.sendRawDataReliable(this._clients[a], data);
            }
        }
    }
    sendDataToAllInRange(range, position, packetName, obj) {
        const data = this._protocol.pack(packetName, obj);
        if (!data)
            return;
        this.debugSendData(packetName);
        for (const a in this._clients) {
            if ((0, utils_1.isPosInRadius)(range, this._clients[a].character.state.position, position)) {
                this.sendRawDataReliable(this._clients[a], data);
            }
        }
    }
    sendDataToAllOthersWithSpawnedEntity(dictionary, client, entityCharacterId = "", packetName, obj) {
        if (!entityCharacterId)
            return;
        const data = this._protocol.pack(packetName, obj);
        if (!data)
            return;
        this.debugSendData(packetName);
        for (const a in this._clients) {
            if (client != this._clients[a] &&
                this._clients[a].spawnedEntities.has(dictionary[entityCharacterId])) {
                this.sendRawDataReliable(this._clients[a], data);
            }
        }
    }
    sendDataToAllAdmins(packetName, obj) {
        const data = this._protocol.pack(packetName, obj);
        if (data) {
            for (const a in this._clients) {
                if (this._clients[a].isAdmin) {
                    this.sendRawDataReliable(this._clients[a], data);
                }
            }
        }
    }
    getClientsInRange(range, position) {
        const clients = [];
        for (const a in this._clients) {
            const client = this._clients[a];
            if ((0, utils_1.isPosInRadius)(range, client.character.state.position, position))
                clients.push(client);
        }
        return clients;
    }
    mountVehicle(client, vehicleGuid) {
        const vehicle = this._vehicles[vehicleGuid];
        if (!vehicle || !vehicle.isMountable)
            return;
        if (vehicle.isLocked) {
            this.sendChatText(client, "Vehicle is locked.");
            return;
        }
        for (const a in this._constructionFoundations) {
            const foundation = this._constructionFoundations[a];
            if (foundation.isSecured &&
                foundation.isInside(vehicle.state.position) &&
                !foundation.getHasPermission(this, client.character.characterId, enums_1.ConstructionPermissionIds.VISIT) &&
                (!client.isAdmin || !client.isDebugMode))
                return;
        }
        if (client.hudTimer != null) {
            clearTimeout(client.hudTimer);
            client.hudTimer = null;
        }
        client.character.isRunning = false; // maybe some async stuff make this useless need to test that
        const seatId = vehicle.getNextSeatId(this), seat = vehicle.seats[seatId], passenger = this._characters[seat];
        if (Number(seatId) < 0)
            return; // no available seats in vehicle
        client.vehicle.mountedVehicle = vehicle.characterId;
        client.isInAir = false;
        if (passenger) {
            // dismount the driver
            const client = this.getClientByCharId(passenger.characterId);
            if (client) {
                this.dismountVehicle(client);
            }
        }
        vehicle.seats[seatId] = client.character.characterId;
        if (seatId == 0) {
            this.takeoverManagedObject(client, vehicle);
            this.abilitiesManager.sendVehicleAbilities(this, client, vehicle);
            //vehicle.checkEngineRequirements(this);
            this.sendData(client, "Vehicle.Owner", {
                guid: vehicle.characterId,
                characterId: client.character.characterId,
                unknownDword1: 0,
                vehicleId: vehicle.vehicleId,
                passengers: [
                    {
                        characterId: client.character.characterId,
                        identity: {
                            characterName: client.character.name
                        },
                        unknownString1: "",
                        unknownByte1: 1
                    }
                ]
            });
        }
        // Mount container on all players in the vehicle
        if (vehicle.getContainer()) {
            client.character.mountContainer(this, vehicle);
        }
        this.sendDataToAllWithSpawnedEntity(this._vehicles, vehicleGuid, "Mount.MountResponse", {
            characterId: client.character.characterId,
            vehicleGuid: vehicle.characterId,
            seatId: Number(seatId),
            isDriver: seatId == 0 ? 1 : 0,
            identity: {}
        });
        this.sendData(client, "Vehicle.Occupy", {
            guid: vehicle.characterId,
            characterId: client.character.characterId,
            vehicleId: vehicle.vehicleId,
            clearLoadout: 0,
            unknownArray1: [
                {
                    unknownDword1: 0,
                    unknownBoolean1: 0
                }
            ],
            passengers: [
                {
                    characterId: client.character.characterId,
                    identity: {
                        characterName: client.character.name
                    }
                }
            ],
            unknownArray2: [{}],
            unknownBytes1: { itemData: {} },
            unknownBytes2: { itemData: {} }
        });
        // Workaround for fifth player vehicle bug. Ensure the camera position updates correctly while maintaining normal client functionality. TODO: Fix this properly (GitHub issue #2304)
        if (seatId == 4) {
            this.sendData(client, "Mount.SeatChangeResponse", {
                characterId: client.character.characterId,
                vehicleGuid: vehicle.characterId,
                identity: {},
                seatId: seatId,
                unknownDword2: 0 // if set to 1 the selected character will have drive access
            });
        }
    }
    dismountVehicle(client) {
        if (!client.vehicle.mountedVehicle)
            return;
        client.enableChecks = false;
        setTimeout(() => {
            client.enableChecks = true;
        }, 5000);
        const vehicle = this._vehicles[client.vehicle.mountedVehicle];
        if (!vehicle) {
            // return if vehicle doesnt exist
            console.log(`Error: ${client.character.name} exited non existing vehicle`);
            this.sendData(client, "Mount.DismountResponse", {
                characterId: client.character.characterId
            });
            return;
        }
        vehicle.removeHotwireEffect(this);
        const seatId = vehicle.getCharacterSeat(client.character.characterId);
        client.character.vehicleExitDate = new Date().getTime();
        if (seatId == -1) {
            console.log(`Error: ${client.character.name} exited vehicle with no seatId set`);
            return;
        }
        if (vehicle.vehicleId == enums_1.VehicleIds.SPECTATE) {
            this.sendData(client, "Mount.DismountResponse", {
                characterId: client.character.characterId
            });
            this.deleteEntity(vehicle.characterId, this._vehicles);
            return;
        }
        vehicle.seats[seatId] = "";
        this.sendDataToAllWithSpawnedEntity(this._characters, client.character.characterId, "Mount.DismountResponse", {
            characterId: client.character.characterId
        });
        client.isInAir = false;
        // Check if there are no more passengers in the vehicle
        if (vehicle.getPassengerList().length === 0) {
            // Turn off the engine
            if (vehicle.engineOn)
                vehicle.stopEngine(this);
            // Unlock the vehicle
            vehicle.isLocked = false;
        }
        client.vehicle.mountedVehicle = "";
        this.sendData(client, "Vehicle.Occupy", {
            guid: "",
            characterId: client.character.characterId,
            vehicleId: 0,
            clearLoadout: 1,
            unknownArray1: [
                {
                    unknownDword1: 0,
                    unknownBoolean1: 0
                }
            ],
            passengers: [],
            unknownArray2: [],
            unknownBytes1: { itemData: {} },
            unknownBytes2: { itemData: {} }
        });
        this.sendDataToAllWithSpawnedEntity(this._vehicles, vehicle.characterId, "Vehicle.Owner", {
            guid: vehicle.characterId,
            characterId: "",
            unknownDword1: 0,
            vehicleId: 0,
            passengers: []
        });
        client.character.dismountContainer(this);
    }
    changeSeat(client, packet) {
        if (!client.vehicle.mountedVehicle)
            return;
        const vehicle = this._vehicles[client.vehicle.mountedVehicle];
        if (!vehicle)
            return;
        const seatCount = vehicle.getSeatCount(), oldSeatId = vehicle.getCharacterSeat(client.character.characterId);
        const seatId = packet.data.seatId ?? 0, seat = vehicle.seats[seatId], passenger = this._characters[seat];
        if (seatId < seatCount &&
            (!vehicle.seats[seatId] || !passenger?.isAlive) &&
            oldSeatId != -1) {
            if (seatId === 2) {
                if (!!vehicle.seats[5])
                    return;
            }
            if (seatId === 3) {
                if (!!vehicle.seats[6])
                    return;
            }
            if (passenger && !passenger?.isAlive) {
                const client = this.getClientByCharId(passenger.characterId);
                if (client) {
                    this.dismountVehicle(client);
                }
            }
            this.sendDataToAllWithSpawnedEntity(this._vehicles, client.vehicle.mountedVehicle, "Mount.SeatChangeResponse", {
                characterId: client.character.characterId,
                vehicleGuid: vehicle.characterId,
                identity: {},
                seatId: packet.data.seatId,
                unknownDword2: packet.data.seatId === 0 ? 1 : 0 // if set to 1 the select character will have drive access
            });
            vehicle.seats[oldSeatId] = "";
            vehicle.seats[seatId] = client.character.characterId;
            if (!oldSeatId && vehicle.engineOn) {
                vehicle.stopEngine(this);
                client.character.dismountContainer(this);
            }
            if (seatId === 0) {
                this.takeoverManagedObject(client, vehicle);
                if (vehicle.hasRequiredComponents(this) && !vehicle.engineOn) {
                    vehicle.startEngine(this);
                }
                if (vehicle.getContainer()) {
                    client.character.mountContainer(this, vehicle);
                }
            }
        }
    }
    //#endregion
    startTimer(client, stringId, time) {
        this.sendData(client, "ClientUpdate.StartTimer", {
            stringId: stringId,
            time: time
        });
    }
    startInteractionTimer(client, stringId, time, animationId) {
        this.sendDataToAllWithSpawnedEntity(this._characters, client.character.characterId, "CharacterState.InteractionStart", {
            characterId: client.character.characterId,
            timeData: {
                duration: time
            },
            animationId: animationId,
            stringId: stringId
        });
    }
    reloadInterrupt(client, weaponItem) {
        if (!weaponItem || !weaponItem.weapon || !weaponItem.weapon.reloadTimer)
            return;
        client.character.clearReloadTimeout();
        this.sendWeaponData(client, "Weapon.Reload", {
            weaponGuid: weaponItem.itemGuid,
            unknownDword1: weaponItem.weapon.ammoCount,
            ammoCount: weaponItem.weapon.ammoCount,
            unknownDword3: weaponItem.weapon.ammoCount,
            currentReloadCount: (0, utils_1.toHex)(++weaponItem.weapon.currentReloadCount)
        });
        this.sendRemoteWeaponUpdateDataToAllOthers(client, client.character.transientId, weaponItem.itemGuid, "Update.ReloadInterrupt", {});
    }
    combatLog(client) {
        if (client.character.isAlive) {
            this.sendChatText(client, "You must be dead to use combatlog");
            return;
        }
        if (!client.character.getCombatLog().length) {
            this.sendChatText(client, "No combatlog info available");
            return;
        }
        const combatlog = client.character.getCombatLog();
        this.sendChatText(client, "---------------------------------COMBATLOG:--------------------------------");
        this.sendChatText(client, `TIME | SOURCE | TARGET | WEAPON | DISTANCE | HITLOCATION | HITPOSITION | OLD HP | NEW HP | PING | ENEMY PING | MESSAGE`);
        combatlog.forEach((e) => {
            const time = `${((Date.now() - e.hitInfo.timestamp) / 1000).toFixed(1)}s`, source = e.source.name == client.character.name
                ? "YOU"
                : e.source.name || "undefined", target = e.target.name == client.character.name
                ? "YOU"
                : e.target.name || "undefined", hitPosition = `[${e.hitInfo.hitPosition[0].toFixed(0)}, ${e.hitInfo.hitPosition[1].toFixed(0)}, ${e.hitInfo.hitPosition[2].toFixed(0)}]`, oldHp = (e.hitInfo.oldHP / 100).toFixed(1), newHp = (e.hitInfo.newHP / 100).toFixed(1), ping = `${e.source.name == client.character.name ? e.source.ping : e.target.ping}ms`, enemyPing = `${e.source.name == client.character.name ? e.target.ping : e.source.ping}ms`;
            this.sendChatText(client, `${time} ${source} ${target} ${this.getItemDefinition(e.hitInfo.weapon || 0)?.MODEL_NAME || "N/A"} ${e.hitInfo.distance}m ${e.hitInfo.hitLocation} ${hitPosition} ${oldHp} ${newHp} ${ping} ${enemyPing} ${e.hitInfo.message}`);
        });
        this.sendChatText(client, "---------------------------------------------------------------------------------");
    }
    //#region ********************INVENTORY********************   } }
    /**
     * Adds an item to a character's inventory.
     *
     * @param client - The client adding the item.
     * @param item - The item to add.
     * @param containerDefinitionId - The id of the container definition.
     * @param character [character=client.character] - The character to add the item to.
     */
    addItem(client, item, containerDefinitionId, character = client.character) {
        const characterIds = new Set();
        const vehicle = this._vehicles[character.characterId];
        if (vehicle) {
            vehicle.getPassengerList().forEach((characterId) => {
                characterIds.add(characterId);
            });
        }
        else {
            if (character instanceof baselootableentity_1.BaseLootableEntity) {
                characterIds.add(character.mountedCharacter || "");
            }
            else {
                characterIds.add(character.characterId);
            }
        }
        characterIds.forEach((characterId) => {
            const client = this.getClientByCharId(characterId);
            if (!client || !client.character?.initialized)
                return;
            this.sendData(client, "ClientUpdate.ItemAdd", {
                characterId: character instanceof character_1.Character2016 || character instanceof vehicle_1.Vehicle2016
                    ? character.characterId
                    : constants_1.EXTERNAL_CONTAINER_GUID,
                data: character.pGetItemData(this, item, containerDefinitionId)
            });
        });
    }
    /**
     * Generates random equipment for the specified entity and slots (Zombies only).
     * To be deprecated soon
     *
     * @param entity - The entity to generate equipment for.
     * @param slots - The slots to generate equipment for.
     * @param excludedModels [excludedModels=[]] - The excluded equipment models.
     */
    generateRandomEquipmentsFromAnEntity(entity, slots, excludedModels = []) {
        slots.forEach((slot) => {
            entity._equipment[slot] = this.generateRandomEquipmentForSlot(slot, entity.gender, excludedModels);
        });
    }
    /**
     * Generates random equipment for a specific slot (Zombies only).
     *
     * @param slotId - The ID of the slot.
     * @param gender - The gender of the entity.
     * @param excludedModels [excludedModels=[]] - The excluded equipment models.
     * @returns The generated equipment.
     */
    generateRandomEquipmentForSlot(slotId, gender, excludedModels = []) {
        const models = equipmentModelTexturesMapping[slotId];
        if (excludedModels.length) {
            for (const model in models) {
                if (excludedModels.includes(model)) {
                    delete models[model];
                }
            }
        }
        const model = (0, utils_1.getRandomKeyFromAnObject)(models);
        const skins = equipmentModelTexturesMapping[slotId][model];
        let skin;
        if (skins) {
            skin = (0, utils_1.getRandomFromArray)(skins);
        }
        else {
            skin = "";
        }
        return {
            modelName: model.replace("<gender>", gender == 1 ? "Male" : "Female"),
            slotId,
            textureAlias: skin,
            guid: (0, utils_1.toBigHex)(this.generateItemGuid())
        };
    }
    isAccountItem(itemDefinitionId) {
        if (!itemDefinitionId)
            return false;
        const itemDef = this.getItemDefinition(itemDefinitionId);
        return [
            "RewardCrate",
            "LockedRewardCrate",
            "RewardCrateKey",
            "AccountRecipe",
            "IncrementEntitlement",
            "EmoteAnimation",
            "AccountGiveRewardSet"
        ].includes(itemDef?.CODE_FACTORY_NAME ?? "");
    }
    /**
     * Gets the rewards for a given itemDefinitionId.
     *
     * @param {number} [itemDefinitionId] - The ID of the crate to retrieve rewards from.
     * @returns {RewardCrateRewardDefinition[]|undefined} The rewards.
     */
    getCrateRewards(itemDefinitionId) {
        if (!itemDefinitionId)
            return;
        return Object.values(this._rewardCrateDefinitions).find((i) => i.itemDefinitionId === itemDefinitionId)?.rewards;
    }
    /**
     * Gets a random reward for a given crate.
     *
     * @param {number} [itemDefinitionId] - The ID of the crate to retrieve rewards from.
     * @returns {number} Reward Item definition ID.
     */
    getRandomCrateReward(itemDefinitionId) {
        const rewards = this.getCrateRewards(itemDefinitionId);
        if (!rewards)
            return;
        const totalChances = rewards.reduce((acc, reward) => acc + reward.rewardChance, 0);
        let randomChance = Math.random() * totalChances;
        for (const rew of rewards) {
            if (randomChance < rew.rewardChance) {
                return {
                    reward: rew.itemDefinitionId,
                    isRare: rew.rewardChance === 1 || rew.rewardChance === 0.5
                };
            }
            randomChance -= rew.rewardChance;
        }
    }
    /**
     * Gets the item definition for a given itemDefinitionId.
     *
     * @param {number} [itemDefinitionId] - The ID of the item definition to retrieve.
     * @returns {ItemDefinition|undefined} The item definition or undefined.
     */
    getItemDefinition(itemDefinitionId) {
        if (!itemDefinitionId)
            return;
        return this._itemDefinitions[itemDefinitionId];
    }
    /**
     * Gets the max durability for a given itemDefinitionId.
     *
     * @param {number} [itemDefinitionId] - The ID of the item definition to retrieve.
     * @returns {ItemDefinition|undefined} The item definition or undefined.
     */
    getItemBaseDurability(itemDefinitionId) {
        if (!itemDefinitionId)
            return 0;
        switch (true) {
            case this.isGeneric(itemDefinitionId) &&
                itemDefinitionId == enums_1.Items.SKINNING_KNIFE:
                return 2000;
            case this.isConstruction(itemDefinitionId):
            case this.isGeneric(itemDefinitionId) && !this.isConvey(itemDefinitionId):
                return 0;
            case itemDefinitionId == enums_1.Items.WEAPON_HATCHET_MAKESHIFT:
            case itemDefinitionId == enums_1.Items.WEAPON_BRANCH:
                return 500;
            case itemDefinitionId == enums_1.Items.WEAPON_BAT_ALUM:
                return 3000;
            case itemDefinitionId == enums_1.Items.WEAPON_AXE_WOOD:
            case itemDefinitionId == enums_1.Items.WEAPON_AXE_FIRE:
                return 4000;
            case itemDefinitionId == enums_1.Items.WEAPON_TORCH:
            case itemDefinitionId == enums_1.Items.WEAPON_COMBATKNIFE:
            case this.isArmor(itemDefinitionId):
                return 1000;
            case this.isHelmet(itemDefinitionId):
                return 100;
            case this.isConvey(itemDefinitionId):
                return 5400;
            case this.isWeapon(itemDefinitionId):
            default:
                return 2000;
        }
    }
    /**
     * Gets the weapon definition for a given weaponDefinitionId.
     *
     * @param {number} weaponDefinitionId - The ID of the weapon definition to retrieve.
     * @returns {WeaponDefinition|undefined} The weapon definition or undefined.
     */
    getWeaponDefinition(weaponDefinitionId) {
        if (!weaponDefinitionId)
            return;
        return this._weaponDefinitions[weaponDefinitionId]?.DATA;
    }
    /**
     * Gets the firegroup definition for a given firegroupId.
     *
     * @param {number} firegroupId - The ID of the firegroup definition to retrieve.
     * @returns {FiregroupDefinition|undefined} The firegroup definition or undefined.
     */
    getFiregroupDefinition(firegroupId) {
        return this._firegroupDefinitions[firegroupId]?.DATA;
    }
    /**
     * Gets the firemode definition for a given firemodeId.
     *
     * @param {number} firemodeId - The ID of the firemode definition to retrieve.
     * @returns {FiremodeDefinition|undefined} The firemode definition or undefined.
     */
    getFiremodeDefinition(firemodeId) {
        return this._firemodeDefinitions[firemodeId]?.DATA.DATA;
    }
    /**
     * Gets the ammoId for a given weapon.
     *
     * @param {number} itemDefinitionId - The itemDefinitionId of the weapon.
     * @returns {number} The ammoId (0 if undefined).
     */
    getWeaponAmmoId(itemDefinitionId) {
        const itemDefinition = this.getItemDefinition(itemDefinitionId), weaponDefinition = this.getWeaponDefinition(itemDefinition?.PARAM1 || 0), firegroupDefinition = this.getFiregroupDefinition(weaponDefinition?.FIRE_GROUPS[0]?.FIRE_GROUP_ID), firemodeDefinition = this.getFiremodeDefinition(firegroupDefinition?.FIRE_MODES[0]?.FIRE_MODE_ID);
        return firemodeDefinition?.AMMO_ITEM_ID || 0;
    }
    /**
     * Gets the reload time in milliseconds for a given weapon.
     *
     * @param {number} itemDefinitionId - The itemDefinitionId of the weapon.
     * @returns {number} The reload time in milliseconds (0 if undefined).
     */
    getWeaponReloadTime(itemDefinitionId) {
        const itemDefinition = this.getItemDefinition(itemDefinitionId), weaponDefinition = this.getWeaponDefinition(itemDefinition?.PARAM1 || 0), firegroupDefinition = this.getFiregroupDefinition(weaponDefinition?.FIRE_GROUPS[0]?.FIRE_GROUP_ID), firemodeDefinition = this.getFiremodeDefinition(firegroupDefinition?.FIRE_MODES[0]?.FIRE_MODE_ID);
        return firemodeDefinition?.RELOAD_TIME_MS || 0;
    }
    /**
     * Gets the clip size for a given weapon.
     *
     * @param {number} itemDefinitionId - The itemDefinitionId of the weapon.
     * @returns {number} The clip size (0 if undefined).
     */
    getWeaponClipSize(itemDefinitionId) {
        const itemDefinition = this.getItemDefinition(itemDefinitionId), weaponDefinition = this.getWeaponDefinition(itemDefinition?.PARAM1 || 0);
        return weaponDefinition.AMMO_SLOTS[0]?.CLIP_SIZE || 0;
    }
    /**
     * Gets the maximum amount of ammo a clip can hold for a given weapon.
     *
     * @param {number} itemDefinitionId - The itemDefinitionId of the weapon.
     * @returns {number} The maximum ammo (0 if undefined).
     */
    getWeaponMaxAmmo(itemDefinitionId) {
        const itemDefinition = this.getItemDefinition(itemDefinitionId), weaponDefinition = this.getWeaponDefinition(itemDefinition?.PARAM1 || 0);
        return weaponDefinition.AMMO_SLOTS[0]?.CLIP_SIZE || 0;
    }
    /**
     * Gets the container definition for a given containerDefinitionId.
     *
     * @param {number} containerDefinitionId - The id of the container definition to retrieve.
     * @returns {ContainerDefinition} The container definition.
     */
    getContainerDefinition(containerDefinitionId) {
        if (this._containerDefinitions[containerDefinitionId]) {
            return this._containerDefinitions[containerDefinitionId];
        }
        else {
            console.log(`Tried to get containerDefinition for invalid containerDefinitionId ${containerDefinitionId}`);
            return this._containerDefinitions[119];
        }
    }
    /**
     * Gets the maximum value for a given resource.
     *
     * @param {ResourceIds} resourceId - The ID of the resource.
     * @returns {number} The maximum value of the resource (0 if undefined).
     */
    getResourceMaxValue(resourceId) {
        if (!resourceDefinitions[resourceId])
            return 0;
        return resourceDefinitions[resourceId].MAX_VALUE || 0;
    }
    /**
     * Generates and returns an unused itemGuid.
     *
     * @returns {bigint} The generated itemGuid.
     */
    generateItemGuid() {
        return ++this.lastItemGuid;
    }
    /**
     * Generates a new item with the specified itemDefinitionId and count.
     *
     * @param {number} itemDefinitionId - The itemDefinitionId of the item to generate.
     * @param {number} [count=1] - The count of the item.
     * @param {boolean} [forceMaxDurability =false] - force the item to have his max durability.
     * @returns {BaseItem|undefined} The generated item, or undefined if the item definition is invalid.
     */
    generateItem(itemDefinitionId, count = 1, forceMaxDurability = false) {
        const itemDefinition = this.getItemDefinition(itemDefinitionId);
        if (!itemDefinition) {
            debug(`[ERROR] GenerateItem: Invalid item definition: ${itemDefinitionId}`);
            return;
        }
        const generatedGuid = (0, utils_1.toBigHex)(this.generateItemGuid());
        let durability = this.getItemBaseDurability(itemDefinitionId);
        let wornOffDurability = 0;
        const weaponDefinitionId = itemDefinition.PARAM1;
        switch (weaponDefinitionId) {
            case enums_1.WeaponDefinitionIds.WEAPON_NAGAFENS_RAGE:
            case enums_1.WeaponDefinitionIds.WEAPON_REAPER:
            case enums_1.WeaponDefinitionIds.WEAPON_FROSTBITE:
            case enums_1.WeaponDefinitionIds.WEAPON_BLAZE:
            case enums_1.WeaponDefinitionIds.WEAPON_PURGE:
                durability = 1000;
                break;
            case enums_1.WeaponDefinitionIds.WEAPON_WRENCH:
                durability = 2000;
                break;
            case enums_1.WeaponDefinitionIds.WEAPON_CROWBAR:
            case enums_1.WeaponDefinitionIds.WEAPON_HAMMER:
                if (!forceMaxDurability) {
                    do {
                        wornOffDurability = Math.floor(Math.random() * durability);
                    } while (wornOffDurability < 500);
                    break;
                }
            case enums_1.WeaponDefinitionIds.WEAPON_308:
            case enums_1.WeaponDefinitionIds.WEAPON_SHOTGUN:
            case enums_1.WeaponDefinitionIds.WEAPON_AK47:
            case enums_1.WeaponDefinitionIds.WEAPON_AR15:
            case enums_1.WeaponDefinitionIds.WEAPON_1911:
            case enums_1.WeaponDefinitionIds.WEAPON_M9:
            case enums_1.WeaponDefinitionIds.WEAPON_MAGNUM:
            case enums_1.WeaponDefinitionIds.WEAPON_R380:
                if (!forceMaxDurability) {
                    do {
                        wornOffDurability = Math.floor(Math.random() * durability);
                    } while (wornOffDurability < 350);
                    break;
                }
        }
        if (wornOffDurability > 0)
            durability = wornOffDurability;
        const itemData = new baseItem_1.BaseItem(itemDefinitionId, generatedGuid, durability, count);
        if (this.isWeapon(itemDefinitionId)) {
            itemData.weapon = new weapon_1.Weapon(itemData);
        }
        return itemData;
    }
    generateAccountItem(itemDefinitionId, count = 1) {
        const itemDefinition = this.getItemDefinition(itemDefinitionId);
        if (!itemDefinition) {
            debug(`[ERROR] GenerateItem: Invalid item definition: ${itemDefinitionId}`);
            return;
        }
        const generatedGuid = (0, utils_1.generateRandomGuid)();
        const itemData = new baseItem_1.BaseItem(itemDefinitionId, generatedGuid, 100, count);
        return itemData;
    }
    /**
     * Checks if an item with the specified itemDefinitionId is a weapon.
     *
     * @param {number} itemDefinitionId - The itemDefinitionId to check.
     * @returns {boolean} True if the item is a weapon, false otherwise.
     */
    isWeapon(itemDefinitionId) {
        return (this.getItemDefinition(itemDefinitionId)?.ITEM_TYPE == enums_1.ItemTypes.WEAPON);
    }
    /**
     * Checks if an item with the specified itemDefinitionId is a container.
     *
     * @param {number} itemDefinitionId - The itemDefinitionId to check.
     * @returns {boolean} True if the item is a container, false otherwise.
     */
    isContainer(itemDefinitionId) {
        return (this.getItemDefinition(itemDefinitionId)?.ITEM_TYPE == enums_1.ItemTypes.CONTAINER);
    }
    /**
     * Checks if an item with the specified itemDefinitionId is an armor.
     *
     * @param {number} itemDefinitionId - The itemDefinitionId to check.
     * @returns {boolean} True if the item is an armor, false otherwise.
     */
    isArmor(itemDefinitionId) {
        return (this.getItemDefinition(itemDefinitionId)?.ITEM_CLASS ==
            enums_1.ItemClasses.BODY_ARMOR);
    }
    /**
     * Checks if an item with the specified itemDefinitionId is a helmet.
     *
     * @param {number} itemDefinitionId - The itemDefinitionId to check.
     * @returns {boolean} True if the item is a helmet, false otherwise.
     */
    isHelmet(itemDefinitionId) {
        const itemDef = this.getItemDefinition(itemDefinitionId);
        return itemDef?.ITEM_CLASS == 25000 && itemDef?.IS_ARMOR == 1;
    }
    /**
     * Checks if an item with the specified itemDefinitionId is a convey.
     *
     * @param {number} itemDefinitionId - The itemDefinitionId to check.
     * @returns {boolean} True if the item is a convey, false otherwise.
     */
    isConvey(itemDefinitionId) {
        return this.getItemDefinition(itemDefinitionId)?.DESCRIPTION_ID == 11895;
    }
    /**
     * Checks if an item with the specified itemDefinitionId is a boot.
     *
     * @param {number} itemDefinitionId - The itemDefinitionId to check.
     * @returns {boolean} True if the item is a convey, false otherwise.
     */
    isBoot(itemDefinitionId) {
        return [11155, 13581].includes(this.getItemDefinition(itemDefinitionId)?.DESCRIPTION_ID ?? 0);
    }
    /**
     * Checks if an item with the specified itemDefinitionId is a zed shoe.
     *
     * @param {number} itemDefinitionId - The itemDefinitionId to check.
     * @returns {boolean} True if the item is a convey, false otherwise.
     */
    isZed(itemDefinitionId) {
        return this.getItemDefinition(itemDefinitionId)?.DESCRIPTION_ID == 15026;
    }
    /**
     * Checks if an item with the specified itemDefinitionId is a gator shoe.
     *
     * @param {number} itemDefinitionId - The itemDefinitionId to check.
     * @returns {boolean} True if the item is a convey, false otherwise.
     */
    isGator(itemDefinitionId) {
        return [14239, 12496].includes(this.getItemDefinition(itemDefinitionId)?.DESCRIPTION_ID ?? 0);
    }
    /**
     * Checks if an item with the specified itemDefinitionId is footwear.
     *
     * @param {number} itemDefinitionId - The itemDefinitionId to check.
     * @returns {boolean} True if the item is a boot, false otherwise.
     */
    isFootwear(itemDefinitionId) {
        return (this.getItemDefinition(itemDefinitionId)?.ITEM_CLASS ==
            enums_1.ItemClasses.FOOTWEAR);
    }
    /**
     * Checks if an item with the specified itemDefinitionId is a generic item type.
     *
     * @param {number} itemDefinitionId - The itemDefinitionId to check.
     * @returns {boolean} True if the item is a generic type, false otherwise.
     */
    isGeneric(itemDefinitionId) {
        return this.getItemDefinition(itemDefinitionId)?.ITEM_TYPE == 1;
    }
    /**
     * Checks if an item with the specified itemDefinitionId is a construction type.
     *
     * @param {number} itemDefinitionId - The itemDefinitionId to check.
     * @returns {boolean} True if the item is a generic type, false otherwise.
     */
    isConstruction(itemDefinitionId) {
        return [40, 41].includes(this.getItemDefinition(itemDefinitionId)?.ITEM_TYPE ?? 0);
    }
    /**
     * Checks if an item with the specified itemDefinitionId is stackable.
     *
     * @param {number} itemDefinitionId - The itemDefinitionId to check.
     * @returns {boolean} True if the item is stackable, false otherwise.
     */
    isStackable(itemDefinitionId) {
        const itemDefinition = this.getItemDefinition(itemDefinitionId);
        if (!itemDefinition)
            return false;
        return itemDefinition.MAX_STACK_SIZE > 1 ? true : false;
    }
    /**
     * Validates if an item can be equipped in the specified equipment slot.
     *
     * @param {number} itemDefinitionId - The itemDefinitionId of the item to validate.
     * @param {number} equipmentSlotId - The equipment slot ID.
     * @returns {boolean} True if the item can be equipped in the slot, false otherwise.
     */
    validateEquipmentSlot(itemDefinitionId, equipmentSlotId) {
        const itemDefinition = this.getItemDefinition(itemDefinitionId);
        if (!itemDefinition || !itemDefinition.FLAG_CAN_EQUIP)
            return false;
        return !!equipSlotItemClasses.find((slot) => slot.ITEM_CLASS === itemDefinition.ITEM_CLASS &&
            equipmentSlotId === slot.EQUIP_SLOT_ID);
    }
    /**
     * Validates that a given itemDefinitionId can be equipped in a given loadout slot.
     *
     * @param {number} itemDefinitionId - The definition ID of an item to validate.
     * @param {number} loadoutSlotId - The loadoutSlotId to have the item validated for.
     * @param {number} loadoutId - The loadoutId of the entity to get the slot for.
     * @returns {boolean} Returns true/false if the item can go in a specified loadout slot.
     */
    validateLoadoutSlot(itemDefinitionId, loadoutSlotId, loadoutId) {
        const itemDefinition = this.getItemDefinition(itemDefinitionId);
        if (!itemDefinition || !itemDefinition.FLAG_CAN_EQUIP)
            return false;
        return !!loadoutSlotItemClasses.find((slot) => slot.ITEM_CLASS == itemDefinition.ITEM_CLASS &&
            loadoutSlotId == slot.SLOT &&
            slot.LOADOUT_ID == loadoutId);
    }
    /**
     * Gets the first loadout slot that a specified item is able to go into.
     *
     * @param {number} itemDefId - The definition ID of an item to check.
     * @param {number} [loadoutId=LoadoutIds.CHARACTER] - Optional: The loadoutId of the entity to get the slot for, default LoadoutIds.CHARACTER.
     * @returns {number} Returns the ID of the first loadout slot that an item can go into (occupied or not).
     */
    getLoadoutSlot(itemDefId, loadoutId = enums_1.LoadoutIds.CHARACTER) {
        const itemDefinition = this.getItemDefinition(itemDefId);
        if (!itemDefinition)
            return 0;
        const loadoutSlotItemClass = loadoutSlotItemClasses.find((slot) => slot.ITEM_CLASS == itemDefinition.ITEM_CLASS &&
            loadoutId == slot.LOADOUT_ID);
        return loadoutSlotItemClass?.SLOT || 0;
    }
    /**
     * Switches the loadout slot for a client.
     *
     * @param {Client} client - The client to switch the loadout slot for.
     * @param {LoadoutItem} loadoutItem - The new loadout item.
     */
    switchLoadoutSlot(client, loadoutItem) {
        const oldLoadoutSlot = client.character.currentLoadoutSlot;
        this.reloadInterrupt(client, client.character._loadout[oldLoadoutSlot]);
        // disable old ability
        const weapon = client.character.getEquippedWeapon(), currentWeaponItemDef = this.getItemDefinition(weapon?.itemDefinitionId);
        if (currentWeaponItemDef) {
            this.abilitiesManager.deactivateAbility(this, client, currentWeaponItemDef.ACTIVATABLE_ABILITY_ID);
        }
        // remove passive equip
        this.clearEquipmentSlot(client.character, client.character.getActiveEquipmentSlot(loadoutItem), true);
        client.character.currentLoadoutSlot = loadoutItem.slotId;
        client.character.equipItem(this, loadoutItem, true, loadoutItem.slotId);
        // equip passive slot
        client.character.equipItem(this, client.character._loadout[oldLoadoutSlot], true, oldLoadoutSlot);
        if (loadoutItem.weapon)
            loadoutItem.weapon.currentReloadCount = 0;
        if (this.isWeapon(loadoutItem.itemDefinitionId)) {
            const itemDefinition = this.getItemDefinition(loadoutItem.itemDefinitionId);
            if (itemDefinition) {
                this.abilitiesManager.deactivateAbility(this, client, itemDefinition.ACTIVATABLE_ABILITY_ID);
            }
            this.sendRemoteWeaponUpdateDataToAllOthers(client, client.character.transientId, loadoutItem.itemGuid, "Update.SwitchFireMode", {
                firegroupIndex: 0,
                firemodeIndex: 0
            });
            this.sendDataToAllOthersWithSpawnedEntity(this._characters, client, client.character.characterId, "Character.WeaponStance", {
                characterId: client.character.characterId,
                stance: client.character.weaponStance
            });
        }
    }
    /**
     * Clears a character's equipment slot.
     *
     * @param {BaseFullCharacter} character - The character to have their equipment slot cleared.
     * @param {number} equipmentSlotId - The equipment slot to clear.
     * @param {boolean} [sendPacket=true] - Optional: Specifies whether to send a packet to other clients, default is true.
     * @returns {boolean} Returns true if the slot was cleared, false if the slot is invalid.
     */
    clearEquipmentSlot(character, equipmentSlotId, sendPacket = true) {
        if (!equipmentSlotId)
            return false;
        delete character._equipment[equipmentSlotId];
        const client = this.getClientByContainerAccessor(character);
        if (!client)
            return true;
        if (client.character.initialized && sendPacket) {
            const data = {
                characterData: {
                    characterId: client.character.characterId
                },
                slotId: equipmentSlotId
            };
            this.sendDataToAllWithSpawnedEntity(this._characters, client.character.characterId, "Equipment.UnsetCharacterEquipmentSlot", data);
        }
        if (equipmentSlotId === enums_1.EquipSlots.RHAND) {
            client.character.currentLoadoutSlot = enums_1.LoadoutSlots.FISTS;
            client.character.equipItem(this, client.character._loadout[enums_1.LoadoutSlots.FISTS]); //equip fists
        }
        return true;
    }
    async lootAccountItem(server, client, item) {
        if (!item) {
            return;
        }
        const accountItems = await server.accountInventoriesManager.getAccountItems(client.loginSessionId);
        if (!accountItems)
            return false;
        const savedItem = accountItems.find((v) => {
            return v.itemDefinitionId === item.itemDefinitionId;
        });
        // if an itemdef is already in the account inventory we only update the stack count
        if (savedItem) {
            savedItem.stackCount += item.stackCount;
            await server.accountInventoriesManager.updateAccountItem(client.loginSessionId, savedItem);
            server.sendData(client, "Items.UpdateAccountItem", {
                itemId: savedItem.itemGuid,
                itemDefinitionId: savedItem.itemDefinitionId,
                itemCount: savedItem.stackCount
            });
        }
        else {
            await server.accountInventoriesManager.addAccountItem(client.loginSessionId, item);
            server.sendData(client, "Items.AddAccountItem", {
                itemId: item.itemGuid,
                itemDefinitionId: item.itemDefinitionId,
                itemCount: item.stackCount
            });
        }
    }
    lootCrateWithChance(client, dropChance) {
        // dropChance ranges 0-1000
        if ((0, utils_1.chance)(dropChance)) {
            this.rewardManager.dropReward(client);
        }
    }
    /**
     * Removes an item from the account inventory.
     *
     * @param {BaseFullCharacter} character - The character to have their items removed.
     * @param {BaseItem} item - The item to remove.
     * @param {number} [count=1]  - Optional: Specifies the amount of items that need to be removed, default is 1.
     * @returns {boolean} Returns true if the item was successfully removed, false if there was an error.
     */
    async removeAccountItem(character, item, count = 1) {
        const client = this.getClientByCharId(character.characterId);
        if (!client)
            return false;
        item.stackCount -= count;
        if (item.stackCount <= 0) {
            await this.accountInventoriesManager.removeAccountItem(client.loginSessionId, item);
            this.sendData(client, "Items.RemoveAccountItem", {
                itemId: item.itemGuid,
                itemDefinitionId: item.itemDefinitionId
            });
        }
        else {
            await this.accountInventoriesManager.updateAccountItem(client.loginSessionId, item);
            this.sendData(client, "Items.UpdateAccountItem", {
                itemId: item.itemGuid,
                itemDefinitionId: item.itemDefinitionId,
                itemCount: item.stackCount
            });
        }
        return true;
    }
    /**
     * Removes an item from the loadout.
     *
     * @param {BaseFullCharacter} character - The character to have their items removed.
     * @param {number} loadoutSlotId - The loadout slot containing the item to remove.
     * @param {boolean} [updateEquipment=true] - Optional: Specifies whether to update the equipment, default is true.
     * @returns {boolean} Returns true if the item was successfully removed, false if there was an error.
     */
    removeLoadoutItem(character, loadoutSlotId, updateEquipment = true) {
        const item = character._loadout[loadoutSlotId], itemDefId = item?.itemDefinitionId; // save before item gets deleted
        if (!item || !itemDefId)
            return false;
        const client = this.getClientByContainerAccessor(character);
        if (client && this.isWeapon(item.itemDefinitionId)) {
            this.sendRemoteWeaponDataToAllOthers(client, client.character.transientId, "RemoteWeapon.RemoveWeapon", {
                guid: item.itemGuid
            });
        }
        if (client)
            this.deleteItem(character, item.itemGuid);
        delete character._loadout[loadoutSlotId];
        character.updateLoadout(this);
        if (client && this.isFootwear(itemDefId)) {
            this.updateFootwear(client, itemDefId, true);
        }
        if (updateEquipment) {
            this.clearEquipmentSlot(character, character.getActiveEquipmentSlot(item));
        }
        if (client) {
            this.checkNightVision(client);
        }
        if (this.getItemDefinition(itemDefId)?.ITEM_TYPE === enums_1.ItemTypes.CONTAINER) {
            delete character._containers[loadoutSlotId];
            if (client)
                this.initializeContainerList(client);
        }
        return true;
    }
    /**
     * Returns a client object by either the characterId of the passed character,
     * or the mountedCharacterId if the passed character is a BaseLootableEntity.
     *
     * @param {BaseFullCharacter | BaseLootableEntity} character - Either a Character or BaseLootableEntity to retrieve its accessing client.
     * @returns {Client | undefined} Returns client or undefined.
     */
    getClientByContainerAccessor(character) {
        let client = this.getClientByCharId(character.characterId);
        if (!client && character instanceof baselootableentity_1.BaseLootableEntity) {
            client = this.getClientByCharId(character.mountedCharacter || "");
        }
        return client;
    }
    /**
     * Removes items from a specific item stack in a container.
     *
     * @param {BaseFullCharacter} character - The character to have their items removed.
     * @param {BaseItem} item - The item object.
     * @param {LoadoutContainer} container - The container that has the item stack in it.
     * @param {number} [count] - Optional: The number of items to remove from the stack, default 1.
     * @returns {boolean} Returns true if the items were successfully removed, false if there was an error.
     */
    removeContainerItem(character, item, container, count) {
        if (item)
            item.debugFlag = "removeContainerItem";
        const client = this.getClientByContainerAccessor(character);
        if (!container || !item)
            return false;
        if (!count)
            count = item.stackCount;
        if (item.stackCount == count) {
            delete container.items[item.itemGuid];
            if (client)
                this.deleteItem(character, item.itemGuid);
        }
        else if (item.stackCount > count) {
            item.stackCount -= count;
            if (client)
                this.updateContainerItem(character, item, container);
        }
        else {
            // if count > removeItem.stackCount
            return false;
        }
        if (client)
            this.updateContainer(character, container);
        return true;
    }
    /**
     * Removes items from a specific item stack in the inventory, including containers and loadout.
     *
     * @param {BaseFullCharacter} character - The character to have their items removed.
     * @param {BaseItem} item - The item object.
     * @param {number} [count=1] - Optional: The number of items to remove from the stack, default 1.
     * @param {boolean} [updateEquipment=true] - Optional: Specifies whether to update the equipment, default is true.
     * @returns {boolean} Returns true if the items were successfully removed, false if there was an error.
     */
    async removeInventoryItem(character, item, count = 1, updateEquipment = true) {
        item.debugFlag = "removeInventoryItem";
        if (count > item.stackCount) {
            console.error(`RemoveInventoryItem: Not enough items in stack! Count ${count} > Stackcount ${item.stackCount}`);
            count = item.stackCount;
        }
        if (this.isAccountItem(item.itemDefinitionId)) {
            return await this.removeAccountItem(character, item);
        }
        else if (character._loadout[item.slotId]?.itemGuid == item.itemGuid) {
            return this.removeLoadoutItem(character, item.slotId, updateEquipment);
        }
        else {
            return this.removeContainerItem(character, item, character.getItemContainer(item.itemGuid), count);
        }
    }
    /**
     * Removes a specified amount of an item across all inventory containers /
     * loadout (LOADOUT DISABLED FOR NOW).
     * @param client The client to have its items removed.
     * @param itemDefinitionId The itemDefinitionId of the item(s) to be removed.
     * @param requiredCount Optional: The number of items to remove, default 1.
     * @returns Returns true if all items were successfully removed, false if there was an error.
     */
    removeInventoryItems(character, itemDefinitionId, requiredCount = 1) {
        const loadoutSlotId = 0; //this.getActiveLoadoutSlot(client, itemDefinitionId);
        // loadout disabled for now
        if (character._loadout[loadoutSlotId]?.itemDefinitionId == itemDefinitionId) {
            // todo: check multiple loadout slots for items
            return this.removeLoadoutItem(character, loadoutSlotId);
        }
        else {
            const removeItems = [];
            for (const container of Object.values(character._containers)) {
                if (!requiredCount)
                    break;
                for (const item of Object.values(container.items)) {
                    if (item.itemDefinitionId == itemDefinitionId) {
                        if (item.stackCount >= requiredCount) {
                            removeItems.push({ container, item, count: requiredCount });
                            requiredCount = 0;
                            break;
                        }
                        else {
                            removeItems.push({ container, item, count: item.stackCount });
                            requiredCount -= item.stackCount;
                        }
                    }
                }
            }
            if (requiredCount) {
                // inventory doesn't have enough items
                return false;
            }
            for (const itemStack of Object.values(removeItems)) {
                if (!this.removeContainerItem(character, itemStack.item, itemStack.container, itemStack.count)) {
                    return false;
                }
            }
            return true;
        }
    }
    /**
     * Removes a single item type from the inventory and spawns it on the ground
     * @param character The character that should drop the item
     * @param item The item object.
     * @param count Optional: The number of items to drop on the ground, default 1.
     * @param animationId The animation to play
     */
    dropItem(character, item, count = 1, animationId) {
        const client = this.getClientByContainerAccessor(character);
        if (!client)
            return;
        item.debugFlag = "dropItem";
        if (!item) {
            this.containerError(client, enums_1.ContainerErrors.NO_ITEM_IN_SLOT);
            return;
        }
        let dropItem;
        if (item.stackCount == count) {
            dropItem = item;
        }
        else if (item.stackCount > count) {
            dropItem = this.generateItem(item.itemDefinitionId, count);
        }
        else {
            return;
        }
        if (!this.removeInventoryItem(character, item, count))
            return;
        this.sendData(client, "Character.DroppedItemNotification", {
            characterId: client.character.characterId,
            itemDefId: item.itemDefinitionId,
            count: count
        });
        const itemDefinition = this.getItemDefinition(item.itemDefinitionId);
        this.startInteractionTimer(client, 0, 0, animationId);
        if (dropItem &&
            dropItem.weapon &&
            itemDefinition &&
            itemDefinition.ITEM_CLASS != enums_1.ItemClasses.WEAPON_THROWABLES) {
            const ammo = this.generateItem(this.getWeaponAmmoId(dropItem.itemDefinitionId), dropItem.weapon.ammoCount);
            if (ammo &&
                dropItem.weapon.ammoCount > 0 &&
                dropItem.weapon.itemDefinitionId != enums_1.Items.WEAPON_REMOVER) {
                client.character.lootContainerItem(this, ammo, ammo.stackCount, true);
            }
            dropItem.weapon.ammoCount = 0;
        }
        const obj = this.worldObjectManager.createLootEntity(this, dropItem, client.character.state.position, new Float32Array([0, Number(Math.random() * 10 - 5), 0, 1]));
        if (!obj)
            return;
        obj.insideBuilding = character.insideBuilding;
        this.executeFuncForAllReadyClientsInRange((c) => {
            c.spawnedEntities.add(obj);
            this.addLightweightNpc(c, obj);
            this.sendData(c, "ClientUpdate.ProximateItems", this.getProximityItems(c));
        }, obj);
    }
    pickupItem(client, guid) {
        const object = this._spawnedItems[guid], item = object.item;
        if (!item) {
            this.sendChatText(client, `[ERROR] Invalid item`);
            return;
        }
        this.sendCompositeEffectToAllWithSpawnedEntity(this._spawnedItems, object, this.getItemDefinition(item.itemDefinitionId)?.PICKUP_EFFECT ??
            enums_1.Effects.SFX_Item_PickUp_Generic);
        client.character.lootItem(this, item);
        this.startInteractionTimer(client, 0, 0, 9);
        if (item.itemDefinitionId === enums_1.Items.FUEL_BIOFUEL ||
            item.itemDefinitionId === enums_1.Items.FUEL_ETHANOL) {
            this.deleteEntity(object.characterId, this._explosives);
        }
        this.deleteEntity(guid, this._spawnedItems);
        delete this.worldObjectManager.spawnedLootObjects[object.spawnerId];
    }
    sendWeaponReload(client, weaponItem, ammoCount) {
        if (!weaponItem.weapon)
            return;
        const maxAmmo = this.getWeaponMaxAmmo(weaponItem.itemDefinitionId);
        this.sendWeaponData(client, "Weapon.Reload", {
            weaponGuid: weaponItem.itemGuid,
            unknownDword1: maxAmmo,
            ammoCount: ammoCount || weaponItem.weapon.ammoCount,
            unknownDword3: maxAmmo,
            currentReloadCount: (0, utils_1.toHex)(++weaponItem.weapon.currentReloadCount)
        });
    }
    deleteItem(character, itemGuid) {
        const characterIds = new Set();
        const vehicle = this._vehicles[character.characterId];
        if (vehicle) {
            vehicle.getPassengerList().forEach((characterId) => {
                characterIds.add(characterId);
            });
        }
        else {
            if (character instanceof baselootableentity_1.BaseLootableEntity) {
                characterIds.add(character.mountedCharacter || "");
            }
            else {
                characterIds.add(character.characterId);
            }
        }
        characterIds.forEach((characterId) => {
            const client = this.getClientByCharId(characterId);
            if (!client || !client.character?.initialized)
                return;
            this.sendData(client, "ClientUpdate.ItemDelete", {
                characterId: character instanceof character_1.Character2016 || character instanceof vehicle_1.Vehicle2016
                    ? character.characterId
                    : constants_1.EXTERNAL_CONTAINER_GUID,
                itemGuid: itemGuid
            });
        });
    }
    initializeContainerList(client, character = client.character) {
        const characterId = character instanceof character_1.Character2016 || character instanceof vehicle_1.Vehicle2016
            ? character.characterId
            : constants_1.EXTERNAL_CONTAINER_GUID;
        this.sendData(client, "Container.InitEquippedContainers", {
            ignore: characterId,
            characterId: characterId,
            containers: character.pGetContainers(this)
        });
    }
    addContainerItem(character, item, container, sendUpdate = true) {
        if (!item)
            return;
        const itemDefId = item.itemDefinitionId, client = this.getClientByContainerAccessor(character);
        item.slotId = Object.keys(container.items).length;
        item.containerGuid = container.itemGuid;
        container.items[item.itemGuid] = item;
        if (!client)
            return;
        if (item.weapon) {
            clearTimeout(item.weapon.reloadTimer);
            delete item.weapon.reloadTimer;
        }
        this.addItem(client, container.items[item.itemGuid], container.containerDefinitionId, character);
        this.updateContainer(character, container);
        if (sendUpdate && client.character.initialized) {
            this.sendData(client, "Reward.AddNonRewardItem", {
                itemDefId: itemDefId,
                iconId: this.getItemDefinition(itemDefId)?.IMAGE_SET_ID ?? 0,
                nameId: this.getItemDefinition(itemDefId)?.NAME_ID ?? 0,
                count: item.stackCount
            });
        }
    }
    updateLoadoutItem(client, item, character = client.character) {
        this.sendData(client, "ClientUpdate.ItemUpdate", {
            characterId: character.characterId,
            data: character.pGetItemData(this, item, constants_1.LOADOUT_CONTAINER_ID)
        });
        //this.updateLoadout(client.character);
    }
    updateContainer(character, container) {
        const characterIds = new Set();
        const vehicle = this._vehicles[character.characterId];
        if (vehicle) {
            vehicle.getPassengerList().forEach((characterId) => {
                characterIds.add(characterId);
            });
        }
        else {
            if (character instanceof baselootableentity_1.BaseLootableEntity) {
                characterIds.add(character.mountedCharacter || "");
            }
            else {
                characterIds.add(character.characterId);
            }
        }
        characterIds.forEach((characterId) => {
            const client = this.getClientByCharId(characterId);
            if (!client || !client.character?.initialized)
                return;
            this.sendData(client, "Container.UpdateEquippedContainer", {
                ignore: character.characterId,
                characterId: character.characterId,
                containerData: character.pGetContainerData(this, container)
            });
        });
    }
    updateContainerItem(character, item, container) {
        const client = this.getClientByContainerAccessor(character);
        if (!client || !client.character.initialized)
            return;
        this.sendData(client, "ClientUpdate.ItemUpdate", {
            characterId: character instanceof character_1.Character2016 || character instanceof vehicle_1.Vehicle2016
                ? character.characterId
                : constants_1.EXTERNAL_CONTAINER_GUID,
            data: character.pGetItemData(this, item, container.containerDefinitionId)
        });
        this.updateContainer(character, container);
    }
    updateItem(client, item) {
        const loadoutItem = client.character.getLoadoutItem(item.itemGuid);
        if (loadoutItem) {
            this.updateLoadoutItem(client, loadoutItem);
            return;
        }
        const container = client.character.getItemContainer(item.itemGuid);
        if (container) {
            this.updateContainerItem(client.character, item, container);
            return;
        }
        const mountedContainer = client.character.mountedContainer;
        if (mountedContainer) {
            const container = mountedContainer.getContainer();
            if (!container)
                return;
            this.updateContainerItem(mountedContainer, item, container);
        }
    }
    /**
     * Clears all items from a character's inventory.
     * @param client The client that'll have it's character's inventory cleared.
     */
    clearInventory(client, updateEquipment = true) {
        for (const item of Object.values(client.character._loadout)) {
            if (client.character._containers[item.slotId]) {
                const container = client.character._containers[item.slotId];
                for (const item of Object.values(container.items)) {
                    this.removeInventoryItem(client.character, item, item.stackCount);
                }
            }
            if (item.slotId != enums_1.LoadoutSlots.FISTS && item.itemDefinitionId) {
                this.removeInventoryItem(client.character, item, item.stackCount, updateEquipment);
            }
        }
    }
    isAdminItem(itemDefinitionId) {
        switch (itemDefinitionId) {
            case enums_1.Items.WEAPON_REMOVER:
            case enums_1.Items.FANNY_PACK_DEV:
                return true;
            default:
                return false;
        }
    }
    useAirdrop(client, character, item) {
        if (!this.airdropManager.allowedAirdropSpawn() && !client.isDebugMode) {
            this.sendAlert(client, "All planes are busy.");
            return;
        }
        if (utils_1._.size(this._clients) < this.airdropManager.minimumPlayers &&
            !this._soloMode &&
            !client.isDebugMode) {
            this.sendAlert(client, "No planes ready. Not enough survivors.");
            return;
        }
        let blockedArea = false;
        for (const a in this._constructionFoundations) {
            if ((0, utils_1.isPosInRadius)(50, this._constructionFoundations[a].state.position, client.character.state.position)) {
                blockedArea = true;
                break;
            }
        }
        if ((client.currentPOI || blockedArea) && !client.isDebugMode) {
            this.sendAlert(client, "You are too close to a restricted area.");
            return;
        }
        if (![enums_1.Items.AIRDROP_TICKET, enums_1.Items.AIRDROP_CODE].includes(item.itemDefinitionId) ||
            !this.airdropManager.spawnAirdrop(client.character.state.position, client.character.hasAirdropClearance ? "Hospital" : "", client.isDebugMode, client.character.characterId) ||
            !this.removeInventoryItem(character, item))
            return;
        this.sendAlert(client, "Your delivery is on the way!");
        if (client.character.hasAirdropClearance) {
            client.character.hasAirdropClearance = false;
        }
    }
    useAmmoBox(client, character, item) {
        const itemDef = this.getItemDefinition(item.itemDefinitionId);
        if (!itemDef)
            return;
        this.utilizeHudTimer(client, itemDef.NAME_ID, 5000, 0, () => {
            if (!this.removeInventoryItem(character, item))
                return;
            switch (item.itemDefinitionId) {
                case enums_1.Items.BUNDLE_GAUZE:
                case enums_1.Items.BUNDLE_EXPLOSIVE_ARROWS:
                case enums_1.Items.BUNDLE_FLAMING_ARROWS:
                case enums_1.Items.BUNDLE_WOODEN_ARROWS_1:
                case enums_1.Items.BUNDLE_WOODEN_ARROWS_2:
                case enums_1.Items.BUNDLE_WOODEN_ARROWS:
                    character.lootItem(this, this.generateItem(itemDef.PARAM1, itemDef.PARAM2), undefined, character instanceof character_1.Character2016);
                    break;
                case enums_1.Items.AMMO_BOX_223:
                    character.lootItem(this, this.generateItem(enums_1.Items.AMMO_223, 60), undefined, character instanceof character_1.Character2016);
                    break;
                case enums_1.Items.AMMO_BOX_45:
                    character.lootItem(this, this.generateItem(enums_1.Items.AMMO_45, 60), undefined, character instanceof character_1.Character2016);
                    break;
            }
        });
    }
    useConsumable(client, character, item, animationId) {
        const itemDef = this.getItemDefinition(item.itemDefinitionId);
        if (!itemDef)
            return;
        let doReturn = true;
        let drinkCount = 0;
        let eatCount = 0;
        let comfortCount = 0;
        let staminaCount = 0;
        let enduranceCount = 0;
        let givetrash = 0;
        let healCount = 0;
        let bandagingCount = 0;
        let timeout = 0;
        let healType;
        for (const a in useoptions_1.UseOptions) {
            if (useoptions_1.UseOptions[a].itemDef == item.itemDefinitionId &&
                (useoptions_1.UseOptions[a].type == enums_1.ItemUseOptions.EAT ||
                    useoptions_1.UseOptions[a].type == enums_1.ItemUseOptions.DRINK ||
                    useoptions_1.UseOptions[a].type == enums_1.ItemUseOptions.USE_MEDICAL)) {
                const useOption = useoptions_1.UseOptions[a];
                doReturn = false;
                timeout = useOption.timeout;
                if (useOption.drinkCount)
                    drinkCount = useOption.drinkCount;
                if (useOption.eatCount)
                    eatCount = useOption.eatCount;
                if (useOption.comfortCount)
                    comfortCount = useOption.comfortCount;
                if (useOption.staminaCount)
                    staminaCount = useOption.staminaCount;
                if (useOption.enduranceCount)
                    enduranceCount = useOption.enduranceCount;
                if (useOption.givetrash)
                    givetrash = useOption.givetrash;
                if (useOption.healCount)
                    healCount = useOption.healCount;
                if (useOption.bandagingCount)
                    bandagingCount = useOption.bandagingCount;
                if (useOption.healType)
                    healType = useOption.healType;
            }
        }
        if (doReturn) {
            this.sendChatText(client, "[ERROR] consumable not mapped to item Definition " +
                item.itemDefinitionId);
            return;
        }
        this.utilizeHudTimer(client, itemDef.NAME_ID, timeout, animationId, () => {
            this.useComsumablePass(client, character, item, eatCount, drinkCount, comfortCount, staminaCount, givetrash, healCount, bandagingCount, enduranceCount, healType);
        });
    }
    taskOptionPass(client, removedItem, rewardItems) {
        if (!this.removeInventoryItems(client.character, removedItem.itemDefinitionId, removedItem.count))
            return;
        rewardItems.forEach((itemInstance) => {
            const item = this.generateItem(itemInstance.itemDefinitionId, itemInstance.count);
            client.character.lootContainerItem(this, item);
        });
        this.lootCrateWithChance(client, 25);
    }
    igniteOption(client, item) {
        const itemDef = this.getItemDefinition(item.itemDefinitionId);
        if (!itemDef)
            return;
        let timeout = 0;
        let doReturn = true;
        for (const a in useoptions_1.UseOptions) {
            if (useoptions_1.UseOptions[a].itemDef == item.itemDefinitionId &&
                useoptions_1.UseOptions[a].type == enums_1.ItemUseOptions.IGNITE) {
                const useOption = useoptions_1.UseOptions[a];
                doReturn = false;
                timeout = useOption.timeout;
            }
        }
        if (doReturn) {
            this.sendChatText(client, "[ERROR] use option not mapped to item Definition " +
                item.itemDefinitionId);
            return;
        }
        this.utilizeHudTimer(client, itemDef.NAME_ID, timeout, 0, () => {
            this.igniteoptionPass(client);
        });
    }
    taskOption(client, timeout, nameId, removedItem, rewardItems) {
        this.utilizeHudTimer(client, nameId, timeout, 0, () => {
            this.taskOptionPass(client, removedItem, rewardItems);
        });
    }
    fillPass(client, character, item) {
        if (client.character.characterStates.inWater) {
            if (!this.removeInventoryItem(character, item))
                return;
            character.lootContainerItem(this, this.generateItem(enums_1.Items.WATER_DIRTY)); // give dirty water
        }
        else {
            this.sendAlert(client, "There is no water source nearby");
        }
    }
    sniffPass(client, character, item) {
        if (!this.removeInventoryItem(character, item))
            return;
        if (item.itemDefinitionId === enums_1.Items.SWIZZLE) {
            this.challengeManager.registerChallengeProgression(client, challengemanager_1.ChallengeType.SWIZZLE, 1);
            this.applyMovementModifier(client, enums_1.MovementModifiers.SWIZZLE);
        }
        else {
            this.applyMovementModifier(client, enums_1.MovementModifiers.ADRENALINE);
        }
    }
    fertilizePlants(client, character, item) {
        if (!this.removeInventoryItem(character, item))
            return;
        for (const characterId in this._temporaryObjects) {
            const object = this._temporaryObjects[characterId];
            if (object instanceof plantingdiameter_1.PlantingDiameter &&
                (0, utils_1.isPosInRadius)(3, object.state.position, client.character.state.position)) {
                object.isFertilized = true;
                object.fertilizedTimestamp = new Date().getTime() + 86400000; // + 1 day
                Object.values(object.seedSlots).forEach((plant) => {
                    if (plant.isFertilized)
                        return;
                    plant.isFertilized = true;
                    const roz = (plant.nextStateTime - new Date().getTime()) / 2;
                    plant.nextStateTime = new Date().getTime() + roz;
                    this.sendDataToAllWithSpawnedEntity(
                    // play burning effect & remove it after 15s
                    this._plants, plant.characterId, "Character.PlayWorldCompositeEffect", {
                        characterId: plant.characterId,
                        effectId: enums_1.Effects.EFX_Crop_Fertilizer,
                        position: new Float32Array([
                            plant.state.position[0],
                            plant.state.position[1],
                            plant.state.position[2],
                            1
                        ]),
                        effectTime: 180
                    });
                });
            }
        }
    }
    usePills(client, character, item) {
        if (!this.removeInventoryItem(character, item))
            return;
        let hudIndicator = undefined;
        switch (item.itemDefinitionId) {
            case enums_1.Items.COLD_MEDICINE:
                hudIndicator = this._hudIndicators["COLD_MEDICINE"];
                break;
            case enums_1.Items.IMMUNITY_BOOSTERS:
                hudIndicator = this._hudIndicators["IMMUNITY_BOOST_LOW"];
                break;
            case enums_1.Items.VITAMINS:
                hudIndicator = this._hudIndicators["IMMUNITY_BOOST_LOW"];
                break;
        }
        if (!hudIndicator)
            return;
        if (client.character.hudIndicators[hudIndicator.typeName]) {
            client.character.hudIndicators[hudIndicator.typeName].expirationTime +=
                600000;
        }
        else {
            client.character.hudIndicators[hudIndicator.typeName] = {
                typeName: hudIndicator.typeName,
                expirationTime: Date.now() + 600000
            };
            this.sendHudIndicators(client);
        }
    }
    useDeerScent(client, character, item) {
        client.character.isDeerScented = true;
        if (!this.removeInventoryItem(character, item))
            return;
        const hudIndicator = this._hudIndicators["DEER SCENT"];
        if (!hudIndicator)
            return;
        if (client.character.timeouts["DEER_SCENT"]) {
            client.character.timeouts["DEER_SCENT"]._onTimeout();
            clearTimeout(client.character.timeouts["DEER_SCENT"]);
            delete client.character.timeouts["DEER_SCENT"];
            if (client.character.hudIndicators[hudIndicator.typeName]) {
                client.character.hudIndicators[hudIndicator.typeName].expirationTime +=
                    300000;
            }
        }
        client.character.hudIndicators[hudIndicator.typeName] = {
            typeName: hudIndicator.typeName,
            expirationTime: Date.now() + 300000
        };
        this.sendHudIndicators(client);
        client.character.timeouts["DEER_SCENT"] = setTimeout(() => {
            if (!client.character.timeouts["DEER_SCENT"]) {
                return;
            }
            client.character.isDeerScented = false;
            delete client.character.timeouts["DEER_SCENT"];
        }, 300000);
    }
    sleep(client) {
        if (this.isBattleRoyale())
            return;
        client.character._resources[enums_1.ResourceIds.ENDURANCE] = 8000;
        client.character._resources[enums_1.ResourceIds.STAMINA] = 600;
        this.applyMovementModifier(client, enums_1.MovementModifiers.RESTED);
        this.sendDataToAllWithSpawnedEntity(this._characters, client.character.characterId, "Animation.Play", {
            characterId: client.character.characterId,
            animationId: 84 // yawning / waking up emote
        });
        this.updateResourceToAllWithSpawnedEntity(client.character.characterId, client.character._resources[enums_1.ResourceIds.STAMINA], enums_1.ResourceIds.STAMINA, enums_1.ResourceTypes.STAMINA, this._characters);
        this.updateResource(client, client.character.characterId, 8000, enums_1.ResourceIds.ENDURANCE, enums_1.ResourceTypes.ENDURANCE);
    }
    useItem(client, character, item, animationId) {
        const itemDef = this.getItemDefinition(item.itemDefinitionId);
        if (!itemDef)
            return;
        const nameId = itemDef.NAME_ID;
        let timeout = 0;
        let useoption = "";
        let doReturn = true;
        for (const a in useoptions_1.UseOptions) {
            if (useoptions_1.UseOptions[a].itemDef == item.itemDefinitionId &&
                useoptions_1.UseOptions[a].type == enums_1.ItemUseOptions.USE) {
                const useOption = useoptions_1.UseOptions[a];
                doReturn = false;
                timeout = useOption.timeout;
            }
        }
        if (doReturn) {
            this.sendChatText(client, "[ERROR] use option not mapped to item Definition " +
                item.itemDefinitionId);
            return;
        }
        switch (item.itemDefinitionId) {
            case enums_1.Items.WATER_EMPTY:
                useoption = "fill";
                break;
            case enums_1.Items.SWIZZLE:
            case enums_1.Items.ADRENALINE_SHOT:
                useoption = "adrenaline";
                timeout = 3000;
                break;
            case enums_1.Items.FERTILIZER:
                this.utilizeHudTimer(client, nameId, timeout, animationId, () => {
                    this.fertilizePlants(client, character, item);
                });
                return;
            case enums_1.Items.COLD_MEDICINE:
            case enums_1.Items.VITAMINS:
            case enums_1.Items.IMMUNITY_BOOSTERS:
                this.utilizeHudTimer(client, nameId, timeout, animationId, () => {
                    this.usePills(client, character, item);
                });
                return;
            case enums_1.Items.DEER_SCENT:
                this.utilizeHudTimer(client, nameId, timeout, animationId, () => {
                    this.useDeerScent(client, character, item);
                });
                return;
            default:
                this.sendChatText(client, "[ERROR] No use option mapped to item Definition " +
                    item.itemDefinitionId);
        }
        switch (useoption) {
            case "fill": // empty bottle
                this.utilizeHudTimer(client, nameId, timeout, animationId, () => {
                    this.fillPass(client, character, item);
                });
                break;
            case "sniff": // swizzle
                this.utilizeHudTimer(client, nameId, timeout, animationId, () => {
                    this.sniffPass(client, character, item);
                });
                break;
            case "adrenaline": // swizzle
                this.utilizeHudTimer(client, nameId, timeout, animationId, () => {
                    if (client.character.hudIndicators[this._hudIndicators["ADRENALINE"].typeName]) {
                        setTimeout(() => {
                            this.sendChatText(client, "You will die of an heart attack.");
                        }, 2000);
                        setTimeout(() => {
                            client.character.timeouts["ADRENALINE"]._onTimeout(true);
                            this.killCharacter(client, {
                                entity: client.character.characterId,
                                damage: 0xff
                            });
                        }, 5000);
                    }
                    else {
                        this.sniffPass(client, character, item);
                    }
                });
                break;
            default:
                return;
        }
    }
    sliceItem(client, character, item, animationId) {
        const itemDef = this.getItemDefinition(item.itemDefinitionId);
        if (!itemDef)
            return;
        const nameId = itemDef.NAME_ID;
        let timeout = 0;
        let doReturn = true;
        for (const a in useoptions_1.UseOptions) {
            if (useoptions_1.UseOptions[a].itemDef == item.itemDefinitionId &&
                useoptions_1.UseOptions[a].type == enums_1.ItemUseOptions.SLICE) {
                const useOption = useoptions_1.UseOptions[a];
                doReturn = false;
                timeout = useOption.timeout;
            }
        }
        if (doReturn) {
            this.sendChatText(client, "[ERROR] use option not mapped to item Definition " +
                item.itemDefinitionId);
            return;
        }
        this.utilizeHudTimer(client, nameId, timeout, animationId, () => {
            this.slicePass(character, item);
        });
    }
    refuelVehicle(client, character, item, vehicleGuid, animationId) {
        const itemDef = this.getItemDefinition(item.itemDefinitionId);
        if (!itemDef)
            return;
        const nameId = itemDef.NAME_ID;
        let fuelValue = 0;
        let timeout = 0;
        let doReturn = true;
        for (const a in useoptions_1.UseOptions) {
            if (useoptions_1.UseOptions[a].itemDef == item.itemDefinitionId &&
                useoptions_1.UseOptions[a].type == enums_1.ItemUseOptions.REFUEL) {
                const useOption = useoptions_1.UseOptions[a];
                doReturn = false;
                timeout = useOption.timeout;
                if (useOption.refuelCount)
                    fuelValue = useOption.refuelCount;
            }
        }
        if (doReturn) {
            this.sendChatText(client, "[ERROR] fuel not mapped to item Definition " + item.itemDefinitionId);
            return;
        }
        const vehicle = this._vehicles[vehicleGuid];
        if (!vehicle) {
            this.sendChatText(client, "[ERROR] Vehicle not found!");
            return;
        }
        if (vehicle._resources[enums_1.ResourceIds.FUEL] >= 10000) {
            this.sendAlert(client, "Fuel tank is full!");
            return;
        }
        if (vehicle.vehicleId == enums_1.VehicleIds.ATV) {
            fuelValue *= 2;
        }
        this.utilizeHudTimer(client, nameId, timeout, animationId, () => {
            this.refuelVehiclePass(client, character, item, vehicleGuid, fuelValue);
        });
    }
    shredItem(client, character, item, animationId) {
        const itemDefinition = this.getItemDefinition(item.itemDefinitionId);
        if (!itemDefinition)
            return;
        const nameId = itemDefinition.NAME_ID, itemType = itemDefinition.ITEM_TYPE;
        let count = 1;
        const timeout = 3000;
        if (item.itemDefinitionId === enums_1.Items.BELT_POUCH ||
            item.itemDefinitionId === enums_1.Items.WAIST_PACK) {
            count = 1;
        }
        else {
            switch (itemType) {
                case 36:
                case 39:
                    count = 1;
                    break;
                case 34: // salvage/shred
                    count = 4;
                    break;
                default:
                    this.sendChatText(client, "[ERROR] Unknown salvage item or count.");
            }
        }
        this.utilizeHudTimer(client, nameId, timeout, animationId, () => {
            this.shredItemPass(character, item, count);
        });
    }
    async salvageAmmo(client, character, item, animationId) {
        const itemDefinition = this.getItemDefinition(item.itemDefinitionId);
        if (!itemDefinition)
            return false;
        const nameId = itemDefinition.NAME_ID;
        const timeout = 1000;
        const allowedItems = [
            enums_1.Items.AMMO_12GA,
            enums_1.Items.AMMO_223,
            enums_1.Items.AMMO_308,
            enums_1.Items.AMMO_380,
            enums_1.Items.AMMO_44,
            enums_1.Items.AMMO_45,
            enums_1.Items.AMMO_762,
            enums_1.Items.AMMO_9MM
        ];
        if (!allowedItems.includes(item.itemDefinitionId)) {
            this.sendAlert(client, `[ERROR] Salvage option not mapped to item definition ${item.itemDefinitionId}`);
            return false;
        }
        const count = item.itemDefinitionId == enums_1.Items.AMMO_12GA ||
            item.itemDefinitionId == enums_1.Items.AMMO_762 ||
            item.itemDefinitionId == enums_1.Items.AMMO_308 ||
            item.itemDefinitionId == enums_1.Items.AMMO_44
            ? 2
            : 1;
        return await new Promise((resolve) => {
            this.utilizeHudTimer(client, nameId, timeout, animationId, async () => {
                if (client.character.mountedContainer != character &&
                    character != client.character) {
                    resolve(false);
                    return;
                }
                resolve(await this.salvageItemPass(character, item, count));
            });
        });
    }
    async useComsumablePass(client, character, item, eatCount, drinkCount, comfortCount, staminaCount, givetrash, healCount, bandagingCount, enduranceCount, healType) {
        if (!this.removeInventoryItem(character, item))
            return;
        if (eatCount) {
            client.character._resources[enums_1.ResourceIds.HUNGER] += eatCount;
            this.updateResource(client, client.character.characterId, client.character._resources[enums_1.ResourceIds.HUNGER], enums_1.ResourceIds.HUNGER);
        }
        if (drinkCount) {
            client.character._resources[enums_1.ResourceIds.HYDRATION] += drinkCount;
            this.updateResource(client, client.character.characterId, client.character._resources[enums_1.ResourceIds.HYDRATION], enums_1.ResourceIds.HYDRATION);
        }
        if (staminaCount) {
            client.character._resources[enums_1.ResourceIds.STAMINA] += staminaCount;
            this.updateResource(client, client.character.characterId, client.character._resources[enums_1.ResourceIds.STAMINA], enums_1.ResourceIds.STAMINA);
        }
        if (enduranceCount) {
            client.character._resources[enums_1.ResourceIds.ENDURANCE] += enduranceCount;
            this.updateResource(client, client.character.characterId, client.character._resources[enums_1.ResourceIds.ENDURANCE], enums_1.ResourceIds.ENDURANCE);
        }
        if (comfortCount) {
            client.character._resources[enums_1.ResourceIds.COMFORT] += comfortCount;
            this.updateResource(client, client.character.characterId, client.character._resources[enums_1.ResourceIds.COMFORT], enums_1.ResourceIds.COMFORT);
        }
        const poisonousFoods = [
            enums_1.Items.MEAT_ROTTEN,
            enums_1.Items.MEAT_BEAR,
            enums_1.Items.MEAT_VENISON,
            enums_1.Items.MEAT_RABBIT,
            enums_1.Items.MEAT_WOLF
        ];
        if (poisonousFoods.includes(item.itemDefinitionId)) {
            client.character.isPoisoned = true;
            for (let i = 0; i < 12; i++) {
                const damageInfo = {
                    entity: "",
                    damage: 25
                };
                client.character.damage(this, damageInfo);
                await promises_1.scheduler.wait(500);
            }
            client.character.isPoisoned = false;
        }
        if (item.itemDefinitionId == enums_1.Items.WATER_DIRTY) {
            const dmgInfoDirtyWater = {
                entity: "",
                damage: 500
            };
            client.character.damage(this, dmgInfoDirtyWater);
        }
        if (givetrash) {
            character.lootContainerItem(this, this.generateItem(givetrash), undefined, character instanceof character_1.Character2016);
        }
        if (item.itemDefinitionId == enums_1.Items.COFFEE_SUGAR) {
            client.character.isCoffeeSugared = true;
            for (let i = 0; i < 10; i++) {
                client.character._resources[enums_1.ResourceIds.STAMINA] += 40;
                this.updateResource(client, client.character.characterId, client.character._resources[enums_1.ResourceIds.STAMINA], enums_1.ResourceIds.STAMINA);
                client.character._resources[enums_1.ResourceIds.ENDURANCE] += 400;
                this.updateResource(client, client.character.characterId, client.character._resources[enums_1.ResourceIds.ENDURANCE], enums_1.ResourceIds.ENDURANCE);
                await promises_1.scheduler.wait(1000);
            }
            client.character.isCoffeeSugared = false;
        }
        if (bandagingCount && healCount) {
            if (!client.character.healingIntervals[healType]) {
                client.character.starthealingInterval(client, this, healType);
            }
            client.character.healType[healType].healingMaxTicks += healCount;
            if (client.character._resources[enums_1.ResourceIds.BLEEDING] > 0) {
                client.character._resources[enums_1.ResourceIds.BLEEDING] -= bandagingCount;
                const bleeding = client.character._resources[enums_1.ResourceIds.BLEEDING];
                this.updateResourceToAllWithSpawnedEntity(client.character.characterId, bleeding, enums_1.ResourceIds.BLEEDING, enums_1.ResourceTypes.BLEEDING, this._characters);
                if (client.character._resources[enums_1.ResourceIds.BLEEDING] < 0)
                    client.character._resources[enums_1.ResourceIds.BLEEDING] = 0;
            }
            this.updateResourceToAllWithSpawnedEntity(client.character.characterId, client.character._resources[enums_1.ResourceIds.BLEEDING], enums_1.ResourceIds.BLEEDING, enums_1.ResourceIds.BLEEDING, this._characters);
        }
    }
    slicePass(character, item) {
        if (!this.removeInventoryItem(character, item))
            return;
        if (item.itemDefinitionId == enums_1.Items.BLACKBERRY_PIE) {
            character.lootContainerItem(this, this.generateItem(enums_1.Items.BLACKBERRY_PIE_SLICE, 4));
        }
    }
    async igniteoptionPass(client) {
        for (const a in this._explosives) {
            const explosive = this._explosives[a];
            if ((0, utils_1.isPosInRadius)(2.0, client.character.state.position, explosive.state.position)) {
                explosive.ignite(this, client);
                return;
            }
        }
        for (const a in this._lootableConstruction) {
            const smeltable = this._lootableConstruction[a];
            if ((0, utils_1.isPosInRadius)(1.5, client.character.state.position, smeltable.state.position)) {
                if (smeltable instanceof lootableconstructionentity_1.LootableConstructionEntity) {
                    if (smeltable.subEntity instanceof smeltingentity_1.SmeltingEntity) {
                        if (smeltable.subEntity.isWorking)
                            continue;
                        smeltable.subEntity.isWorking = true;
                        const effectTime = Math.ceil(this.smeltingManager.burnTime / 1000) -
                            Math.floor((Date.now() - this.smeltingManager.lastBurnTime) / 1000);
                        this.smeltingManager._smeltingEntities[smeltable.characterId] =
                            smeltable.characterId;
                        this.sendDataToAllWithSpawnedEntity(smeltable.subEntity.dictionary, smeltable.characterId, "Character.PlayWorldCompositeEffect", {
                            characterId: smeltable.characterId,
                            effectId: smeltable.subEntity.workingEffect,
                            position: smeltable.state.position,
                            effectTime
                        });
                    }
                }
            }
        }
        for (const a in this._worldLootableConstruction) {
            const smeltable = this._worldLootableConstruction[a];
            if ((0, utils_1.isPosInRadius)(1.5, client.character.state.position, smeltable.state.position)) {
                if (smeltable instanceof lootableconstructionentity_1.LootableConstructionEntity) {
                    if (smeltable.subEntity instanceof smeltingentity_1.SmeltingEntity) {
                        if (smeltable.subEntity.isWorking)
                            continue;
                        smeltable.subEntity.isWorking = true;
                        const effectTime = Math.ceil(this.smeltingManager.burnTime / 1000) -
                            Math.floor((Date.now() - this.smeltingManager.lastBurnTime) / 1000);
                        this.smeltingManager._smeltingEntities[smeltable.characterId] =
                            smeltable.characterId;
                        this.sendDataToAllWithSpawnedEntity(smeltable.subEntity.dictionary, smeltable.characterId, "Character.PlayWorldCompositeEffect", {
                            characterId: smeltable.characterId,
                            effectId: smeltable.subEntity.workingEffect,
                            position: smeltable.state.position,
                            effectTime
                        });
                    }
                }
            }
        }
    }
    refuelVehiclePass(client, character, item, vehicleGuid, fuelValue) {
        // Also check if the vehicle is dismounted, jumping out of the car won't cancel the interaction
        if (!client.vehicle.mountedVehicle ||
            !this.removeInventoryItem(character, item))
            return;
        const vehicle = this._vehicles[vehicleGuid];
        vehicle._resources[enums_1.ResourceIds.FUEL] += fuelValue;
        // check if refuel amount is over 100, if so adjust to 100 to prevent over-fueling.
        if (vehicle._resources[enums_1.ResourceIds.FUEL] >= 10000) {
            vehicle._resources[enums_1.ResourceIds.FUEL] = 10000;
        }
        this.updateResourceToAllWithSpawnedEntity(vehicleGuid, vehicle._resources[enums_1.ResourceIds.FUEL], enums_1.ResourceIds.FUEL, enums_1.ResourceTypes.FUEL, this._vehicles);
    }
    shredItemPass(character, item, count) {
        if (!this.removeInventoryItem(character, item))
            return;
        character.lootItem(this, this.generateItem(enums_1.Items.CLOTH, count), undefined, character instanceof character_1.Character2016);
    }
    async salvageItemPass(character, item, count) {
        if (!this.removeInventoryItem(character, item))
            return false;
        if (item.itemDefinitionId == enums_1.Items.AMMO_12GA) {
            character.lootItem(this, this.generateItem(enums_1.Items.SHARD_PLASTIC, 1));
        }
        character.lootItem(this, this.generateItem(enums_1.Items.ALLOY_LEAD, count));
        character.lootItem(this, this.generateItem(enums_1.Items.SHARD_BRASS, 1));
        character.lootItem(this, this.generateItem(enums_1.Items.GUNPOWDER_REFINED, 1));
        return true;
    }
    repairOption(client, character, item, repairItem, animationId) {
        const durability = repairItem.currentDurability;
        if (durability >= 2000) {
            // todo: get max durability from somewhere, do not hard-code
            this.sendChatText(client, "This weapon is already at max durability.");
            return;
        }
        const itemDefinition = this.getItemDefinition(repairItem.itemDefinitionId);
        if (!itemDefinition)
            return;
        const weaponDefinitionId = itemDefinition.PARAM1;
        switch (weaponDefinitionId) {
            case enums_1.WeaponDefinitionIds.WEAPON_NAGAFENS_RAGE:
            case enums_1.WeaponDefinitionIds.WEAPON_REAPER:
            case enums_1.WeaponDefinitionIds.WEAPON_BLAZE:
            case enums_1.WeaponDefinitionIds.WEAPON_FROSTBITE:
            case enums_1.WeaponDefinitionIds.WEAPON_PURGE:
                this.sendChatText(client, "This weapon cannot be repaired.");
                return;
        }
        const itemDef = this.getItemDefinition(item.itemDefinitionId);
        if (!itemDef)
            return;
        const nameId = itemDef.NAME_ID;
        this.utilizeHudTimer(client, nameId, 5000, animationId, () => {
            this.repairOptionPass(client, character, item, repairItem, durability);
            this.challengeManager.registerChallengeProgression(client, challengemanager_1.ChallengeType.NO_WASTE, 1);
        });
    }
    repairOptionPass(client, character, item, repairItem, durability) {
        const diff = 2000 - durability, isGunKit = item.itemDefinitionId == enums_1.Items.GUN_REPAIR_KIT, repairAmount = diff < 500 ? diff : 500;
        if (!this.removeInventoryItem(character, item))
            return;
        if (isGunKit) {
            repairItem.currentDurability = 2000;
        }
        else {
            repairItem.currentDurability += repairAmount;
        }
        // used to update the item's durability on-screen regardless of container / loadout
        this.updateItem(client, repairItem);
    }
    createThrowableProjectile(client, packet, itemDefinition, createNpc, overrideProjectileId) {
        const characterId = this.generateGuid();
        const transientId = this.getTransientId(characterId);
        const npc = new projectileentity_1.ProjectileEntity(characterId, transientId, 0, packet.packet.position, new Float32Array([0, 0, 0, 0]), this, itemDefinition.ID, overrideProjectileId
            ? packet.packet.sessionProjectileCount +
                parseInt(client.character.characterId.slice(-5), 16)
            : packet.packet.projectileUniqueId, client.character.characterId);
        this._throwableProjectiles[npc.characterId] = npc;
        if (!createNpc)
            return;
        this.getClientsInRange(200, packet.packet.position).forEach((c) => {
            this.addLightweightNpc(c, npc);
            c.spawnedEntities.add(npc);
        });
    }
    handleWeaponFireStateUpdate(client, weaponItem, firestate) {
        if (firestate == 64) {
            // empty firestate
            this.sendRemoteWeaponUpdateDataToAllOthers(client, client.character.transientId, weaponItem.itemGuid, "Update.Empty", {});
            this.sendRemoteWeaponUpdateDataToAllOthers(client, client.character.transientId, weaponItem.itemGuid, "Update.FireState", {
                state: {
                    firestate: 64,
                    transientId: client.character.transientId,
                    position: client.character.state.position
                }
            });
        }
        // prevent empty weapons from entering an active firestate
        const itemDefinition = this.getItemDefinition(weaponItem.itemDefinitionId);
        if (!itemDefinition)
            return;
        const weaponDefinitionId = itemDefinition.PARAM1;
        if (!weaponItem.weapon?.ammoCount) {
            switch (weaponDefinitionId) {
                case enums_1.WeaponDefinitionIds.WEAPON_BOW_MAKESHIFT:
                case enums_1.WeaponDefinitionIds.WEAPON_BOW_RECURVE:
                case enums_1.WeaponDefinitionIds.WEAPON_BOW_WOOD:
                    break;
                default:
                    return;
            }
        }
        if (firestate > 0) {
            this.sendRemoteWeaponUpdateDataToAllOthers(client, client.character.transientId, weaponItem.itemGuid, "Update.Chamber", {});
        }
        this.sendRemoteWeaponUpdateDataToAllOthers(client, client.character.transientId, weaponItem.itemGuid, "Update.FireState", {
            state: {
                firestate: firestate,
                transientId: client.character.transientId,
                position: client.character.state.position
            }
        });
    }
    handleWeaponFire(client, weaponItem, packet) {
        if (!weaponItem.weapon || weaponItem.weapon.ammoCount <= 0) {
            return;
        }
        this.challengeManager.registerChallengeProgression(client, challengemanager_1.ChallengeType.GLOBAL_DISARMAMENT, 1);
        if (weaponItem.weapon.ammoCount > 0) {
            weaponItem.weapon.ammoCount -= 1;
        }
        /*if (!client.vehicle.mountedVehicle && this.fairPlayManager.fairPlayValues) {
          if (
            getDistance(client.character.state.position, packet.packet.position) >
            this.fairPlayManager.fairPlayValues?.maxPositionDesync
          ) {
            this.sendChatText(
              client,
              `FairPlay: Your shot didnt register due to position desync`
            );
            this.sendChatTextToAdmins(
              `FairPlay: ${
                client.character.name
              }'s shot didnt register due to position desync by ${getDistance(
                client.character.state.position,
                packet.packet.position
              )}`
            );
          }
        }*/
        /*const drift = Math.abs(
          packet.gameTime - getCurrentServerTimeWrapper().getTruncatedU32()
        );
        if (drift > this.fairPlayManager.maxPing + 200) {
          this.sendChatText(
            client,
            `FairPlay: Your shot didnt register due to packet loss or high ping`
          );
          this.sendChatTextToAdmins(
            `FairPlay: ${client.character.name}'s shot wasnt registered due to time drift by ${drift}`
          );
          return;
        }*/
        const itemDefinition = this.getItemDefinition(weaponItem.itemDefinitionId);
        if (!itemDefinition)
            return;
        const weaponDefinitionId = itemDefinition.PARAM1;
        const keys = Object.keys(client.fireHints);
        const lastFireHint = client.fireHints[Number(keys[keys.length - 1])];
        if (lastFireHint) {
            let blockedTime = 50;
            switch (weaponDefinitionId) {
                case enums_1.WeaponDefinitionIds.WEAPON_308:
                case enums_1.WeaponDefinitionIds.WEAPON_REAPER:
                    blockedTime = 1300;
                    break;
                case enums_1.WeaponDefinitionIds.WEAPON_SHOTGUN:
                case enums_1.WeaponDefinitionIds.WEAPON_NAGAFENS_RAGE:
                    blockedTime = 400;
                    break;
            }
            if (packet.gameTime - lastFireHint.timeStamp < blockedTime)
                return;
        }
        let hitNumber = 0;
        if (!client.vehicle.mountedVehicle &&
            !(0, utils_1.isPosInRadius)(3, client.character.state.position, packet.packet.position)) {
            hitNumber = 1;
        }
        const shotProjectiles = weaponDefinitionId == enums_1.WeaponDefinitionIds.WEAPON_SHOTGUN ||
            weaponDefinitionId == enums_1.WeaponDefinitionIds.WEAPON_NAGAFENS_RAGE
            ? 12
            : 1;
        for (let x = 0; x < shotProjectiles; x++) {
            const fireHint = {
                id: packet.packet.sessionProjectileCount + x,
                position: packet.packet.position,
                rotation: client.character.state.yaw,
                hitNumber: hitNumber,
                weaponItem: weaponItem,
                timeStamp: packet.gameTime,
                projectileUniqueId: packet.packet.sessionProjectileCount +
                    x +
                    parseInt(client.character.characterId.slice(-5), 16)
            };
            client.fireHints[packet.packet.sessionProjectileCount + x] = fireHint;
            setTimeout(() => {
                delete client.fireHints[packet.packet.sessionProjectileCount + x];
            }, 10000);
        }
        this.fairPlayManager.hitMissFairPlayCheck(this, client, false, "");
        this.stopHudTimer(client);
        this.sendRemoteWeaponUpdateDataToAllOthers(client, client.character.transientId, weaponItem.itemGuid, "Update.ProjectileLaunch", {});
        if (itemDefinition.ITEM_CLASS == enums_1.ItemClasses.THROWABLES) {
            this.createThrowableProjectile(client, packet, itemDefinition, true);
            const inv = client.character.getInventoryAsContainer();
            const similarItems = inv[itemDefinition.ID];
            const currentSlotId = weaponItem.slotId;
            // remove the equipped grenade
            this.removeInventoryItem(client.character, weaponItem);
            // auto-equip the next similar grenade from inventory if available
            if (similarItems && similarItems.length) {
                const nextNade = similarItems.find((v) => {
                    return v !== weaponItem;
                });
                if (nextNade) {
                    client.character.equipContainerItem(this, nextNade, currentSlotId);
                }
            }
            return;
        }
        else if (itemDefinition.ID == enums_1.Items.WEAPON_BOW_RECURVE) {
            this.createThrowableProjectile(client, packet, itemDefinition, false, true);
            return;
        }
        if (this.damageWeapons) {
            this.damageItem(client.character, weaponItem, 5);
        }
    }
    handleWeaponReload(client, weaponItem) {
        if (!weaponItem.weapon)
            return;
        if (weaponItem.weapon.reloadTimer)
            return;
        const maxAmmo = this.getWeaponMaxAmmo(weaponItem.itemDefinitionId); // max clip size
        if (weaponItem.weapon.ammoCount >= maxAmmo)
            return;
        // force 0 firestate so gun doesnt shoot randomly after reloading
        this.sendRemoteWeaponUpdateDataToAllOthers(client, client.character.transientId, weaponItem.itemGuid, "Update.FireState", {
            state: {
                firestate: 0,
                transientId: client.character.transientId,
                position: client.character.state.position
            }
        });
        this.sendRemoteWeaponUpdateDataToAllOthers(client, client.character.transientId, weaponItem.itemGuid, "Update.Reload", {});
        const weaponAmmoId = this.getWeaponAmmoId(weaponItem.itemDefinitionId), reloadTime = this.getWeaponReloadTime(weaponItem.itemDefinitionId);
        const itemDefinition = this.getItemDefinition(weaponItem.itemDefinitionId);
        if (!itemDefinition)
            return;
        const weaponDefinitionId = itemDefinition.PARAM1;
        // Allow reloading for grenades
        if (itemDefinition.ITEM_CLASS == enums_1.ItemClasses.WEAPON_THROWABLES) {
            weaponItem.weapon.ammoCount = 1;
            this.sendWeaponReload(client, weaponItem, 1);
            return;
        }
        switch (weaponDefinitionId) {
            case enums_1.WeaponDefinitionIds.WEAPON_BOW_MAKESHIFT:
            case enums_1.WeaponDefinitionIds.WEAPON_BOW_RECURVE:
            case enums_1.WeaponDefinitionIds.WEAPON_BOW_WOOD:
                weaponItem.weapon.reloadTimer = setTimeout(() => {
                    const currentWeapon = client.character.getEquippedWeapon();
                    if (!weaponItem.weapon ||
                        !currentWeapon ||
                        currentWeapon.itemGuid != weaponItem.itemGuid) {
                        client.character.clearReloadTimeout();
                        return;
                    }
                    if (!weaponItem.weapon?.reloadTimer) {
                        client.character.clearReloadTimeout();
                        return;
                    }
                    const maxReloadAmount = maxAmmo - weaponItem.weapon.ammoCount, // how much ammo is needed for full clip
                    reserveAmmo = client.character.getInventoryItemAmount(weaponAmmoId), // how much ammo is in inventory
                    reloadAmount = reserveAmmo >= maxReloadAmount ? maxReloadAmount : reserveAmmo; // actual amount able to reload
                    if (!this.removeInventoryItems(client.character, weaponAmmoId, reloadAmount)) {
                        client.character.clearReloadTimeout();
                        return;
                    }
                    this.sendWeaponReload(client, weaponItem, (weaponItem.weapon.ammoCount += reloadAmount));
                    client.character.clearReloadTimeout();
                }, reloadTime);
                return;
        }
        //#region SHOTGUN ONLY
        if (weaponAmmoId == enums_1.Items.AMMO_12GA) {
            weaponItem.weapon.reloadTimer = setTimeout(() => {
                if (!weaponItem.weapon?.reloadTimer) {
                    client.character.clearReloadTimeout();
                    return;
                }
                const reserveAmmo = // how much ammo is in inventory
                 client.character.getInventoryItemAmount(weaponAmmoId);
                if (!reserveAmmo ||
                    (weaponItem.weapon.ammoCount < maxAmmo &&
                        !this.removeInventoryItems(client.character, weaponAmmoId, 1)) ||
                    ++weaponItem.weapon.ammoCount == maxAmmo) {
                    this.sendWeaponReload(client, weaponItem);
                    this.sendRemoteWeaponUpdateDataToAllOthers(client, client.character.transientId, weaponItem.itemGuid, "Update.ReloadLoopEnd", {
                        endLoop: true
                    });
                    client.character.clearReloadTimeout();
                    return;
                }
                if (reserveAmmo - 1 < 0) {
                    // updated reserve ammo
                    this.sendWeaponReload(client, weaponItem);
                    this.sendRemoteWeaponUpdateDataToAllOthers(client, client.character.transientId, weaponItem.itemGuid, "Update.ReloadLoopEnd", {
                        endLoop: true
                    });
                    client.character.clearReloadTimeout();
                    return;
                }
                weaponItem.weapon.reloadTimer.refresh();
            }, reloadTime);
            return;
        }
        //#endregion
        weaponItem.weapon.reloadTimer = setTimeout(() => {
            const currentWeapon = client.character.getEquippedWeapon();
            if (!weaponItem.weapon?.reloadTimer ||
                !currentWeapon ||
                currentWeapon.itemGuid != weaponItem.itemGuid) {
                return;
            }
            const maxReloadAmount = maxAmmo - weaponItem.weapon.ammoCount, // how much ammo is needed for full clip
            reserveAmmo = client.character.getInventoryItemAmount(weaponAmmoId), // how much ammo is in inventory
            reloadAmount = reserveAmmo >= maxReloadAmount ? maxReloadAmount : reserveAmmo; // actual amount able to reload
            if (!this.removeInventoryItems(client.character, weaponAmmoId, reloadAmount)) {
                return;
            }
            this.sendWeaponReload(client, weaponItem, (weaponItem.weapon.ammoCount += reloadAmount));
            client.character.clearReloadTimeout();
        }, reloadTime);
    }
    pUtilizeHudTimer = (0, node_util_1.promisify)(this.utilizeHudTimer);
    stopHudTimer(client) {
        if (client.hudTimer === null) {
            // No timer running so nothing to do
            return;
        }
        this.utilizeHudTimer(client, 0, 0, 0, () => {
            client.hudTimer = null;
            this.sendDataToAllWithSpawnedEntity(this._characters, client.character.characterId, "CharacterState.InteractionStop", {
                characterId: client.character.characterId
            });
            // TODO: this should be somewhere else
            const vehicle = this._vehicles[client.vehicle.mountedVehicle ?? ""];
            if (!vehicle)
                return;
            vehicle.removeHotwireEffect(this);
        });
    }
    utilizeHudTimer(client, nameId, timeout, animationId, callback) {
        if (!animationId) {
            this.startTimer(client, nameId, timeout);
        }
        else {
            this.startInteractionTimer(client, nameId, timeout, animationId);
        }
        if (client.hudTimer != null) {
            clearTimeout(client.hudTimer);
        }
        client.posAtTimerStart = client.character.state.position;
        client.hudTimer = setTimeout(() => {
            callback.apply(this);
            client.hudTimer = null;
            this.sendDataToAllWithSpawnedEntity(this._characters, client.character.characterId, "CharacterState.InteractionStop", {
                characterId: client.character.characterId
            });
        }, timeout);
    }
    containerError(client, error) {
        switch (error) {
            case enums_1.ContainerErrors.DOES_NOT_ACCEPT_ITEMS:
                this.sendChatText(client, "Container Error: ContainerDoesNotAcceptItems");
                break;
            case enums_1.ContainerErrors.NOT_MUTABLE:
                this.sendChatText(client, "Container Error: ContainerIsNotMutable");
                break;
            case enums_1.ContainerErrors.NOT_CONSTRUCTED:
                this.sendChatText(client, "Container Error: ContainerNotConstructed");
                break;
            case enums_1.ContainerErrors.NO_SPACE:
                this.sendChatText(client, "Container Error: ContainerHasNoSpace");
                break;
            case enums_1.ContainerErrors.INVALID_LOADOUT_SLOT:
                this.sendChatText(client, "Container Error: InvalidLoadoutSlot");
                break;
            case enums_1.ContainerErrors.NO_PERMISSION:
                this.sendChatText(client, "Container Error: NoPermission");
            case enums_1.ContainerErrors.UNACCEPTED_ITEM:
                this.sendChatText(client, "Container Error: Item not accepted");
                break;
            default:
                this.sendData(client, "Container.Error", {
                    characterId: client.character.characterId,
                    containerError: error
                });
                break;
        }
    }
    clearMovementModifiers(client) {
        for (const a in client.character.timeouts) {
            client.character.timeouts[a]._onTimeout();
            clearTimeout(client.character.timeouts[a]);
            delete client.character.timeouts[a];
        }
    }
    applyMovementModifier(client, modifier) {
        this.multiplyMovementModifier(client, modifier);
        let hudIndicator;
        switch (modifier) {
            case enums_1.MovementModifiers.SWIZZLE:
                hudIndicator = this._hudIndicators["SWIZZLE"];
                if (client.character.timeouts["swizzle"]) {
                    client.character.timeouts["swizzle"]._onTimeout();
                    clearTimeout(client.character.timeouts["swizzle"]);
                    delete client.character.timeouts["swizzle"];
                    if (client.character.hudIndicators[hudIndicator.typeName]) {
                        client.character.hudIndicators[hudIndicator.typeName].expirationTime += 30000;
                    }
                }
                client.character.hudIndicators[hudIndicator.typeName] = {
                    typeName: hudIndicator.typeName,
                    expirationTime: Date.now() + 30000
                };
                this.sendHudIndicators(client);
                this.addScreenEffect(client, this._screenEffects["SWIZZLE"]);
                client.character.timeouts["swizzle"] = setTimeout(() => {
                    if (!client.character.timeouts["swizzle"]) {
                        return;
                    }
                    this.divideMovementModifier(client, modifier);
                    delete client.character.timeouts["swizzle"];
                }, 30000);
                break;
            case enums_1.MovementModifiers.RESTED:
                hudIndicator = this._hudIndicators["WELL_RESTED"];
                if (client.character.timeouts["WELL_RESTED"]) {
                    client.character.timeouts["WELL_RESTED"]._onTimeout();
                    clearTimeout(client.character.timeouts["WELL_RESTED"]);
                    delete client.character.timeouts["WELL_RESTED"];
                    if (client.character.hudIndicators[hudIndicator.typeName]) {
                        client.character.hudIndicators[hudIndicator.typeName].expirationTime += 120000;
                    }
                }
                client.character.hudIndicators[hudIndicator.typeName] = {
                    typeName: hudIndicator.typeName,
                    expirationTime: Date.now() + 120000
                };
                this.sendHudIndicators(client);
                client.character.timeouts["WELL_RESTED"] = setTimeout(() => {
                    if (!client.character.timeouts["WELL_RESTED"]) {
                        return;
                    }
                    this.divideMovementModifier(client, modifier);
                    delete client.character.timeouts["WELL_RESTED"];
                }, 120000);
                break;
            case enums_1.MovementModifiers.SNARED:
                if (client.character.timeouts["snared"]) {
                    client.character.timeouts["snared"]._onTimeout();
                    clearTimeout(client.character.timeouts["snared"]);
                    delete client.character.timeouts["snared"];
                }
                client.character.timeouts["snared"] = setTimeout(() => {
                    if (!client.character.timeouts["snared"]) {
                        return;
                    }
                    this.divideMovementModifier(client, modifier);
                    delete client.character.timeouts["snared"];
                }, 15000);
                break;
            case enums_1.MovementModifiers.ADRENALINE:
                hudIndicator = this._hudIndicators[enums_1.ResourceIndicators.ADRENALINE];
                if (client.character.timeouts["ADRENALINE"]) {
                    client.character.timeouts["ADRENALINE"]._onTimeout();
                    clearTimeout(client.character.timeouts["ADRENALINE"]);
                    delete client.character.timeouts["ADRENALINE"];
                    if (client.character.hudIndicators[hudIndicator.typeName]) {
                        client.character.hudIndicators[hudIndicator.typeName].expirationTime += 30000;
                    }
                }
                client.character.hudIndicators[hudIndicator.typeName] = {
                    typeName: hudIndicator.typeName,
                    expirationTime: Date.now() + 30000
                };
                this.sendHudIndicators(client);
                const adrenalineBoostInterval = (0, node_timers_1.setInterval)(() => {
                    client.character._resources[enums_1.ResourceIds.STAMINA] = 600;
                }, 1000);
                client.character.timeouts["ADRENALINE"] = setTimeout((skipAfterEffect) => {
                    if (!client.character.timeouts["ADRENALINE"]) {
                        return;
                    }
                    this.divideMovementModifier(client, modifier);
                    delete client.character.timeouts["ADRENALINE"];
                    (0, node_timers_1.clearInterval)(adrenalineBoostInterval);
                    if (!skipAfterEffect && client.character.isAlive) {
                        setTimeout(() => {
                            hudIndicator =
                                this._hudIndicators[enums_1.ResourceIndicators.ADRENALINE_AFTER_EFFECTS];
                            if (client.character.timeouts["ADRENALINE AFTER EFFECTS"]) {
                                client.character.timeouts["ADRENALINE AFTER EFFECTS"]._onTimeout();
                                clearTimeout(client.character.timeouts["ADRENALINE AFTER EFFECTS"]);
                                delete client.character.timeouts["ADRENALINE AFTER EFFECTS"];
                                if (client.character.hudIndicators[hudIndicator.typeName]) {
                                    client.character.hudIndicators[hudIndicator.typeName].expirationTime += 30000;
                                }
                            }
                            client.character.hudIndicators[hudIndicator.typeName] = {
                                typeName: hudIndicator.typeName,
                                expirationTime: Date.now() + 30000
                            };
                            this.sendHudIndicators(client);
                            const adrenalineAfterEffectsInterval = (0, node_timers_1.setInterval)(() => {
                                // 4% per interval
                                client.character._resources[enums_1.ResourceIds.STAMINA] -= 24;
                            }, 1000);
                            client.character.timeouts["ADRENALINE AFTER EFFECTS"] =
                                setTimeout(() => {
                                    (0, node_timers_1.clearInterval)(adrenalineAfterEffectsInterval);
                                    if (!client.character.timeouts["ADRENALINE AFTER EFFECTS"]) {
                                        return;
                                    }
                                    delete client.character.timeouts["ADRENALINE AFTER EFFECTS"];
                                    (0, node_timers_1.clearInterval)(adrenalineAfterEffectsInterval);
                                }, 30000);
                        }, 2000);
                    }
                }, 30000);
                break;
            case enums_1.MovementModifiers.BOOTS:
                // some stuff
                break;
        }
    }
    multiplyMovementModifier(client, modifier) {
        this.sendData(client, "ClientUpdate.ModifyMovementSpeed", {
            speed: modifier
        });
    }
    divideMovementModifier(client, modifier) {
        const modifierFixed = 1 / modifier;
        if (!client.character.initialized)
            return;
        this.sendData(client, "ClientUpdate.ModifyMovementSpeed", {
            speed: modifierFixed
        });
    }
    checkRespirator(character) {
        if (!character._equipment[enums_1.EquipSlots.FACE])
            return false;
        const itemGuid = character._equipment[enums_1.EquipSlots.FACE].guid;
        if (itemGuid) {
            const item = character.getInventoryItem(itemGuid);
            const itemDefinition = this.getItemDefinition(item?.itemDefinitionId ?? 0);
            if (!item || !itemDefinition)
                return false;
            if (itemDefinition.DESCRIPTION_ID == 11014) {
                return true;
            }
            else {
                return false;
            }
        }
    }
    updateFootwear(client, itemDefId, isRemoved = false) {
        if (!client.character.isReady)
            return;
        let movementType;
        if (this.isConvey(itemDefId) ||
            this.isZed(itemDefId) ||
            this.isGator(itemDefId)) {
            movementType = enums_1.MovementModifiers.CONVEYS;
        }
        else if (this.isBoot(itemDefId)) {
            movementType = enums_1.MovementModifiers.BOOTS;
        }
        if (movementType) {
            if (isRemoved) {
                this.divideMovementModifier(client, movementType);
            }
            else {
                this.multiplyMovementModifier(client, movementType);
            }
        }
        client.character.updateFootwearAudio(this);
    }
    checkNightVision(client, character = client.character) {
        if (character._loadout[29] &&
            character._loadout[29].itemDefinitionId == enums_1.Items.NV_GOGGLES)
            return;
        const index = character.screenEffects.indexOf("NIGHTVISION");
        if (index > -1) {
            character.screenEffects.splice(index, 1);
            this.removeScreenEffect(client, this._screenEffects["NIGHTVISION"]);
        }
    }
    detectEnasMovement(server, client, stanceFlags) {
        if (stanceFlags.CROUCHING) {
            if (client.character.isCrouching) {
                return;
            }
            client.character.isCrouching = true;
            if (Date.now() - client.character.lastCrouchTime <= 1500) {
                client.character.crouchCount++;
            }
            else {
                client.character.crouchCount = 0;
                client.character.lastCrouchTime = 0;
            }
            client.character.lastCrouchTime = Date.now();
            if (client.character.crouchCount >= 6) {
                server.sendData(client, "ClientUpdate.ModifyMovementSpeed", {
                    speed: 0.5
                });
                setTimeout(() => {
                    server.sendData(client, "ClientUpdate.ModifyMovementSpeed", {
                        speed: 2
                    });
                }, 1500);
                client.character.crouchCount = 0;
            }
        }
        else {
            client.character.isCrouching = false;
        }
    }
    //#endregion
    async reloadZonePacketHandlers() {
        //@ts-expect-error we reload it
        delete this._packetHandlers;
        delete require.cache[require.resolve("./zonepackethandlers")];
        this._packetHandlers =
            new (require("./zonepackethandlers").ZonePacketHandlers)();
        await this.reloadCommandCache();
    }
    generateGuid() {
        return (0, utils_1.generateRandomGuid)();
    }
    _sendRawDataReliable(client, data) {
        /*this.tasksManager.register(() => {
          this._gatewayServer.sendTunnelData(
            client.soeClientId,
            data,
            SOEOutputChannels.Reliable
          );
        });*/
        this._gatewayServer.sendTunnelData(client.soeClientId, data, soeoutputstream_1.SOEOutputChannels.Reliable);
    }
    sendRawDataReliable(client, data) {
        this._sendRawDataReliable(client, data);
    }
    sendUnbufferedRawDataReliable(client, data) {
        this._sendRawDataReliable(client, data);
    }
    getAllCurrentUsedTransientId() {
        const allTransient = {};
        for (const key in this._doors) {
            const door = this._doors[key];
            allTransient[door.transientId] = key;
        }
        for (const key in this._lootableProps) {
            const prop = this._lootableProps[key];
            allTransient[prop.transientId] = key;
        }
        for (const key in this._vehicles) {
            const vehicle = this._vehicles[key];
            allTransient[vehicle.transientId] = key;
        }
        for (const key in this._npcs) {
            const npc = this._npcs[key];
            allTransient[npc.transientId] = key;
        }
        for (const key in this._spawnedItems) {
            const object = this._spawnedItems[key];
            allTransient[object.transientId] = key;
        }
        return allTransient;
    }
    async fetchLoginInfo() {
        const resolver = new node_dns_1.Resolver();
        const loginServerAddress = await (0, utils_1.resolveHostAddress)(resolver, "loginserver.h1emu.com");
        this._loginServerInfo.address = loginServerAddress[0];
    }
    executeFuncForAllReadyClients(callback) {
        for (const client in this._clients) {
            const clientObj = this._clients[client];
            if (!clientObj.isLoading) {
                callback(clientObj);
            }
        }
    }
    executeFuncForAllReadyClientsInRange(callback, entity) {
        for (const client in this._clients) {
            const clientObj = this._clients[client];
            if (!clientObj.isLoading &&
                (0, utils_1.isPosInRadius)(entity.npcRenderDistance || this.charactersRenderDistance, clientObj.character.state.position, entity.state.position)) {
                callback(clientObj);
            }
        }
    }
    async startRoutinesLoop() {
        if (utils_1._.size(this._clients) <= 0) {
            this.routinesLoopTimer = setTimeout(() => {
                this.startRoutinesLoop();
            }, 3000);
            return;
        }
        for (const a in this._clients) {
            while (this.isSaving) {
                await promises_1.scheduler.wait(500);
            }
            const startTime = Date.now();
            const client = this._clients[a];
            if (!client.isLoading) {
                client.routineCounter++;
                this.constructionManager.constructionPermissionsManager(this, client);
                if (!this.disableMapBoundsCheck) {
                    this.checkInMapBounds(client);
                }
                this.checkZonePing(client);
                if (client.routineCounter >= 3) {
                    this.createFairPlayInternalPacket(client);
                    this.assignChunkRenderDistance(client);
                    this.removeOutOfDistanceEntities(client);
                    if (!this.disablePOIManager) {
                        this.POIManager(client);
                    }
                    client.routineCounter = 0;
                }
                //this.constructionManager.spawnConstructionParentsInRange(this, client); // put back into grid for now
                this.vehicleManager(client);
                this.spawnGridObjects(client); // Spawn base parts before the player
                this.spawnCharacters(client);
                //this.constructionManager.worldConstructionManager(this, client); // put into grid
                client.posAtLastRoutine = client.character.state.position;
            }
            const endTime = Date.now();
            const timeTaken = endTime - startTime;
            if (timeTaken > this.tickRate) {
                console.log(`Routine took ${timeTaken}ms to execute, which is more than the tickRate ${this.tickRate}`);
            }
            await promises_1.scheduler.wait(this.tickRate, {});
        }
        this.updateSpectatorMap();
        this.startRoutinesLoop();
    }
    executeRoutine(client) {
        this.constructionManager.constructionPermissionsManager(this, client);
        //this.constructionManager.spawnConstructionParentsInRange(this, client); // put into grid
        this.vehicleManager(client);
        this.removeOutOfDistanceEntities(client);
        this.spawnGridObjects(client); // Spawn base parts before the player
        this.spawnCharacters(client);
        //this.constructionManager.worldConstructionManager(this, client);
        if (!this.disablePOIManager) {
            this.POIManager(client);
        }
        client.posAtLastRoutine = client.character.state.position;
    }
    firstRoutine(client) {
        //this.constructionManager.spawnConstructionParentsInRange(this, client); // put into grid
        this.spawnLoadingGridObjects(client);
        this.spawnCharacters(client);
        //this.constructionManager.worldConstructionManager(this, client);
        if (!this.disablePOIManager) {
            this.POIManager(client);
        }
        client.posAtLastRoutine = client.character.state.position;
    }
    async checkZonePing(client) {
        const ping = this._gatewayServer.getSoeClientAvgPing(client.soeClientId);
        if (client.isAdmin ||
            Number(client.character.lastLoginDate) + 30000 > new Date().getTime() ||
            !ping) {
            return;
        }
        client.zonePings.push(ping > 400 ? 400 : ping); // dont push values higher than 400, that would increase average value drasticaly and it's the resend rate
        if (ping >= this.fairPlayManager.maxPing) {
            this.sendAlert(client, `Your ping is very high: ${ping}. You may be kicked soon`);
            client.pingWarnings += 1;
        }
        else {
            client.pingWarnings = 0;
        }
        if (client.zonePings.length < 15)
            return;
        const averagePing = client.zonePings.reduce((a, b) => a + b, 0) / client.zonePings.length;
        if (averagePing >= this.fairPlayManager.maxPing &&
            client.pingWarnings > 3) {
            this.kickPlayer(client);
            this.sendChatTextToAdmins(`${client.character.name} has been been kicked for average ping: ${averagePing}`);
            return;
        }
        client.zonePings.splice(0, 1); // remove oldest ping val
    }
    checkInMapBounds(client) {
        let inMapBounds = false;
        this._spawnGrid.forEach((cell) => {
            const pos = client.character.state.position;
            if (pos[0] >= cell.position[0] - cell.width / 2 &&
                pos[0] <= cell.position[0] + cell.width / 2 &&
                pos[2] >= cell.position[2] - cell.height / 2 &&
                pos[2] <= cell.position[2] + cell.height / 2) {
                inMapBounds = true;
            }
        });
        const index = client.character.screenEffects.indexOf("OUTOFMAPBOUNDS");
        if (!inMapBounds && (!client.isAdmin || !client.isDebugMode)) {
            const damageInfo = {
                entity: "Server.OutOfMapBounds",
                damage: 1000
            };
            this.sendAlert(client, `The radiation here seems to be dangerously high`);
            client.character.damage(this, damageInfo);
            if (index <= -1) {
                client.character.screenEffects.push("OUTOFMAPBOUNDS");
                this.addScreenEffect(client, this._screenEffects["OUTOFMAPBOUNDS"]);
            }
        }
        else {
            if (index > -1) {
                client.character.screenEffects.splice(index, 1);
                this.removeScreenEffect(client, this._screenEffects["OUTOFMAPBOUNDS"]);
            }
        }
    }
    initHudIndicatorDataSource() {
        hudIndicators.forEach((indicator) => {
            this._hudIndicators[indicator.typeName] = {
                id: indicator.ID,
                typeName: indicator.typeName,
                nameId: indicator.nameId,
                descriptionId: indicator.descriptionId,
                imageSetId: indicator.imageSetId
            };
        });
    }
    initScreenEffectDataSource() {
        screenEffects.forEach((effect) => {
            this._screenEffects[effect.typeName] = {
                effectId: effect.id,
                typeName: effect.typeName,
                duration: effect.duration,
                screenBrightness: effect.screenBrightness,
                colorGradingFilename: effect.colorGradingFilename,
                colorGrading: effect.colorGrading,
                screenCover: effect.screenCover,
                transparency: effect.transparency,
                color: parseInt(effect.color, 16),
                unknownDword3: effect?.unknownDword3 ?? 0,
                unknownDword7: effect?.unknownDword7 ?? 0,
                unknownDword16: effect?.unknownDword16 ?? 0,
                unknownDword17: effect?.unknownDword17 ?? 0,
                unknownDword18: effect?.unknownDword18 ?? 0,
                unknownDword19: effect?.unknownDword19 ?? 0
            };
        });
    }
    createFairPlayInternalPacket(client) {
        // send fake packet for plugin to handle
        const packet = {
            name: "FairPlay.Internal",
            flag: 0,
            data: null
        };
        this.onZoneDataEvent(client, packet);
    }
    initClientEffectsDataSource() {
        clientEffectsDataSource.forEach((clientEffect) => {
            this._clientEffectsData[clientEffect.SERVER_EFFECT_ID] = {
                id: clientEffect.ABILITY_ID,
                typeName: clientEffect.TYPE_NAME,
                animationName: clientEffect.STRING1
            };
        });
    }
    initModelsDataSource() {
        models.forEach((model) => {
            this._modelsData[model.ID] = {
                id: model.ID,
                fileName: model.MODEL_FILE_NAME,
                materialType: model.MATERIAL_TYPE
            };
        });
    }
    initUseOptionsDataSource() {
        itemUseOptionsDataSource.forEach((useOption) => {
            this._itemUseOptions[useOption.ITEM_USE_OPTION_ID] = {
                id: useOption.ITEM_USE_OPTION_ID,
                typeName: useOption.TYPE_NAME,
                animationId: useOption.INTERACTION_ANIMATION_ID
            };
        });
    }
    sendHudIndicators(client) {
        this.sendData(client, "Effect.RemoveUiIndicators", {
            unknownData1: {
                unknownQword1: client.character.characterId
            },
            unknownData2: {}
        });
        client.character.resourceHudIndicators.forEach((typeName, index) => {
            const indicator = this._hudIndicators[typeName];
            if (!indicator) {
                // to help identifying the issue
                console.log(`Unknown hud indicator: ${typeName} removing it`);
                client.character.resourceHudIndicators.splice(index, 1);
                return;
            }
            this.sendData(client, "Effect.AddUiIndicator", {
                characterId: client.character.characterId,
                hudElementGuid: this.generateGuid(),
                unknownData1: {
                    hudElementId: indicator.nameId
                },
                hudElementData: {
                    nameId: indicator.nameId,
                    descriptionId: indicator.descriptionId,
                    imageSetId: indicator.imageSetId
                },
                unknownData3: {},
                unknownData4: {},
                unknownData5: {}
            });
        });
        for (const a in client.character.hudIndicators) {
            const indicator = this._hudIndicators[client.character.hudIndicators[a].typeName];
            this.sendData(client, "Effect.AddUiIndicator", {
                characterId: client.character.characterId,
                hudElementGuid: this.generateGuid(),
                unknownData1: {
                    hudElementId: indicator.nameId
                },
                hudElementData: {
                    nameId: indicator.nameId,
                    descriptionId: indicator.descriptionId,
                    imageSetId: indicator.imageSetId
                },
                unknownData3: {},
                unknownData4: {},
                unknownData5: {}
            });
        }
    }
    addScreenEffect(client, effect) {
        this.sendData(client, "ScreenEffect.ApplyScreenEffect", effect);
    }
    removeScreenEffect(client, effect) {
        this.sendData(client, "ScreenEffect.RemoveScreenEffect", effect);
    }
    _sendDataToAll(packetName, obj) {
        const data = this._protocol.pack(packetName, obj);
        if (data) {
            for (const a in this._clients) {
                this.sendRawDataReliable(this._clients[a], data);
            }
        }
    }
    sendDataToAll(packetName, obj) {
        this._sendDataToAll(packetName, obj);
    }
    dropVehicleManager(client, vehicleGuid) {
        this.sendManagedObjectResponseControlPacket(client, {
            control: false,
            objectCharacterId: vehicleGuid
        });
        client.managedObjects.splice(client.managedObjects.findIndex((e) => e === vehicleGuid), 1);
        delete this._vehicles[vehicleGuid]?.manager;
    }
    sendZonePopulationUpdate() {
        let populationNumber = utils_1._.size(this._clients);
        this._loginConnectionManager.sendData({
            ...this._loginServerInfo,
            // TODO: what a dirty hack
            serverId: Infinity
        }, "UpdateZonePopulation", { population: populationNumber });
    }
    filterOutOfDistance(element, playerPosition) {
        return !(0, utils_1.isPosInRadius)(element.npcRenderDistance || this.charactersRenderDistance, playerPosition, element.state.position);
    }
    dismissVehicle(vehicleGuid) {
        this.sendDataToAll("Character.RemovePlayer", {
            characterId: vehicleGuid
        });
        this.deleteEntity(vehicleGuid, this._vehicles);
    }
    /**
     * Generates a new transientId and maps it to a provided characterId.
     * @param characterId The characterId to map the transientId to.
     */
    getTransientId(characterId) {
        const generatedTransient = this._transientIdGenerator.next()
            .value;
        this._transientIds[generatedTransient] = characterId;
        this._characterIds[characterId] = generatedTransient;
        return generatedTransient;
    }
    /**
     * Reloads all packet handlers, structures, and commands for the entire server.
     * @param client The client that called the function.
     */
    reloadPackets(client) {
        this.reloadZonePacketHandlers();
        this._protocol.reloadPacketDefinitions();
        this.sendChatText(client, "[DEV] Packets reloaded", true);
    }
    /**
     * Disconnects a client from the zone.
     * @param client The client to be disconnected
     */
    timeoutClient(client) {
        if (!!this._clients[client.sessionId]) {
            // if hasn't already deleted
            debug(`Client (${client.soeClientId}) disconnected ( ping timeout )`);
            this.deleteClient(client);
        }
    }
    pSetImmediate = (0, node_util_1.promisify)(setImmediate);
    pSetTimeout = (0, node_util_1.promisify)(setTimeout);
    sendChatText(client, message, clearChat) {
        this.chatManager.sendChatText(this, client, message, clearChat);
    }
    sendChatTextToAllOthers(client, message, clearChat = false) {
        this.chatManager.sendChatText(this, client, message, clearChat);
    }
    sendChatTextToAdmins(message, clearChat = false) {
        this.chatManager.sendChatTextToAdmins(this, message, clearChat);
    }
    sendGlobalChatText(message, clearChat = false) {
        this.chatManager.sendGlobalChatText(this, message, clearChat);
    }
    sendConsoleText(client, message, showConsole = false, clearOutput = false) {
        this.sendData(client, "H1emu.PrintToConsole", {
            message,
            showConsole,
            clearOutput
        });
    }
    sendConsoleTextToAdmins(message, showConsole = false, clearOutput = false) {
        for (const a in this._clients) {
            const client = this._clients[a];
            if (client.isAdmin) {
                this.sendData(client, "H1emu.PrintToConsole", {
                    message,
                    showConsole,
                    clearOutput
                });
            }
        }
    }
    playerNotFound(client, inputString, targetClient) {
        if (typeof targetClient == "string") {
            this.sendChatText(client, `Could not find player "${inputString.toLowerCase()}", did you mean "${targetClient}"?`);
            return true;
        }
        return false;
    }
    getShaderParameterGroup(itemDefinitionId) {
        return (this.dynamicappearance.SHADER_SEMANTIC_DEFINITIONS.find((definition) => {
            return definition.SHADER_PARAMETER_GROUP_ID == itemDefinitionId;
        })?.SHADER_PARAMETER_GROUP ?? []);
    }
    getShaderGroupId(itemDefinitionId) {
        return (this.dynamicappearance.ITEM_APPEARANCE_DEFINITIONS.filter((definition) => {
            return definition.ITEM_APPEARANCE_DATA.ITEM_ID == itemDefinitionId;
        })[0]?.ITEM_APPEARANCE_DATA?.SHADER_PARAMETER_GROUP_ID ?? 0);
    }
    deployParachute(client, shaderGroupId) {
        const characterId = this.generateGuid(), vehicle = new vehicle_1.Vehicle2016(characterId, this.getTransientId(characterId), 9374, client.character.state.position, client.character.state.rotation, this, (0, utils_1.getCurrentServerTimeWrapper)().getTruncatedU32(), enums_1.VehicleIds.PARACHUTE, shaderGroupId, client.character.characterId);
        this.worldObjectManager.createVehicle(this, vehicle, true);
        if (!this._vehicles[characterId]) {
            return;
        }
        this.sendData(client, "AddLightweightVehicle", {
            ...vehicle.pGetLightweightVehicle(),
            unknownGuid1: this.generateGuid()
        });
        client.spawnedEntities.add(vehicle);
        vehicle.onReadyCallback = (client) => {
            this.sendData(client, "Vehicle.AutoMount", {
                guid: vehicle.characterId,
                unknownBoolean1: true,
                unknownDword1: 0
            });
        };
    }
    updateSpectatorMap() {
        this.sendDataToAllAdmins("Spectator.AllSpectators", {
            unknownDword1: 1,
            unknownArray1: Object.values(this._clients).map((client) => ({
                characterId: client.character.characterId,
                characterName: client?.character?.name ?? "",
                playerHeading: (0, utils_1.quat2heading)(client.character.state.rotation),
                playerX: client.character.state.position[0],
                playerY: client.character.state.position[1],
                playerZ: client.character.state.position[2],
                unknownArray2: []
            })),
            unknownArray2: []
        });
    }
    addToSyncTeleport(client, position) {
        this._syncTeleport[client.character.characterId] = false;
        this.sendData(client, "SynchronizedTeleport.WaitingForPlayers", {});
        this.sendData(client, "ClientUpdate.UpdateLocation", {
            position: position,
            triggerLoadingScreen: true
        });
        client.character.state.position = position;
    }
    updateSyncTeleport() {
        const readyClients = [];
        // This is so some actions can hold players for some extra time if needed
        // A little sidenote, if you want players to hold for 20 seconds. set it to 2 since the worldRoutine triggers every 10 seconds by default
        if (this.blockSyncTeleportTicks > 0) {
            this.blockSyncTeleportTicks--;
            return;
        }
        for (const characterId in this._syncTeleport) {
            const client = this.getClientByCharId(characterId);
            if (!client) {
                delete this._syncTeleport[characterId];
                continue;
            }
            if (!this._syncTeleport[characterId]) {
                return;
            }
            readyClients.push(client);
        }
        if (readyClients.length == 0)
            return;
        this._syncTeleport = {};
        readyClients.forEach((client) => {
            this.sendData(client, "SynchronizedTeleport.PlayersReady", {
                cooldown: 5000
            });
        });
        setTimeout(() => {
            readyClients.forEach((client) => {
                this.sendData(client, "SynchronizedTeleport.Release", {});
            });
        }, 5000);
    }
    isSurvival() {
        return this.gameMode == enums_1.GameModes.SURVIVAL;
    }
    isBattleRoyale() {
        return this.gameMode > 0;
    }
}
exports.ZoneServer2016 = ZoneServer2016;
if (process.env.VSCODE_DEBUG === "true") {
    const PackageSetting = require("../../../package.json");
    process.env.H1Z1_SERVER_VERSION = PackageSetting.version;
    new ZoneServer2016(1117, Buffer.from(constants_1.DEFAULT_CRYPTO_KEY, "base64"), process.env.MONGO_URL, 2).start();
}
//# sourceMappingURL=zoneserver.js.map